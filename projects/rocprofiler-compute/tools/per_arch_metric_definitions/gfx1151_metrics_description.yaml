System Speed-of-Light:
  VALU FLOPs (FP16):
    plain: 'Sixteen-bit VALU throughput (GFLOP/s) from SQ_INSTS_VALU × 64 / kernel time. Theoretical peak uses packed FP16 at 2× the FP32 per-CU rate: max_sclk (MHz) × $cu_per_gpu × 256 / 1000 GFLOP/s. Public RDNA 3.5 summaries cite dual-CU WGPs, dual-issue VALU, and chip FP16 peak ~2× FP32; this formula is the same 2× relationship at the per-CU model used here. $cu_per_gpu is total CU count, not WGP (1 WGP = 2 CUs on RDNA 3.5).'
    rst: 'Sixteen-bit VALU throughput (GFLOP/s) from SQ_INSTS_VALU × 64 / kernel time. Theoretical peak uses packed FP16 at 2× the FP32 per-CU rate: max_sclk (MHz) × $cu_per_gpu × 256 / 1000 GFLOP/s. Public RDNA 3.5 summaries cite dual-CU WGPs, dual-issue VALU, and chip FP16 peak ~2× FP32; this formula is the same 2× relationship at the per-CU model used here. $cu_per_gpu is total CU count, not WGP (1 WGP = 2 CUs on RDNA 3.5).'
    unit: GFLOP/s
  VALU FLOPs (FP32):
    plain: 'Thirty-two-bit VALU throughput (GFLOP/s) from SQ_INSTS_VALU × 64 / kernel time. Theoretical peak: max_sclk (MHz) × $cu_per_gpu × 128 / 1000 GFLOP/s (wave64 / dual-issue VALU class used for Navi3-style parts). $cu_per_gpu is the total number of Compute Units from system info, not WGP count (on RDNA 3.5, one WGP pairs two CUs).'
    rst: 'Thirty-two-bit VALU throughput (GFLOP/s) from SQ_INSTS_VALU × 64 / kernel time. Theoretical peak: max_sclk (MHz) × $cu_per_gpu × 128 / 1000 GFLOP/s (wave64 / dual-issue VALU class used for Navi3-style parts). $cu_per_gpu is the total number of Compute Units from system info, not WGP count (on RDNA 3.5, one WGP pairs two CUs).'
    unit: GFLOP/s
  IPC:
    plain: Instructions Per Cycle — ratio of instructions executed over busy cycles. Calculated as (Total Instructions - Internal Instructions) / Busy Cycles. Higher IPC indicates better instruction throughput.
    rst: Instructions Per Cycle — ratio of instructions executed over busy cycles. Calculated as (Total Instructions - Internal Instructions) / Busy Cycles. Higher IPC indicates better instruction throughput.
    unit: Instr/cycle
  Wavefront Occupancy:
    plain: 'Average wavefront occupancy proxy: SQ_WAVE_CYCLES_sum / SQ_BUSY_CYCLES_avr per sample, where SQ_BUSY_CYCLES_avr is the mean of per–shader-engine SQ_BUSY_CYCLES (active-wave cycles per SE). Peak is $max_waves_per_cu × $cu_per_gpu (theoretical in-flight cap). Pct of peak scales the average against that ceiling.'
    rst: 'Average wavefront occupancy proxy: SQ_WAVE_CYCLES_sum / SQ_BUSY_CYCLES_avr per sample, where SQ_BUSY_CYCLES_avr is the mean of per–shader-engine SQ_BUSY_CYCLES (active-wave cycles per SE). Peak is $max_waves_per_cu × $cu_per_gpu (theoretical in-flight cap). Pct of peak scales the average against that ceiling.'
    unit: Wavefronts
  L2 Cache Hit Rate:
    plain: 'GL2C last-level cache hit rate (same as Memory Chart / GL2C panel). Formula: 100 * GL2C_HIT_sum / (GL2C_HIT_sum + GL2C_MISS_sum) when the denominator is non-zero.'
    rst: 'GL2C last-level cache hit rate (same as Memory Chart / GL2C panel). Formula: 100 * GL2C_HIT_sum / (GL2C_HIT_sum + GL2C_MISS_sum) when the denominator is non-zero.'
    unit: Percent
  L2-Fabric Read BW:
    plain: 'GL2C/EA read bandwidth from sized request counters (gl2c_perf_sel_ea_rdreq_32b/64B/96B/128B): 32·GL2C_EA_RDREQ_32B_sum + 64·GL2C_EA_RDREQ_64B_sum + 96·GL2C_EA_RDREQ_96B_sum + 128·GL2C_EA_RDREQ_128B_sum, divided by kernel wall time. Sums over GL2C instances. Peak / % of peak are N/A (no sysinfo memory BW).'
    rst: 'GL2C/EA read bandwidth from sized request counters (gl2c_perf_sel_ea_rdreq_32b/64B/96B/128B): 32·GL2C_EA_RDREQ_32B_sum + 64·GL2C_EA_RDREQ_64B_sum + 96·GL2C_EA_RDREQ_96B_sum + 128·GL2C_EA_RDREQ_128B_sum, divided by kernel wall time. Sums over GL2C instances. Peak / % of peak are N/A (no sysinfo memory BW).'
    unit: Bytes/s
  L2-Fabric Write BW:
    plain: 'GL2C/EA write bandwidth: GL2C_MC_WRREQ_sum is the EA write transaction count (32 B or 64 B per txn; the spec names this path MC_WRREQ). Bandwidth = 32·(GL2C_MC_WRREQ_sum − GL2C_EA_WRREQ_64B_sum) + 64·GL2C_EA_WRREQ_64B_sum, over time. Peak / % of peak are N/A (no sysinfo memory BW).'
    rst: 'GL2C/EA write bandwidth: GL2C_MC_WRREQ_sum is the EA write transaction count (32 B or 64 B per txn; the spec names this path MC_WRREQ). Bandwidth = 32·(GL2C_MC_WRREQ_sum − GL2C_EA_WRREQ_64B_sum) + 64·GL2C_EA_WRREQ_64B_sum, over time. Peak / % of peak are N/A (no sysinfo memory BW).'
    unit: Bytes/s
  LDS Bank Conflicts:
    plain: Number of LDS bank conflicts. Peak / % of Peak are N/A for raw counts.
    rst: Number of LDS bank conflicts. Peak / % of Peak are N/A for raw counts.
    unit: Conflicts
  Wave Dependency Wait:
    plain: Percent of wave lifetime spent waiting for data dependencies (e.g. VMEM/LDS results).
    rst: Percent of wave lifetime spent waiting for data dependencies (e.g. VMEM/LDS results).
    unit: Percent
  Wave Issue Wait:
    plain: Percent of wave lifetime spent waiting for any instruction to be ready to issue.
    rst: Percent of wave lifetime spent waiting for any instruction to be ready to issue.
    unit: Percent
  TCP Cache Hit Rate:
    plain: 'Texture Cache Per-pipe (TCP) hit rate. TCP is the L0 vector cache in RDNA3.5. Same as Memory Chart TCP Hit Rate: 100 * (TCP_REQ_sum - TCP_REQ_MISS_sum) / TCP_REQ_sum.'
    rst: 'Texture Cache Per-pipe (TCP) hit rate. TCP is the L0 vector cache in RDNA3.5. Same as Memory Chart TCP Hit Rate: 100 * (TCP_REQ_sum - TCP_REQ_MISS_sum) / TCP_REQ_sum.'
    unit: Percent
  TCP Cache BW:
    plain: 'Same as Memory Chart “TCP Request Bandwidth” (table 303): TCP_REQ_sum × 64 B / kernel wall time. Peak / % of peak are N/A (same as Memory Chart — no theoretical ceiling row).'
    rst: 'Same as Memory Chart “TCP Request Bandwidth” (table 303): TCP_REQ_sum × 64 B / kernel wall time. Peak / % of peak are N/A (same as Memory Chart — no theoretical ceiling row).'
    unit: Bytes/s
  GL1C Hit Rate:
    plain: 'GL1 Cache hit rate (same as Memory Chart / GL1C panel). Formula: 100 * (GL1C_REQ_sum - GL1C_REQ_MISS_sum) / GL1C_REQ_sum when GL1C_REQ_sum != 0.'
    rst: 'GL1 Cache hit rate (same as Memory Chart / GL1C panel). Formula: 100 * (GL1C_REQ_sum - GL1C_REQ_MISS_sum) / GL1C_REQ_sum when GL1C_REQ_sum != 0.'
    unit: Percent
  GL1C Read BW:
    plain: 'Same as Memory Chart table 307 “GL1C-GL2 Read Bandwidth”: 32·GL1C_GL2_REQ_READ_32B_sum + 64·GL1C_GL2_REQ_READ_64B_sum + 128·GL1C_GL2_REQ_READ_128B_sum, over time. Peak / % of peak are N/A.'
    rst: 'Same as Memory Chart table 307 “GL1C-GL2 Read Bandwidth”: 32·GL1C_GL2_REQ_READ_32B_sum + 64·GL1C_GL2_REQ_READ_64B_sum + 128·GL1C_GL2_REQ_READ_128B_sum, over time. Peak / % of peak are N/A.'
    unit: Bytes/s
  GL2C Cache BW:
    plain: 'Same as Memory Chart table 308 “GL2C Read Bandwidth”: sized client read bins plus 96 B compressed reads, over time. Peak / % of peak are N/A.'
    rst: 'Same as Memory Chart table 308 “GL2C Read Bandwidth”: sized client read bins plus 96 B compressed reads, over time. Peak / % of peak are N/A.'
    unit: Bytes/s
  Scalar Data Cache Hit Rate:
    plain: 'Same as Memory Chart Dcache Hit Rate: 100 * SQC_DCACHE_HITS_sum / (SQC_DCACHE_HITS_sum + SQC_DCACHE_MISSES_sum) when the denominator is non-zero.'
    rst: 'Same as Memory Chart Dcache Hit Rate: 100 * SQC_DCACHE_HITS_sum / (SQC_DCACHE_HITS_sum + SQC_DCACHE_MISSES_sum) when the denominator is non-zero.'
    unit: Percent
  Scalar Data Cache BW:
    plain: 'Same as Memory Chart table 302 “Dcache-GL1 Read Bandwidth”: SQC_TC_DATA_READ_REQ_sum × 128 B / kernel time. Peak / % of peak are N/A.'
    rst: 'Same as Memory Chart table 302 “Dcache-GL1 Read Bandwidth”: SQC_TC_DATA_READ_REQ_sum × 128 B / kernel time. Peak / % of peak are N/A.'
    unit: Bytes/s
  Instruction Cache Hit Rate:
    plain: 'Same as Memory Chart Icache Hit Rate: 100 * SQC_ICACHE_HITS_sum / (SQC_ICACHE_HITS_sum + SQC_ICACHE_MISSES_sum) when the denominator is non-zero.'
    rst: 'Same as Memory Chart Icache Hit Rate: 100 * SQC_ICACHE_HITS_sum / (SQC_ICACHE_HITS_sum + SQC_ICACHE_MISSES_sum) when the denominator is non-zero.'
    unit: Percent
  Instruction Cache BW:
    plain: 'Same as Memory Chart table 301 “ICache-GL1 Read Bandwidth”: SQC_TC_INST_REQ_sum × 128 B / kernel time. Peak / % of peak are N/A.'
    rst: 'Same as Memory Chart table 301 “ICache-GL1 Read Bandwidth”: SQC_TC_INST_REQ_sum × 128 B / kernel time. Peak / % of peak are N/A.'
    unit: Bytes/s
Memory chart - Instruction Cache:
  ICache Utilization:
    plain: 'Definition: Percentage of cycles the instruction cache is busy. Formula: 100 * SQC_ICACHE_BUSY_CYCLES_sum / SQ_BUSY_CYCLES_sum'
    rst: 'Definition: Percentage of cycles the instruction cache is busy. Formula: 100 * SQC_ICACHE_BUSY_CYCLES_sum / SQ_BUSY_CYCLES_sum'
    unit: Percent
  ICache Hit Rate:
    plain: 'Definition: Share of instruction cache accesses that hit, using hit+miss as denominator. Formula: 100 * SQC_ICACHE_HITS_sum / (SQC_ICACHE_HITS_sum + SQC_ICACHE_MISSES_sum) when the denominator is non-zero. HITS/REQ alone can exceed 100% (per-SQ per-bank counters).'
    rst: 'Definition: Share of instruction cache accesses that hit, using hit+miss as denominator. Formula: 100 * SQC_ICACHE_HITS_sum / (SQC_ICACHE_HITS_sum + SQC_ICACHE_MISSES_sum) when the denominator is non-zero. HITS/REQ alone can exceed 100% (per-SQ per-bank counters).'
    unit: Percent
  ICache Miss Rate:
    plain: 'Definition: Share of instruction cache accesses that miss (same denominator as hit rate). Formula: 100 * SQC_ICACHE_MISSES_sum / (SQC_ICACHE_HITS_sum + SQC_ICACHE_MISSES_sum)'
    rst: 'Definition: Share of instruction cache accesses that miss (same denominator as hit rate). Formula: 100 * SQC_ICACHE_MISSES_sum / (SQC_ICACHE_HITS_sum + SQC_ICACHE_MISSES_sum)'
    unit: Percent
  ICache Requests:
    plain: 'Count of instruction-cache (SQC ICache) requests issued, per normalization unit. Formula: SQC_ICACHE_REQ_sum / $denom'
    rst: 'Count of instruction-cache (SQC ICache) requests issued, per normalization unit. Formula: SQC_ICACHE_REQ_sum / $denom'
    unit: Requests per Normalization Unit
  ICache Request Stall Rate:
    plain: 'Percent of shader busy cycles where the instruction cache input interface was stalled (valid without ready). Formula: 100 * SQC_ICACHE_INPUT_VALID_READYB_sum / SQ_BUSY_CYCLES_sum.'
    rst: 'Percent of shader busy cycles where the instruction cache input interface was stalled (valid without ready). Formula: 100 * SQC_ICACHE_INPUT_VALID_READYB_sum / SQ_BUSY_CYCLES_sum.'
    unit: Percent
  ICache-GL1 Read Bandwidth:
    plain: Bytes per second of read traffic from the instruction path toward GL1 (texture-cache path instruction requests), using 128 B per SQC_TC_INST_REQ event.
    rst: Bytes per second of read traffic from the instruction path toward GL1 (texture-cache path instruction requests), using 128 B per SQC_TC_INST_REQ event.
    unit: Bytes/s
Memory chart - Scalar Data Cache:
  Dcache Utilization:
    plain: 'Definition: Percentage of cycles the scalar data cache is busy. Formula: 100 * SQC_DCACHE_BUSY_CYCLES_sum / SQ_BUSY_CYCLES_sum'
    rst: 'Definition: Percentage of cycles the scalar data cache is busy. Formula: 100 * SQC_DCACHE_BUSY_CYCLES_sum / SQ_BUSY_CYCLES_sum'
    unit: Percent
  Dcache Hit Rate:
    plain: 'Definition: Share of scalar data cache accesses that hit (hit+miss denominator). Formula: 100 * SQC_DCACHE_HITS_sum / (SQC_DCACHE_HITS_sum + SQC_DCACHE_MISSES_sum) when the denominator is non-zero.'
    rst: 'Definition: Share of scalar data cache accesses that hit (hit+miss denominator). Formula: 100 * SQC_DCACHE_HITS_sum / (SQC_DCACHE_HITS_sum + SQC_DCACHE_MISSES_sum) when the denominator is non-zero.'
    unit: Percent
  Dcache Requests:
    plain: 'Count of scalar data-cache (SQC DCache) requests, per normalization unit. Formula: SQC_DCACHE_REQ_sum / $denom'
    rst: 'Count of scalar data-cache (SQC DCache) requests, per normalization unit. Formula: SQC_DCACHE_REQ_sum / $denom'
    unit: Requests per Normalization Unit
  Dcache Request Stall Rate:
    plain: 'Percent of shader busy cycles where the scalar data cache input interface was stalled. Formula: 100 * SQC_DCACHE_INPUT_VALID_READYB_sum / SQ_BUSY_CYCLES_sum.'
    rst: 'Percent of shader busy cycles where the scalar data cache input interface was stalled. Formula: 100 * SQC_DCACHE_INPUT_VALID_READYB_sum / SQ_BUSY_CYCLES_sum.'
    unit: Percent
  Dcache-GL1 Read Bandwidth:
    plain: Bytes per second of scalar read traffic from SQC toward GL1 (SQC_TC_DATA_READ_REQ at 128 B per request).
    rst: Bytes per second of scalar read traffic from SQC toward GL1 (SQC_TC_DATA_READ_REQ at 128 B per request).
    unit: Bytes/s
Memory chart - TCP Cache (Vector L0):
  TCP Hit Rate:
    plain: 'Definition: Percentage of TCP (L0 vector cache) requests that hit in cache. Formula: 100 * (TCP_REQ_sum - TCP_REQ_MISS_sum) / TCP_REQ_sum'
    rst: 'Definition: Percentage of TCP (L0 vector cache) requests that hit in cache. Formula: 100 * (TCP_REQ_sum - TCP_REQ_MISS_sum) / TCP_REQ_sum'
    unit: Percent
  TCP Total Requests:
    plain: Total TCP (vector L0) requests per normalization unit (reads, writes and related traffic aggregated in TCP_REQ_sum).
    rst: Total TCP (vector L0) requests per normalization unit (reads, writes and related traffic aggregated in TCP_REQ_sum).
    unit: Requests per Normalization Unit
  TCP Read Requests:
    plain: TCP read requests per normalization unit (TCP_REQ_READ_sum).
    rst: TCP read requests per normalization unit (TCP_REQ_READ_sum).
    unit: Requests per Normalization Unit
  TCP Write Requests:
    plain: TCP write requests per normalization unit (TCP_REQ_WRITE_sum).
    rst: TCP write requests per normalization unit (TCP_REQ_WRITE_sum).
    unit: Requests per Normalization Unit
  TCP Miss Requests:
    plain: TCP requests that missed in the L0 vector cache and required a fill from GL1C (TCP_REQ_MISS_sum / $denom).
    rst: TCP requests that missed in the L0 vector cache and required a fill from GL1C (TCP_REQ_MISS_sum / $denom).
    unit: Requests per Normalization Unit
  TCP Request Bandwidth:
    plain: Heuristic TCP request throughput using 64 B per TCP_REQ event over the kernel window.
    rst: Heuristic TCP request throughput using 64 B per TCP_REQ event over the kernel window.
    unit: Bytes/s
Memory chart - LDS (Local Data Share):
  LDS Atomic Instructions:
    plain: 'Definition: Number of LDS atomic instructions executed. Description: Atomic operations on Local Data Share memory with return values. Formula: SQ_LDS_ATOMIC_RETURN_sum / $denom'
    rst: 'Definition: Number of LDS atomic instructions executed. Description: Atomic operations on Local Data Share memory with return values. Formula: SQ_LDS_ATOMIC_RETURN_sum / $denom'
    unit: Instructions per Normalization Unit
  LDS Bank Conflict Rate:
    plain: 'Definition: Percentage of LDS accesses that resulted in bank conflicts. Description: Bank conflicts occur when multiple work-items access the same LDS bank simultaneously. Formula: 100 * SQC_LDS_BANK_CONFLICT_sum / SQC_LDS_IDX_ACTIVE_sum'
    rst: 'Definition: Percentage of LDS accesses that resulted in bank conflicts. Description: Bank conflicts occur when multiple work-items access the same LDS bank simultaneously. Formula: 100 * SQC_LDS_BANK_CONFLICT_sum / SQC_LDS_IDX_ACTIVE_sum'
    unit: Percent
  LDS Estimated Bandwidth:
    plain: 'Definition: Estimated LDS bandwidth based on active index cycles minus bank conflicts. Description: RDNA3.5 has 32 LDS banks per CU, each capable of 4 bytes per cycle. Formula: ((SQC_LDS_IDX_ACTIVE_sum - SQC_LDS_BANK_CONFLICT_sum) * 4 * 32) / time'
    rst: 'Definition: Estimated LDS bandwidth based on active index cycles minus bank conflicts. Description: RDNA3.5 has 32 LDS banks per CU, each capable of 4 bytes per cycle. Formula: ((SQC_LDS_IDX_ACTIVE_sum - SQC_LDS_BANK_CONFLICT_sum) * 4 * 32) / time'
    unit: Bytes/s
  LDS Instructions:
    plain: Total LDS (Local Data Share) instructions executed per normalization unit (SQ_INSTS_LDS_sum).
    rst: Total LDS (Local Data Share) instructions executed per normalization unit (SQ_INSTS_LDS_sum).
    unit: Instructions per Normalization Unit
  LDS Instruction Cycles:
    plain: Cycles spent executing LDS instructions per normalization unit (SQ_INST_CYCLES_LDS_sum).
    rst: Cycles spent executing LDS instructions per normalization unit (SQ_INST_CYCLES_LDS_sum).
    unit: Cycles per Normalization Unit
  LDS Wait Cycles:
    plain: Cycles waves spent waiting on LDS (wait-state attribution), per normalization unit (SQ_WAIT_INST_LDS_sum).
    rst: Cycles waves spent waiting on LDS (wait-state attribution), per normalization unit (SQ_WAIT_INST_LDS_sum).
    unit: Cycles per Normalization Unit
Memory chart - GL1C Cache (L1):
  GL1C Utilization:
    plain: 'Definition: Percentage of cycles the GL1C (L1 cache) is busy. Formula: 100 * GL1C_BUSY_sum / GL1C_CYCLE_sum'
    rst: 'Percentage of cycles the GL1C (L1 cache) is busy. Formula: 100 * GL1C_BUSY_sum / GL1C_CYCLE_sum'
    unit: Percent
  GL1C Hit Rate:
    plain: 'Definition: Percentage of GL1C cache requests that hit. Formula: 100 * (GL1C_REQ_sum - GL1C_REQ_MISS_sum) / GL1C_REQ_sum'
    rst: 'Percentage of GL1C cache requests that hit. Formula: 100 * (GL1C_REQ_sum - GL1C_REQ_MISS_sum) / GL1C_REQ_sum'
    unit: Percent
  GL1C Total Requests:
    plain: Total GL1C cache requests per normalization unit (GL1C_REQ_sum).
    rst: Total GL1C cache requests per normalization unit (GL1C_REQ_sum).
    unit: Requests per Normalization Unit
  GL1C Read Requests:
    plain: GL1C read requests per normalization unit (GL1C_REQ_READ_sum).
    rst: GL1C read requests per normalization unit (GL1C_REQ_READ_sum).
    unit: Requests per Normalization Unit
  GL1C Write Requests:
    plain: GL1C write requests per normalization unit (GL1C_REQ_WRITE_sum).
    rst: GL1C write requests per normalization unit (GL1C_REQ_WRITE_sum).
    unit: Requests per Normalization Unit
  GL1C Miss Requests:
    plain: GL1C miss events per normalization unit (GL1C_REQ_MISS_sum).
    rst: GL1C miss events per normalization unit (GL1C_REQ_MISS_sum).
    unit: Requests per Normalization Unit
  GL1C Starve Rate:
    plain: 'Percent of GL1C cycles with no requests from TCP (starvation). Formula: 100 * GL1C_STARVE_sum / GL1C_CYCLE_sum.'
    rst: 'Percent of GL1C cycles with no requests from TCP (starvation). Formula: 100 * GL1C_STARVE_sum / GL1C_CYCLE_sum.'
    unit: Percent
  GL1C Stall GL2 Backpressure:
    plain: 'Percent of GL1C busy cycles spent stalled on GL2 backpressure. Formula: 100 * GL1C_STALL_GL2_GL1_sum / GL1C_BUSY_sum.'
    rst: 'Percent of GL1C busy cycles spent stalled on GL2 backpressure. Formula: 100 * GL1C_STALL_GL2_GL1_sum / GL1C_BUSY_sum.'
    unit: Percent
Memory chart - GL2C Cache (L2):
  GL2C Hit Rate:
    plain: 'Definition: Percentage of GL2C (L2 cache) requests that hit. Formula: 100 * GL2C_HIT_sum / (GL2C_HIT_sum + GL2C_MISS_sum)'
    rst: 'Percentage of GL2C (L2 cache) requests that hit. Formula: 100 * GL2C_HIT_sum / (GL2C_HIT_sum + GL2C_MISS_sum)'
    unit: Percent
  GL2C Utilization:
    plain: 'Percent of GL2C cycles the L2 cache was busy. Formula: 100 * GL2C_BUSY_sum / GL2C_CYCLE_sum.'
    rst: 'Percent of GL2C cycles the L2 cache was busy. Formula: 100 * GL2C_BUSY_sum / GL2C_CYCLE_sum.'
    unit: Percent
  GL2C Total Requests:
    plain: Total L2 client requests per normalization unit (GL2C_REQ_sum).
    rst: Total L2 client requests per normalization unit (GL2C_REQ_sum).
    unit: Requests per Normalization Unit
  GL2C Read Requests:
    plain: L2 read requests per normalization unit (GL2C_READ_sum).
    rst: L2 read requests per normalization unit (GL2C_READ_sum).
    unit: Requests per Normalization Unit
  GL2C Write Requests:
    plain: L2 write requests per normalization unit (GL2C_WRITE_sum).
    rst: L2 write requests per normalization unit (GL2C_WRITE_sum).
    unit: Requests per Normalization Unit
  GL2C Atomic Requests:
    plain: L2 atomic requests per normalization unit (GL2C_ATOMIC_sum).
    rst: L2 atomic requests per normalization unit (GL2C_ATOMIC_sum).
    unit: Requests per Normalization Unit
  GL2C Read Bandwidth:
    plain: Bytes per second for L2 read traffic (32/64/128 B and compressed 96 B read bins).
    rst: Bytes per second for L2 read traffic (32/64/128 B and compressed 96 B read bins).
    unit: Bytes/s
  GL2C Write Bandwidth:
    plain: Bytes per second for L2 write traffic (64 B per write request in this panel).
    rst: Bytes per second for L2 write traffic (64 B per write request in this panel).
    unit: Bytes/s
Memory chart - GCEA to System Memory:
  SARB Utilization:
    plain: 'Definition: Percentage of cycles the System Arbiter (SARB) in GCEA (Graphics Core Efficiency Arbiter) was busy. Description: SARB handles arbitration between the GPU and the system memory interface. Formula: 100 * GCEA_SARB_BUSY_sum / GCEA_ALWAYS_COUNT_sum'
    rst: 'Percentage of cycles the System Arbiter (SARB) in GCEA (Graphics Core Efficiency Arbiter) was busy. SARB handles arbitration between the GPU and the system memory interface. Formula: 100 * GCEA_SARB_BUSY_sum / GCEA_ALWAYS_COUNT_sum'
    unit: Percent
  DRAM Read Bandwidth:
    plain: 'Definition: Bandwidth for read requests to system memory (DRAM). Description: RDNA3.5 is an APU using DDR5/LPDDR5 system memory instead of HBM. Formula: GCEA_RDRAM_SIZE_REQ_sum * 32 / time'
    rst: 'Bandwidth for read requests to system memory (DRAM). RDNA3.5 is an APU using DDR5/LPDDR5X system memory instead of HBM. Formula: GCEA_RDRAM_SIZE_REQ_sum * 32 / time'
    unit: Bytes/s
  DRAM Write Bandwidth:
    plain: 'Definition: Bandwidth for write requests to system memory (DRAM). Description: RDNA3.5 is an APU using DDR5/LPDDR5 system memory instead of HBM. Formula: GCEA_WDRAM_SIZE_REQ_sum * 32 / time'
    rst: 'Definition: Bandwidth for write requests to system memory (DRAM). Description: RDNA3.5 is an APU using DDR5/LPDDR5 system memory instead of HBM. Formula: GCEA_WDRAM_SIZE_REQ_sum * 32 / time'
    unit: Bytes/s
  SARB Stall Rate:
    plain: 'Percent of GCEA sample cycles the System Arbiter (SARB) was stalled. Formula: 100 * GCEA_SARB_STALLED_sum / GCEA_ALWAYS_COUNT_sum.'
    rst: 'Percent of GCEA sample cycles the System Arbiter (SARB) was stalled. Formula: 100 * GCEA_SARB_STALLED_sum / GCEA_ALWAYS_COUNT_sum.'
    unit: Percent
  DRAM Read Requests:
    plain: DRAM read requests per client group from the GCEA memory path (GCEA_RDRAM_REQ_PER_CLIGRP_sum / $denom).
    rst: DRAM read requests per client group from the GCEA memory path (GCEA_RDRAM_REQ_PER_CLIGRP_sum / $denom).
    unit: Requests per Normalization Unit
  DRAM Write Requests:
    plain: DRAM write requests per client group from the GCEA memory path (GCEA_WDRAM_REQ_PER_CLIGRP_sum / $denom).
    rst: DRAM write requests per client group from the GCEA memory path (GCEA_WDRAM_REQ_PER_CLIGRP_sum / $denom).
    unit: Requests per Normalization Unit
  Read Returns:
    plain: Valid read-return events on the GCEA return path per normalization unit (GCEA_RRET_VLD_sum / $denom).
    rst: Valid read-return events on the GCEA return path per normalization unit (GCEA_RRET_VLD_sum / $denom).
    unit: Count per Normalization Unit
  Write Returns:
    plain: Valid write-return events on the GCEA return path per normalization unit (GCEA_WRET_VLD_sum / $denom).
    rst: Valid write-return events on the GCEA return path per normalization unit (GCEA_WRET_VLD_sum / $denom).
    unit: Count per Normalization Unit
Roofline Performance Rates:
  VALU FLOPs (F16):
    plain: The total 16-bit floating-point operations executed per second on the VALU. Theoretical FP16 throughput is 2× the FP32 FMA peak (packed half on Navi3 / RDNA3-class VALU). This is presented with the value of the peak empirical F16 FLOPs achievable on the specific accelerator.
    rst: The total 16-bit floating-point operations executed per second on the VALU. Theoretical FP16 throughput is 2× the FP32 FMA peak (packed half on Navi3 / RDNA3-class VALU). This is presented with the value of the peak empirical F16 FLOPs achievable on the specific accelerator.
    unit: GFLOP/s
  VALU FLOPs (F32):
    plain: The total 32-bit floating-point operations executed per second on the VALU. Navi3 / RDNA3-class CUs sustain up to 128 V_FMA_F32 ops per cycle per CU in wave64 mode; the roofline peak column uses empirically measured F32 throughput. This is presented with the value of the peak empirical F32 FLOPs achievable on the specific accelerator.
    rst: The total 32-bit floating-point operations executed per second on the VALU. Navi3 / RDNA3-class CUs sustain up to 128 V_FMA_F32 ops per cycle per CU in wave64 mode; the roofline peak column uses empirically measured F32 throughput. This is presented with the value of the peak empirical F32 FLOPs achievable on the specific accelerator.
    unit: GFLOP/s
  VALU FLOPs (F64):
    plain: The total 64-bit floating-point operations executed per second on the VALU. FP64 throughput is typically 1/16 or 1/32 of FP32 on RDNA3.5 consumer parts. This is presented with the value of the peak empirical F64 FLOPs achievable on the specific accelerator.
    rst: The total 64-bit floating-point operations executed per second on the VALU. FP64 throughput is typically 1/16 or 1/32 of FP32 on RDNA3.5 consumer parts. This is presented with the value of the peak empirical F64 FLOPs achievable on the specific accelerator.
    unit: GFLOP/s
  GL2 Cache Bandwidth:
    plain: The number of bytes transferred between GL1C and GL2C per unit time. GL2C is the last-level cache in RDNA3.5 before system memory. The peak empirically measured bandwidth achievable on the specific accelerator is displayed alongside for comparison.
    rst: The number of bytes transferred between GL1C and GL2C per unit time. GL2C is the last-level cache in RDNA3.5 before system memory. The peak empirically measured bandwidth achievable on the specific accelerator is displayed alongside for comparison.
    unit: Bytes/s
  GL1 Cache Bandwidth:
    plain: The number of bytes transferred between TCP (L0) and GL1C per unit time. GL1C is the shared L1 cache within each Shader Engine. The peak empirically measured bandwidth achievable on the specific accelerator is displayed alongside for comparison.
    rst: The number of bytes transferred between TCP (L0) and GL1C per unit time. GL1C is the shared L1 cache within each Shader Engine. The peak empirically measured bandwidth achievable on the specific accelerator is displayed alongside for comparison.
    unit: Bytes/s
  TCP (L0) Cache Bandwidth:
    plain: The number of bytes looked up in the TCP (Texture Cache Per-pipe) per unit time. TCP is the L0 vector cache in RDNA3.5, one per WGP. The peak empirically measured bandwidth achievable on the specific accelerator is displayed alongside for comparison.
    rst: The number of bytes looked up in the TCP (Texture Cache Per-pipe) per unit time. TCP is the L0 vector cache in RDNA3.5, one per WGP. The peak empirically measured bandwidth achievable on the specific accelerator is displayed alongside for comparison.
    unit: Bytes/s
  LDS Bandwidth:
    plain: Indicates the maximum amount of bytes that could have been loaded from, stored to, or atomically updated in the LDS per unit time. RDNA3.5 has 32 LDS banks per CU. The peak empirically measured LDS bandwidth achievable on the specific accelerator is displayed alongside for comparison.
    rst: Indicates the maximum amount of bytes that could have been loaded from, stored to, or atomically updated in the LDS per unit time. RDNA3.5 has 32 LDS banks per CU. The peak empirically measured LDS bandwidth achievable on the specific accelerator is displayed alongside for comparison.
    unit: Bytes/s
Roofline Plot Points:
  AI TCP:
    plain: The Arithmetic Intensity (AI) relative to TCP (L0 Cache). It is the ratio of total floating-point operations (FLOPs) to total bytes transferred from TCP to the shader cores. This value is used as the x-coordinate for the TCP roofline.
    rst: The Arithmetic Intensity (AI) relative to TCP (L0 Cache). It is the ratio of total floating-point operations (FLOPs) to total bytes transferred from TCP to the shader cores. This value is used as the x-coordinate for the TCP roofline.
    unit: FLOPs/Byte
  AI GL1:
    plain: The Arithmetic Intensity (AI) relative to GL1C (L1 Cache). It is the ratio of total floating-point operations (FLOPs) to total bytes transferred between GL1C and TCP. This value is used as the x-coordinate for the GL1 roofline.
    rst: The Arithmetic Intensity (AI) relative to GL1C (L1 Cache). It is the ratio of total floating-point operations (FLOPs) to total bytes transferred between GL1C and TCP. This value is used as the x-coordinate for the GL1 roofline.
    unit: FLOPs/Byte
  AI GL2:
    plain: The Arithmetic Intensity (AI) relative to GL2C (L2 Cache). It is the ratio of total floating-point operations (FLOPs) to total bytes transferred between GL2C and GL1C. This value is used as the x-coordinate for the GL2 roofline.
    rst: The Arithmetic Intensity (AI) relative to GL2C (L2 Cache). It is the ratio of total floating-point operations (FLOPs) to total bytes transferred between GL2C and GL1C. This value is used as the x-coordinate for the GL2 roofline.
    unit: FLOPs/Byte
  Performance (GFLOPs):
    plain: The overall achieved performance, measured in GigaFLOPs per second (GFLOP/s). This is calculated as the sum of all VALU floating-point operations divided by the total execution time. This value is used as the y-coordinate for the kernel's point on the Roofline plot.
    rst: The overall achieved performance, measured in GigaFLOPs per second (GFLOP/s). This is calculated as the sum of all VALU floating-point operations divided by the total execution time. This value is used as the y-coordinate for the kernel's point on the Roofline plot.
    unit: GFLOP/s
CPC Utilization:
  CPC Busy:
    plain: Percentage of CPC always-count cycles the Command Processor Compute was busy processing commands. Computed as 100 * CPC_STAT_BUSY / CPC_ALWAYS_COUNT.
    rst: Percentage of CPC always-count cycles the Command Processor Compute was busy processing commands. Computed as 100 * CPC_STAT_BUSY / CPC_ALWAYS_COUNT.
    unit: Percent
  CPC Idle:
    plain: Percentage of CPC always-count cycles the Command Processor Compute was idle. Computed as 100 * CPC_STAT_IDLE / CPC_ALWAYS_COUNT.
    rst: Percentage of CPC always-count cycles the Command Processor Compute was idle. Computed as 100 * CPC_STAT_IDLE / CPC_ALWAYS_COUNT.
    unit: Percent
  CPC Stalled:
    plain: Percentage of CPC always-count cycles the Command Processor Compute was stalled. Computed as 100 * CPC_STAT_STALL / CPC_ALWAYS_COUNT.
    rst: Percentage of CPC always-count cycles the Command Processor Compute was stalled. Computed as 100 * CPC_STAT_STALL / CPC_ALWAYS_COUNT.
    unit: Percent
CPC Interface Utilization:
  TCIU Busy:
    plain: Percentage of CPC always-count cycles the TC (Texture Cache) Interface Unit was busy. Computed as 100 * CPC_TCIU_BUSY / CPC_ALWAYS_COUNT.
    rst: Percentage of CPC always-count cycles the TC (Texture Cache) Interface Unit was busy. Computed as 100 * CPC_TCIU_BUSY / CPC_ALWAYS_COUNT.
    unit: Percent
  UTCL2 Busy:
    plain: Percentage of CPC always-count cycles the UTCL2 (Unified Translation Cache L2) interface was busy. Computed as 100 * CPC_UTCL2IU_BUSY / CPC_ALWAYS_COUNT.
    rst: Percentage of CPC always-count cycles the UTCL2 (Unified Translation Cache L2) interface was busy. Computed as 100 * CPC_UTCL2IU_BUSY / CPC_ALWAYS_COUNT.
    unit: Percent
  GCRIU Busy:
    plain: Percentage of CPC always-count cycles the GCR (Graphics Cache Rinse) Interface Unit was busy. Computed as 100 * CPC_GCRIU_BUSY / CPC_ALWAYS_COUNT.
    rst: Percentage of CPC always-count cycles the GCR (Graphics Cache Rinse) Interface Unit was busy. Computed as 100 * CPC_GCRIU_BUSY / CPC_ALWAYS_COUNT.
    unit: Percent
MEC Stall Cycles:
  ME1 Stall on RCIU Ready:
    plain: Cycles ME1 was stalled waiting for RCIU (Register Cache Interface Unit) to be ready (CPC_ME1_STALL_WAIT_ON_RCIU_READY).
    rst: Cycles ME1 was stalled waiting for RCIU (Register Cache Interface Unit) to be ready (CPC_ME1_STALL_WAIT_ON_RCIU_READY).
    unit: Cycles per Normalization Unit
  ME1 Stall on Memory Read:
    plain: Cycles ME1 was stalled waiting for memory read completion (CPC_ME1_STALL_WAIT_ON_MEM_READ).
    rst: Cycles ME1 was stalled waiting for memory read completion (CPC_ME1_STALL_WAIT_ON_MEM_READ).
    unit: Cycles per Normalization Unit
  ME1 Stall on Memory Write:
    plain: Cycles ME1 was stalled waiting for memory write completion (CPC_ME1_STALL_WAIT_ON_MEM_WRITE).
    rst: Cycles ME1 was stalled waiting for memory write completion (CPC_ME1_STALL_WAIT_ON_MEM_WRITE).
    unit: Cycles per Normalization Unit
  ME1 Stall on ROQ Data:
    plain: Cycles ME1 was stalled waiting for data from the Ring Output Queue (CPC_ME1_STALL_ON_DATA_FROM_ROQ).
    rst: Cycles ME1 was stalled waiting for data from the Ring Output Queue (CPC_ME1_STALL_ON_DATA_FROM_ROQ).
    unit: Cycles per Normalization Unit
CPC Memory Requests:
  TCIU Read Requests:
    plain: Number of read requests sent to the TC Interface Unit.
    rst: Number of read requests sent to the TC Interface Unit.
    unit: Count per Normalization Unit
  TCIU Write Requests:
    plain: Number of write requests sent to the TC Interface Unit.
    rst: Number of write requests sent to the TC Interface Unit.
    unit: Count per Normalization Unit
  GUS Read Requests:
    plain: Number of read requests sent to the Global Unified Shader memory interface.
    rst: Number of read requests sent to the Global Unified Shader memory interface.
    unit: Count per Normalization Unit
  GUS Write Requests:
    plain: Number of write requests sent to the Global Unified Shader memory interface.
    rst: Number of write requests sent to the Global Unified Shader memory interface.
    unit: Count per Normalization Unit
MEC Instruction Cache:
  Instruction Cache Hits:
    plain: Number of MEC instruction cache hits.
    rst: Number of MEC instruction cache hits.
    unit: Count per Normalization Unit
  Instruction Cache Misses:
    plain: Number of MEC instruction cache misses.
    rst: Number of MEC instruction cache misses.
    unit: Count per Normalization Unit
  Instruction Cache Hit Rate:
    plain: Percentage of MEC instruction cache accesses that hit in the cache.
    rst: Percentage of MEC instruction cache accesses that hit in the cache.
    unit: Percent
SPI Utilization:
  SPI Busy:
    plain: 'Percentage of GPU active (GUI) cycles where any SPI was busy. Computed as 100 * GRBM_SPI_BUSY / GRBM_GUI_ACTIVE (GRBM block). Do not use block-level SPI_BUSY here: it is aggregated over every SPI instance, so summed busy cycles can far exceed GUI-active cycles when multiple shader engines are active in parallel.'
    rst: 'Percentage of GPU active (GUI) cycles where any SPI was busy. Computed as 100 * GRBM_SPI_BUSY / GRBM_GUI_ACTIVE (GRBM block). Do not use block-level SPI_BUSY here: it is aggregated over every SPI instance, so summed busy cycles can far exceed GUI-active cycles when multiple shader engines are active in parallel.'
    unit: Percent
Wave Dispatch Statistics:
  Compute Wave Dispatches:
    plain: 'SPI counter SPI_CSN_WAVE (block SPI, event 50): compute (CSN) path wave count as stored in pmc_perf (column SPI_CSN_WAVE). Same idea as WGP “Dispatched Waves” (SQ_WAVES_sum) but at SPI; values may differ slightly. Normalized by CLI $denom: per_kernel → ÷1; per_wave → ÷SQ_WAVES; per_cycle → ÷$GRBM_GUI_ACTIVE_PER_XCD; per_second → ÷kernel wall time (s).'
    rst: 'SPI counter SPI_CSN_WAVE (block SPI, event 50): compute (CSN) path wave count as stored in pmc_perf (column SPI_CSN_WAVE). Same idea as WGP “Dispatched Waves” (SQ_WAVES_sum) but at SPI; values may differ slightly. Normalized by CLI $denom: per_kernel → ÷1; per_wave → ÷SQ_WAVES; per_cycle → ÷$GRBM_GUI_ACTIVE_PER_XCD; per_second → ÷kernel wall time (s).'
    unit: Count per Normalization Unit
  Wave Dispatch Rate:
    plain: SPI_CSN_WAVE / GRBM_GUI_ACTIVE — waves per GUI-active cycle (can exceed 1 when many SEs dispatch in parallel). Uses the same SPI_CSN_WAVE column as collection emits.
    rst: SPI_CSN_WAVE / GRBM_GUI_ACTIVE — waves per GUI-active cycle (can exceed 1 when many SEs dispatch in parallel). Uses the same SPI_CSN_WAVE column as collection emits.
    unit: Waves/cycle
WGP Utilization:
  WGP Utilization:
    plain: WGP busy as a percent of GRBM GUI-active cycles. RDNA3.5 WGPs pair 2 CUs, each with 2 SIMD32 units.
    rst: WGP busy as a percent of GRBM GUI-active cycles. RDNA3.5 WGPs pair 2 CUs, each with 2 SIMD32 units.
    unit: Percent
Wave Life:
  Wave Life:
    plain: Average number of cycles a wave exists from creation to completion. Calculated as SQ_WAVE_CYCLES / SQ_WAVES.
    rst: Average number of cycles a wave exists from creation to completion. Calculated as SQ_WAVE_CYCLES / SQ_WAVES.
    unit: Cycles
Wave Instruction Mix:
  Total Instructions:
    plain: Total number of instructions executed across all waves.
    rst: Total number of instructions executed across all waves.
    unit: Count per Normalization Unit
  Instructions - VALU:
    plain: Number of Vector ALU instructions executed (SQ_INSTS_VALU). VALU handles all vector arithmetic and logic operations. Transcendental VALU ops are reported separately as "Instructions - Transcendental" in the same Wave Instruction Mix table.
    rst: Number of Vector ALU instructions executed (SQ_INSTS_VALU). VALU handles all vector arithmetic and logic operations. Transcendental VALU ops are reported separately as "Instructions - Transcendental" in the same Wave Instruction Mix table.
    unit: Count per Normalization Unit
  Instructions - SALU:
    plain: Number of Scalar ALU instructions executed. SALU handles scalar operations that are uniform across the wave.
    rst: Number of Scalar ALU instructions executed. SALU handles scalar operations that are uniform across the wave.
    unit: Count per Normalization Unit
  Instructions - SMEM:
    plain: Number of Scalar Memory instructions executed. SMEM loads uniform data through the scalar cache.
    rst: Number of Scalar Memory instructions executed. SMEM loads uniform data through the scalar cache.
    unit: Count per Normalization Unit
  Instructions - VMEM:
    plain: Number of Vector Memory instructions executed. Includes global, buffer, and texture memory operations.
    rst: Number of Vector Memory instructions executed. Includes global, buffer, and texture memory operations.
    unit: Count per Normalization Unit
  Instructions - LDS:
    plain: Total Local Data Share instructions (SQ_INSTS_LDS). LDS provides fast shared memory within a workgroup. For LDS sub-categories (direct/param/ wave32 param loads), see the "LDS Instruction Mix" table.
    rst: Total Local Data Share instructions (SQ_INSTS_LDS). LDS provides fast shared memory within a workgroup. For LDS sub-categories (direct/param/ wave32 param loads), see the "LDS Instruction Mix" table.
    unit: Count per Normalization Unit
  Instructions - Internal:
    plain: Internal SQ instructions (SQ_INSTS_INTERNAL) per normalization unit — bookkeeping or non-user-visible work attributed to the scheduler.
    rst: Internal SQ instructions (SQ_INSTS_INTERNAL) per normalization unit — bookkeeping or non-user-visible work attributed to the scheduler.
    unit: Count per Normalization Unit
  Instructions - Transcendental:
    plain: Transcendental VALU instructions (SQ_INSTS_VALU_TRANS), per normalization unit — e.g. sin/cos/log approximations on the vector ALU.
    rst: Transcendental VALU instructions (SQ_INSTS_VALU_TRANS), per normalization unit — e.g. sin/cos/log approximations on the vector ALU.
    unit: Count per Normalization Unit
LDS Instruction Mix:
  LDS Direct Load Instructions:
    plain: LDS direct load instructions issued (SQ_INSTS_LDS_DIRECT_LOAD). Subset of total LDS; not required to sum with other LDS mix rows to equal total.
    rst: LDS direct load instructions issued (SQ_INSTS_LDS_DIRECT_LOAD). Subset of total LDS; not required to sum with other LDS mix rows to equal total.
    unit: Count per Normalization Unit
  LDS Parameter Load Instructions:
    plain: LDS parameter load instructions issued (SQ_INSTS_LDS_PARAM_LOAD).
    rst: LDS parameter load instructions issued (SQ_INSTS_LDS_PARAM_LOAD).
    unit: Count per Normalization Unit
  WAVE32 LDS Parameter Load Instructions:
    plain: LDS parameter load instructions under wave32 execution (SQ_INSTS_WAVE32_LDS_PARAM_LOAD).
    rst: LDS parameter load instructions under wave32 execution (SQ_INSTS_WAVE32_LDS_PARAM_LOAD).
    unit: Count per Normalization Unit
Wait State Analysis:
  Wait for Any:
    plain: Percentage of wave lifetime spent waiting for any reason including memory, barriers, or instruction fetch. Computed as 100 * SQ_WAIT_ANY / SQ_WAVE_CYCLES.
    rst: Percentage of wave lifetime spent waiting for any reason including memory, barriers, or instruction fetch. Computed as 100 * SQ_WAIT_ANY / SQ_WAVE_CYCLES.
    unit: Percent
  Wait for Instruction Fetch:
    plain: Percentage of wave lifetime spent waiting for instructions to arrive from the instruction cache. Computed as 100 * SQ_WAIT_IFETCH / SQ_WAVE_CYCLES.
    rst: Percentage of wave lifetime spent waiting for instructions to arrive from the instruction cache. Computed as 100 * SQ_WAIT_IFETCH / SQ_WAVE_CYCLES.
    unit: Percent
  Wait for Barrier:
    plain: Percentage of wave lifetime spent waiting at barrier synchronization points. Computed as 100 * SQ_WAIT_BARRIER / SQ_WAVE_CYCLES.
    rst: Percentage of wave lifetime spent waiting at barrier synchronization points. Computed as 100 * SQ_WAIT_BARRIER / SQ_WAVE_CYCLES.
    unit: Percent
  Wait for Counter:
    plain: Percentage of wave lifetime spent waiting for waitcnt counters (VMCNT, LGKMCNT, etc.) to reach target values. Computed as 100 * SQ_WAIT_CNT_ANY / SQ_WAVE_CYCLES.
    rst: Percentage of wave lifetime spent waiting for waitcnt counters (VMCNT, LGKMCNT, etc.) to reach target values. Computed as 100 * SQ_WAIT_CNT_ANY / SQ_WAVE_CYCLES.
    unit: Percent
WGP Instruction Cache:
  Icache Hit Rate:
    plain: Instruction cache hit percentage using 100 × SQC_ICACHE_HITS / (SQC_ICACHE_HITS + SQC_ICACHE_MISSES). Do not divide only by SQC_ICACHE_REQ — hits and requests are both per-SQ per-bank, and summed hits can exceed summed requests.
    rst: Instruction cache hit percentage using 100 × SQC_ICACHE_HITS / (SQC_ICACHE_HITS + SQC_ICACHE_MISSES). Do not divide only by SQC_ICACHE_REQ — hits and requests are both per-SQ per-bank, and summed hits can exceed summed requests.
    unit: Percent
WGP Scalar Data Cache:
  Dcache Hit Rate:
    plain: 'Scalar data cache hit percentage: 100 × SQC_DCACHE_HITS / (SQC_DCACHE_HITS + SQC_DCACHE_MISSES). Same rationale as Icache (HITS vs REQ can exceed 100%).'
    rst: 'Scalar data cache hit percentage: 100 × SQC_DCACHE_HITS / (SQC_DCACHE_HITS + SQC_DCACHE_MISSES). Same rationale as Icache (HITS vs REQ can exceed 100%).'
    unit: Percent
TCP Utilization:
  TCP Busy:
    plain: 'Estimated TCP utilization: 100 × TCP_GATE_EN2 / (GRBM_GUI_ACTIVE × NUM_TCP), with NUM_TCP = max(1, ⌊$cu_per_gpu / 2⌋) (one TCP per WGP, 2 CUs per WGP on RDNA3.5). Uses aggregate TCP_GATE_EN2 (not TCP_GATE_EN2_sum). GRBM_GUI_ACTIVE is scaled by NUM_TCP so the denominator matches multi-TCP capacity. GATE_EN2 is tcp_perf_sel_gate_en2 (core clocks on, not windowed). If only per-instance *_sum is available, align the numerator/denominator with your CSV layout.'
    rst: 'Estimated TCP utilization: 100 × TCP_GATE_EN2 / (GRBM_GUI_ACTIVE × NUM_TCP), with NUM_TCP = max(1, ⌊$cu_per_gpu / 2⌋) (one TCP per WGP, 2 CUs per WGP on RDNA3.5). Uses aggregate TCP_GATE_EN2 (not TCP_GATE_EN2_sum). GRBM_GUI_ACTIVE is scaled by NUM_TCP so the denominator matches multi-TCP capacity. GATE_EN2 is TCP_PERF_SEL_GATE_EN2 (core clocks on, not windowed). If only per-instance counter sums (typically ``_sum`` columns) are available, align the numerator/denominator with your CSV layout.'
    unit: Percent
TCP Request Statistics:
  Total Requests:
    plain: Total number of requests to the TCP cache including reads, writes, and atomics.
    rst: Total number of requests to the TCP cache including reads, writes, and atomics.
    unit: Count per Normalization Unit
  Read Requests:
    plain: Number of read requests to the TCP cache.
    rst: Number of read requests to the TCP cache.
    unit: Count per Normalization Unit
  Write Requests:
    plain: Number of write requests to the TCP cache.
    rst: Number of write requests to the TCP cache.
    unit: Count per Normalization Unit
  Miss Requests:
    plain: Number of requests that missed in the TCP cache and required fetching from GL1C.
    rst: Number of requests that missed in the TCP cache and required fetching from GL1C.
    unit: Count per Normalization Unit
TCP Cache Performance:
  Hit Rate:
    plain: Percentage of TCP requests that hit in the cache. Higher hit rates reduce traffic to GL1C and improve performance.
    rst: Percentage of TCP requests that hit in the cache. Higher hit rates reduce traffic to GL1C and improve performance.
    unit: Percent
TCP TCP-GL1 Interface:
  GL1 Read Requests:
    plain: Number of read requests sent from TCP to GL1C due to cache misses.
    rst: Number of read requests sent from TCP to GL1C due to cache misses.
    unit: Count per Normalization Unit
  GL1 Read 128B Requests:
    plain: TCP→GL1 read requests that transferred 128 bytes (TCP_GL1_REQ_READ_128B_sum / $denom). Subset of total GL1 read requests; large-line fills show up here.
    rst: TCP→GL1 read requests that transferred 128 bytes (TCP_GL1_REQ_READ_128B_sum / $denom). Subset of total GL1 read requests; large-line fills show up here.
    unit: Count per Normalization Unit
  GL1 Write Requests:
    plain: TCP→GL1 write or writeback requests (TCP_GL1_REQ_WRITE_sum / $denom).
    rst: TCP→GL1 write or writeback requests (TCP_GL1_REQ_WRITE_sum / $denom).
    unit: Count per Normalization Unit
TCP Stalls:
  TA Req Stall:
    plain: Cycles TCP stalled the Texture Addresser request interface due to backpressure.
    rst: Cycles TCP stalled the Texture Addresser request interface due to backpressure.
    unit: Cycles per Normalization Unit
  GL1 Back Pressure:
    plain: Cycles TCP was stalled due to back pressure from GL1C.
    rst: Cycles TCP was stalled due to back pressure from GL1C.
    unit: Cycles per Normalization Unit
  Data FIFO Stall:
    plain: Cycles TCP stalled because the internal data FIFO could not accept progress (TCP_DATA_FIFO_STALL_sum / $denom). Often correlates with downstream backpressure.
    rst: Cycles TCP stalled because the internal data FIFO could not accept progress (TCP_DATA_FIFO_STALL_sum / $denom). Often correlates with downstream backpressure.
    unit: Cycles per Normalization Unit
GL1C Utilization:
  GL1C Busy:
    plain: 'Percentage of cycles where the GL1C cache was busy processing requests. GL1C is the shared L1 cache within each Shader Engine. Per sample: 100 * GL1C_BUSY / GL1C_CYCLE (when GL1C_CYCLE != 0).'
    rst: 'Percentage of cycles where the GL1C cache was busy processing requests. GL1C is the shared L1 cache within each Shader Engine. Per sample: 100 * GL1C_BUSY / GL1C_CYCLE (when GL1C_CYCLE != 0).'
    unit: Percent
  GL1C Starve:
    plain: 'Percentage of cycles where GL1C had no pending requests from TCP. Per sample: 100 * GL1C_STARVE / GL1C_CYCLE (when GL1C_CYCLE != 0).'
    rst: 'Percentage of cycles where GL1C had no pending requests from TCP. Per sample: 100 * GL1C_STARVE / GL1C_CYCLE (when GL1C_CYCLE != 0).'
    unit: Percent
GL1C Request Statistics:
  Total Requests:
    plain: Total number of requests to the GL1C cache from TCP.
    rst: Total number of requests to the GL1C cache from TCP.
    unit: Count per Normalization Unit
  Read Requests:
    plain: Number of read requests to the GL1C cache.
    rst: Number of read requests to the GL1C cache.
    unit: Count per Normalization Unit
  Write Requests:
    plain: Number of write requests to the GL1C cache.
    rst: Number of write requests to the GL1C cache.
    unit: Count per Normalization Unit
  Miss Requests:
    plain: Number of requests that missed in the GL1C cache.
    rst: Number of requests that missed in the GL1C cache.
    unit: Count per Normalization Unit
GL1C Cache Performance:
  Hit Rate:
    plain: Percentage of GL1C requests that hit in the cache.
    rst: Percentage of GL1C requests that hit in the cache.
    unit: Percent
GL1C-GL2 Interface:
  GL2 Read Requests:
    plain: Number of read requests sent from GL1C to GL2C due to cache misses.
    rst: Number of read requests sent from GL1C to GL2C due to cache misses.
    unit: Count per Normalization Unit
  GL2 Read 128B Requests:
    plain: GL1C→GL2 read requests using 128-byte lines (GL1C_GL2_REQ_READ_128B_sum / $denom).
    rst: GL1C→GL2 read requests using 128-byte lines (GL1C_GL2_REQ_READ_128B_sum / $denom).
    unit: Count per Normalization Unit
  GL2 Write Requests:
    plain: GL1C→GL2 write requests (GL1C_GL2_REQ_WRITE_sum / $denom).
    rst: GL1C→GL2 write requests (GL1C_GL2_REQ_WRITE_sum / $denom).
    unit: Count per Normalization Unit
GL1C Stalls:
  GL2 Stall:
    plain: Cycles GL1C was stalled due to backpressure from GL2C.
    rst: Cycles GL1C was stalled due to backpressure from GL2C.
    unit: Cycles per Normalization Unit
  LFIFO Full Stall:
    plain: Cycles GL1C stalled because the return/load FIFO was full (GL1C_STALL_LFIFO_FULL_sum / $denom), often indicating downstream or consumer backpressure.
    rst: Cycles GL1C stalled because the return/load FIFO was full (GL1C_STALL_LFIFO_FULL_sum / $denom), often indicating downstream or consumer backpressure.
    unit: Cycles per Normalization Unit
GL2C Cache Performance:
  GL2C Hit Rate:
    plain: Percentage of GL2C requests that hit in the cache. GL2C is the last-level cache before system memory.
    rst: Percentage of GL2C requests that hit in the cache. GL2C is the last-level cache before system memory.
    unit: Percent
  GL2C Hits:
    plain: Number of requests that hit in the GL2C cache.
    rst: Number of requests that hit in the GL2C cache.
    unit: Count per Normalization Unit
  GL2C Misses:
    plain: Number of requests that missed in the GL2C cache and went to memory.
    rst: Number of requests that missed in the GL2C cache and went to memory.
    unit: Count per Normalization Unit
GL2C Request Statistics:
  Total Requests:
    plain: Number of requests of all types to the GL2C cache (counter GL2C_REQ). This is measured at the tag block (request path into GL2C tag / lookup). Not guaranteed to equal Read + Write + Atomic; those use separate perf events and may omit or overlap traffic relative to this aggregate.
    rst: Number of requests of all types to the GL2C cache (counter GL2C_REQ). This is measured at the tag block (request path into GL2C tag / lookup). Not guaranteed to equal Read + Write + Atomic; those use separate perf events and may omit or overlap traffic relative to this aggregate.
    unit: Count per Normalization Unit
  Read Requests:
    plain: Number of read requests to the GL2C cache (GL2C_READ).
    rst: Number of read requests to the GL2C cache (GL2C_READ).
    unit: Count per Normalization Unit
  Write Requests:
    plain: Number of write requests to the GL2C cache (GL2C_WRITE).
    rst: Number of write requests to the GL2C cache (GL2C_WRITE).
    unit: Count per Normalization Unit
  Atomic Requests:
    plain: Number of atomic requests at the GL2C cache (GL2C_ATOMIC).
    rst: Number of atomic requests at the GL2C cache (GL2C_ATOMIC).
    unit: Count per Normalization Unit
  EA Read Requests (DRAM):
    plain: Number of GL2C/EA read requests destined for DRAM (memory controller). gl2c_perf_sel_ea_rdreq_dram (GL2C_EA_RDREQ_DRAM). Reported values are summed over GL2C instances.
    rst: Number of GL2C/EA read requests destined for DRAM (memory controller). gl2c_perf_sel_ea_rdreq_dram (GL2C_EA_RDREQ_DRAM). Reported values are summed over GL2C instances.
    unit: Count per Normalization Unit
  EA Write Requests (DRAM):
    plain: Number of GL2C/EA write requests destined for DRAM (memory controller). gl2c_perf_sel_ea_wrreq_dram (GL2C_EA_WRREQ_DRAM). Reported values are summed over GL2C instances.
    rst: Number of GL2C/EA write requests destined for DRAM (memory controller). gl2c_perf_sel_ea_wrreq_dram (GL2C_EA_WRREQ_DRAM). Reported values are summed over GL2C instances.
    unit: Count per Normalization Unit
GL2C Bandwidth:
  GL2C Read BW:
    plain: Read bandwidth from GL2C_READ_{32,64,128}_REQ byte accounting plus GL2C_COMPRESSED_READ_96_REQ (96 B compressed reads). EA 96 B reads use GL2C_EA_RDREQ_96B.
    rst: Read bandwidth from GL2C_READ_{32,64,128}_REQ byte accounting plus GL2C_COMPRESSED_READ_96_REQ (96 B compressed reads). EA 96 B reads use GL2C_EA_RDREQ_96B.
    unit: Bytes/s
  GL2C Write BW:
    plain: Achieved write bandwidth at the GL2C level.
    rst: Achieved write bandwidth at the GL2C level.
    unit: Bytes/s
DRAM Read Interface:
  DRAM Read Requests:
    plain: Number of read requests sent to system memory (DRAM) per client group. RDNA3.5 uses DDR5/LPDDR5 system memory instead of HBM.
    rst: Number of read requests sent to system memory (DRAM) per client group. RDNA3.5 uses DDR5/LPDDR5 system memory instead of HBM.
    unit: Count per Normalization Unit
  DRAM Read Chained Requests:
    plain: Number of chained (sequential) read request chains sent to DRAM. Chained requests improve memory efficiency for sequential access patterns.
    rst: Number of chained (sequential) read request chains sent to DRAM. Chained requests improve memory efficiency for sequential access patterns.
    unit: Count per Normalization Unit
  DRAM Read Banks Active:
    plain: Number of DRAM banks with pending read requests (saturates at 16). More active banks indicate better memory parallelism.
    rst: Number of DRAM banks with pending read requests (saturates at 16). More active banks indicate better memory parallelism.
    unit: Count per Normalization Unit
  DRAM Read Size (32B increments):
    plain: Total size of DRAM read requests in 32-byte increments.
    rst: Total size of DRAM read requests in 32-byte increments.
    unit: Count per Normalization Unit
DRAM Write Interface:
  DRAM Write Requests:
    plain: Number of write requests sent to system memory (DRAM) per client group.
    rst: Number of write requests sent to system memory (DRAM) per client group.
    unit: Count per Normalization Unit
  DRAM Write Chained Requests:
    plain: Number of chained (sequential) write request chains sent to DRAM.
    rst: Number of chained (sequential) write request chains sent to DRAM.
    unit: Count per Normalization Unit
  DRAM Write Banks Active:
    plain: Number of DRAM banks with pending write requests (saturates at 16).
    rst: Number of DRAM banks with pending write requests (saturates at 16).
    unit: Count per Normalization Unit
  DRAM Write Size (32B increments):
    plain: Total size of DRAM write requests in 32-byte increments.
    rst: Total size of DRAM write requests in 32-byte increments.
    unit: Count per Normalization Unit
System Arbiter (SARB):
  SARB Busy:
    plain: 'Percentage of cycles the System Arbiter was actively processing requests. Per sample: 100 * GCEA_SARB_BUSY / GCEA_ALWAYS_COUNT (when GCEA_ALWAYS_COUNT != 0). GCEA_ALWAYS_COUNT is the cycle-count denominator for these percentages.'
    rst: 'Percentage of cycles the System Arbiter was actively processing requests. Per sample: 100 * GCEA_SARB_BUSY / GCEA_ALWAYS_COUNT (when GCEA_ALWAYS_COUNT != 0). GCEA_ALWAYS_COUNT is the cycle-count denominator for these percentages.'
    unit: Percent
  SARB Stalled:
    plain: 'Percentage of cycles the System Arbiter was stalled (has valid request but downstream not ready). Per sample: 100 * GCEA_SARB_STALLED / GCEA_ALWAYS_COUNT (when GCEA_ALWAYS_COUNT != 0).'
    rst: 'Percentage of cycles the System Arbiter was stalled (has valid request but downstream not ready). Per sample: 100 * GCEA_SARB_STALLED / GCEA_ALWAYS_COUNT (when GCEA_ALWAYS_COUNT != 0).'
    unit: Percent
  SARB Starving:
    plain: 'Percentage of cycles the System Arbiter was starving (no valid requests but downstream ready). Per sample: 100 * GCEA_SARB_STARVING / GCEA_ALWAYS_COUNT (when GCEA_ALWAYS_COUNT != 0).'
    rst: 'Percentage of cycles the System Arbiter was starving (no valid requests but downstream ready). Per sample: 100 * GCEA_SARB_STARVING / GCEA_ALWAYS_COUNT (when GCEA_ALWAYS_COUNT != 0).'
    unit: Percent
  SARB Total Requests:
    plain: Total requests per virtual channel through the System Arbiter.
    rst: Total requests per virtual channel through the System Arbiter.
    unit: Count per Normalization Unit
  SARB DRAM Requests:
    plain: DRAM-bound requests per virtual channel through the System Arbiter.
    rst: DRAM-bound requests per virtual channel through the System Arbiter.
    unit: Count per Normalization Unit
Return Interface:
  Read Returns:
    plain: Number of read data returns from the external memory interface.
    rst: Number of read data returns from the external memory interface.
    unit: Count per Normalization Unit
  Write Returns:
    plain: Number of write acknowledgements from the external memory interface.
    rst: Number of write acknowledgements from the external memory interface.
    unit: Count per Normalization Unit
  Probe Requests:
    plain: Number of probe-invalidate requests (cache coherency operations).
    rst: Number of probe-invalidate requests (cache coherency operations).
    unit: Count per Normalization Unit
GPU Utilization:
  GPU Busy:
    plain: 'Percentage of time the GPU was actively processing work. Per sample: 100 * GRBM_GUI_ACTIVE / GRBM_COUNT (when GRBM_COUNT != 0). Avg / min / max are taken across samples.'
    rst: 'Percentage of time the GPU was actively processing work. Per sample: 100 * GRBM_GUI_ACTIVE / GRBM_COUNT (when GRBM_COUNT != 0). Avg / min / max are taken across samples.'
    unit: Percent
  CP Busy:
    plain: 'Percentage of GUI-active cycles the Command Processor was busy. Per sample: 100 * GRBM_CP_BUSY / GRBM_GUI_ACTIVE (when GRBM_GUI_ACTIVE != 0). CP handles kernel dispatch and command stream processing.'
    rst: 'Percentage of GUI-active cycles the Command Processor was busy. Per sample: 100 * GRBM_CP_BUSY / GRBM_GUI_ACTIVE (when GRBM_GUI_ACTIVE != 0). CP handles kernel dispatch and command stream processing.'
    unit: Percent
  GL2C Busy:
    plain: 'Percentage of GUI-active cycles the GL2 Cache was busy. Per sample: 100 * GRBM_GL2C_BUSY / GRBM_GUI_ACTIVE (when GRBM_GUI_ACTIVE != 0). High utilization indicates memory-intensive workload.'
    rst: 'Percentage of GUI-active cycles the GL2 Cache was busy. Per sample: 100 * GRBM_GL2C_BUSY / GRBM_GUI_ACTIVE (when GRBM_GUI_ACTIVE != 0). High utilization indicates memory-intensive workload.'
    unit: Percent
Shader Engine Utilization:
  TA Busy:
    plain: 'Percentage of GUI-active cycles the Texture Addresser was busy. Per sample: 100 * GRBM_TA_BUSY / GRBM_GUI_ACTIVE (when GRBM_GUI_ACTIVE != 0).'
    rst: 'Percentage of GUI-active cycles the Texture Addresser was busy. Per sample: 100 * GRBM_TA_BUSY / GRBM_GUI_ACTIVE (when GRBM_GUI_ACTIVE != 0).'
    unit: Percent
Memory chart - TCP-GL1 Interface:
  TCP-GL1 Read Requests:
    plain: Read requests from TCP to GL1C (miss/refill path), per normalization unit.
    rst: Read requests from TCP to GL1C (miss/refill path), per normalization unit.
    unit: Requests per Normalization Unit
  TCP-GL1 Write Requests:
    plain: Write-related requests from TCP toward GL1C, per normalization unit.
    rst: Write-related requests from TCP toward GL1C, per normalization unit.
    unit: Requests per Normalization Unit
  TCP-GL1 Read Bandwidth:
    plain: Bytes per second for TCP→GL1 read traffic (64 B per TCP_GL1_REQ_READ event).
    rst: Bytes per second for TCP→GL1 read traffic (64 B per TCP_GL1_REQ_READ event).
    unit: Bytes/s
  TCP-GL1 Write Bandwidth:
    plain: Bytes per second for TCP→GL1 write traffic (64 B per TCP_GL1_REQ_WRITE event).
    rst: Bytes per second for TCP→GL1 write traffic (64 B per TCP_GL1_REQ_WRITE event).
    unit: Bytes/s
Memory chart - GL1C-GL2 Interface:
  GL1-GL2 Read Requests:
    plain: Read requests from GL1C to GL2C per normalization unit (GL1C_GL2_REQ_READ_sum).
    rst: Read requests from GL1C to GL2C per normalization unit (GL1C_GL2_REQ_READ_sum).
    unit: Requests per Normalization Unit
  GL1-GL2 Write Requests:
    plain: Write requests from GL1C to GL2C per normalization unit (GL1C_GL2_REQ_WRITE_sum).
    rst: Write requests from GL1C to GL2C per normalization unit (GL1C_GL2_REQ_WRITE_sum).
    unit: Requests per Normalization Unit
  GL1-GL2 Read Bandwidth:
    plain: Bytes per second on the GL1C→GL2C read interface (32/64/128 B request bins).
    rst: Bytes per second on the GL1C→GL2C read interface (32/64/128 B request bins).
    unit: Bytes/s
  GL1-GL2 Write Bandwidth:
    plain: Bytes per second on the GL1C→GL2C write interface (32/64 B request bins).
    rst: Bytes per second on the GL1C→GL2C write interface (32/64 B request bins).
    unit: Bytes/s
  GL1-GL2 Read Latency:
    plain: Average cycles from GL1C read request to response (GL1C_GL2_REQ_READ_LATENCY_sum / GL1C_GL2_REQ_READ_sum) when the denominator is non-zero.
    rst: Average cycles from GL1C read request to response (GL1C_GL2_REQ_READ_LATENCY_sum / GL1C_GL2_REQ_READ_sum) when the denominator is non-zero.
    unit: Cycles
  GL1-GL2 Write Latency:
    plain: Average cycles from GL1C write request to completion (GL1C_GL2_REQ_WRITE_LATENCY_sum / GL1C_GL2_REQ_WRITE_sum) when the denominator is non-zero.
    rst: Average cycles from GL1C write request to completion (GL1C_GL2_REQ_WRITE_LATENCY_sum / GL1C_GL2_REQ_WRITE_sum) when the denominator is non-zero.
    unit: Cycles
Wavefront Launch Stats:
  Grid Size:
    plain: Number of work-items in the dispatch grid for the kernel (Grid_Size), averaged/min/maxed across samples. Defines total parallel work launched.
    rst: Number of work-items in the dispatch grid for the kernel (Grid_Size), averaged/min/maxed across samples. Defines total parallel work launched.
    unit: Work-items
  Workgroup Size:
    plain: Number of work-items per workgroup (Workgroup_Size). Together with grid size and resource limits it determines wave/wavefront count.
    rst: Number of work-items per workgroup (Workgroup_Size). Together with grid size and resource limits it determines wave/wavefront count.
    unit: Work-items
  VGPRs:
    plain: Vector general-purpose registers allocated per wave (Arch_VGPR). Limits occupancy when the per-CU VGPR capacity is exhausted.
    rst: Vector general-purpose registers allocated per wave (Arch_VGPR). Limits occupancy when the per-CU VGPR capacity is exhausted.
    unit: Registers
  SGPRs:
    plain: Scalar general-purpose registers allocated for the wave (SGPR). Uniform data and execution state live in the scalar path.
    rst: Scalar general-purpose registers allocated for the wave (SGPR). Uniform data and execution state live in the scalar path.
    unit: Registers
  LDS Allocation:
    plain: Local Data Share bytes reserved per workgroup (LDS_Per_Workgroup). High LDS per group reduces how many workgroups can run concurrently on a CU/WGP.
    rst: Local Data Share bytes reserved per workgroup (LDS_Per_Workgroup). High LDS per group reduces how many workgroups can run concurrently on a CU/WGP.
    unit: Bytes
  Scratch Allocation:
    plain: Scratch (private) memory bytes per work-item (Scratch_Per_Workitem). Backed by off-chip memory when spill/stack exceeds limits.
    rst: Scratch (private) memory bytes per work-item (Scratch_Per_Workitem). Backed by off-chip memory when spill/stack exceeds limits.
    unit: Bytes per Work-item
Wave Dispatch:
  Dispatched Waves:
    plain: Number of shader waves launched (SQ_WAVES_sum), averaged/min/maxed. Each wave is a SIMD execution entity (typically wave32 on this configuration).
    rst: Number of shader waves launched (SQ_WAVES_sum), averaged/min/maxed. Each wave is a SIMD execution entity (typically wave32 on this configuration).
    unit: Waves
  Dispatched Threads:
    plain: Number of work-items launched as attributed in SQ_ITEMS_sum (threads), averaged/min/maxed.
    rst: Number of work-items launched as attributed in SQ_ITEMS_sum (threads), averaged/min/maxed.
    unit: Threads
VMEM Instruction Mix:
  Instructions - VMEM Flat:
    plain: Flat-addressing vector memory instructions (SQ_INSTS_FLAT) per normalization unit. Flat can resolve to global, scratch or LDS depending on address.
    rst: Flat-addressing vector memory instructions (SQ_INSTS_FLAT) per normalization unit. Flat can resolve to global, scratch or LDS depending on address.
    unit: Count per Normalization Unit
  Instructions - TEX Load:
    plain: Texture / image load instructions (SQ_INSTS_TEX_LOAD) per normalization unit.
    rst: Texture / image load instructions (SQ_INSTS_TEX_LOAD) per normalization unit.
    unit: Count per Normalization Unit
  Instructions - TEX Store:
    plain: Texture / image store instructions (SQ_INSTS_TEX_STORE) per normalization unit.
    rst: Texture / image store instructions (SQ_INSTS_TEX_STORE) per normalization unit.
    unit: Count per Normalization Unit
  Instructions - Flat Load:
    plain: Flat load instructions (SQ_INSTS_FLAT_LOAD) per normalization unit.
    rst: Flat load instructions (SQ_INSTS_FLAT_LOAD) per normalization unit.
    unit: Count per Normalization Unit
  Instructions - Flat Store:
    plain: Flat store instructions (SQ_INSTS_FLAT_STORE) per normalization unit.
    rst: Flat store instructions (SQ_INSTS_FLAT_STORE) per normalization unit.
    unit: Count per Normalization Unit
