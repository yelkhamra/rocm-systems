# AUTOGENERATED FILE. Only edit for testing purposes, not for development. Generated from utils/unified_config.yaml. Generated by utils/split_config.py
Panel Config:
  id: 1600
  title: Vector L1 Data Cache
  data source:
  - metric_table:
      id: 1601
      title: vL1D Speed-of-Light
      header:
        metric: Metric
        value: Avg
        unit: Unit
      metric:
        Hit rate:
          value: 100 - 100 * SUM(TCP_TCC_READ_REQ_sum + TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum) / SUM(TCP_TOTAL_CACHE_ACCESSES_sum)
          unit: Percent
        Bandwidth Utilization:
          value: 100 * 128 * SUM(TCP_TOTAL_CACHE_ACCESSES_sum) / SUM(End_Timestamp - Start_Timestamp) / ($max_sclk / 1000 * 128 * $cu_per_gpu)
          unit: Percent
        Utilization:
          value: 100 * SUM(TCP_GATE_EN2_sum) / SUM(TCP_GATE_EN1_sum)
          unit: Percent
        Coalescing:
          value: 100 * 64 * SUM(TA_TOTAL_WAVEFRONTS_sum) / (4 * SUM(TCP_TOTAL_ACCESSES_sum))
          unit: Percent
      comparable: false
      cli_style: simple_bar
      tui_style: simple_bar
  - metric_table:
      id: 1602
      title: vL1D cache stall metrics
      header:
        metric: Metric
        expr: Expression
      metric:
        Stalled on L2 Data:
          expr: 100 * TCP_PENDING_STALL_CYCLES_sum / TCP_GATE_EN1_sum
        Stalled on L2 Req:
          expr: 100 * TCP_TCR_TCP_STALL_CYCLES_sum / TCP_GATE_EN1_sum
        Stalled on Address:
          expr: 100 * TCP_TCP_TA_ADDR_STALL_CYCLES_sum / TCP_GATE_EN1_sum
        Stalled on Data:
          expr: 100 * TCP_TCP_TA_DATA_STALL_CYCLES_sum / TCP_GATE_EN1_sum
        Stalled on Latency FIFO:
          expr: 100 * TCP_LFIFO_STALL_CYCLES_sum / TCP_GATE_EN1_sum
        Stalled on Request FIFO:
          expr: 100 * TCP_RFIFO_STALL_CYCLES_sum / TCP_GATE_EN1_sum
        Stalled on Read Return:
          expr: 100 * TCP_TCR_RDRET_STALL_sum / TCP_GATE_EN1_sum
        Tag RAM Stall (Read):
          expr: 100 * TCP_READ_TAGCONFLICT_STALL_CYCLES_sum / TCP_GATE_EN1_sum
        Tag RAM Stall (Write):
          expr: 100 * TCP_WRITE_TAGCONFLICT_STALL_CYCLES_sum / TCP_GATE_EN1_sum
        Tag RAM Stall (Atomic):
          expr: 100 * TCP_ATOMIC_TAGCONFLICT_STALL_CYCLES_sum / TCP_GATE_EN1_sum
      cli_style: simple_box
      tui_style: simple_box
  - metric_table:
      id: 1603
      title: vL1D cache access metrics
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        unit: Unit
      metric:
        Total Req:
          avg: SUM(TCP_TOTAL_ACCESSES_sum) / SUM($denom)
          min: MIN(TCP_TOTAL_ACCESSES_sum / $denom)
          max: MAX(TCP_TOTAL_ACCESSES_sum / $denom)
          unit: (Requests + $normUnit)
        Read Req:
          avg: SUM(TCP_TOTAL_READ_sum) / SUM($denom)
          min: MIN(TCP_TOTAL_READ_sum / $denom)
          max: MAX(TCP_TOTAL_READ_sum / $denom)
          unit: (Requests + $normUnit)
        Write Req:
          avg: SUM(TCP_TOTAL_WRITE_sum) / SUM($denom)
          min: MIN(TCP_TOTAL_WRITE_sum / $denom)
          max: MAX(TCP_TOTAL_WRITE_sum / $denom)
          unit: (Requests + $normUnit)
        Atomic Req:
          avg: SUM(TCP_TOTAL_ATOMIC_WITH_RET_sum + TCP_TOTAL_ATOMIC_WITHOUT_RET_sum) / SUM($denom)
          min: MIN((TCP_TOTAL_ATOMIC_WITH_RET_sum + TCP_TOTAL_ATOMIC_WITHOUT_RET_sum) / $denom)
          max: MAX((TCP_TOTAL_ATOMIC_WITH_RET_sum + TCP_TOTAL_ATOMIC_WITHOUT_RET_sum) / $denom)
          unit: (Requests + $normUnit)
        Cache BW:
          avg: 128 * SUM(TCP_TOTAL_CACHE_ACCESSES_sum) / SUM(End_Timestamp - Start_Timestamp)
          min: 128 * MIN(TCP_TOTAL_CACHE_ACCESSES_sum / (End_Timestamp - Start_Timestamp))
          max: 128 * MAX(TCP_TOTAL_CACHE_ACCESSES_sum / (End_Timestamp - Start_Timestamp))
          unit: Gbps
        Cache Hit Rate:
          avg: 100 - 100 * SUM(TCP_TCC_READ_REQ_sum + TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum) / SUM(TCP_TOTAL_CACHE_ACCESSES_sum)
          min: MIN(100 - 100 * (TCP_TCC_READ_REQ_sum + TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum) / TCP_TOTAL_CACHE_ACCESSES_sum)
          max: MAX(100 - 100 * (TCP_TCC_READ_REQ_sum + TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum) / TCP_TOTAL_CACHE_ACCESSES_sum)
          unit: Percent
        Cache Accesses:
          avg: SUM(TCP_TOTAL_CACHE_ACCESSES_sum) / SUM($denom)
          min: MIN(TCP_TOTAL_CACHE_ACCESSES_sum / $denom)
          max: MAX(TCP_TOTAL_CACHE_ACCESSES_sum / $denom)
          unit: (Requests + $normUnit)
        Cache Hits:
          avg: SUM(TCP_TOTAL_CACHE_ACCESSES_sum - (TCP_TCC_READ_REQ_sum + TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum)) / SUM($denom)
          min: MIN((TCP_TOTAL_CACHE_ACCESSES_sum - (TCP_TCC_READ_REQ_sum + TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum)) / $denom)
          max: MAX((TCP_TOTAL_CACHE_ACCESSES_sum - (TCP_TCC_READ_REQ_sum + TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum)) / $denom)
          unit: (Requests + $normUnit)
        Invalidations:
          avg: SUM(TCP_TOTAL_WRITEBACK_INVALIDATES_sum) / SUM($denom)
          min: MIN(TCP_TOTAL_WRITEBACK_INVALIDATES_sum / $denom)
          max: MAX(TCP_TOTAL_WRITEBACK_INVALIDATES_sum / $denom)
          unit: (Requests + $normUnit)
        L1-L2 BW:
          avg: SUM(128 * TCP_TCC_READ_REQ_sum + 64 * (TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum)) / SUM(End_Timestamp - Start_Timestamp)
          min: MIN((128 * TCP_TCC_READ_REQ_sum + 64 * (TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum)) / (End_Timestamp - Start_Timestamp))
          max: MAX((128 * TCP_TCC_READ_REQ_sum + 64 * (TCP_TCC_WRITE_REQ_sum + TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum)) / (End_Timestamp - Start_Timestamp))
          unit: Gbps
        Tag RAM 0 Req:
          avg: SUM(TCP_TAGRAM0_REQ_sum) / SUM($denom)
          min: MIN(TCP_TAGRAM0_REQ_sum / $denom)
          max: MAX(TCP_TAGRAM0_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        Tag RAM 1 Req:
          avg: SUM(TCP_TAGRAM1_REQ_sum) / SUM($denom)
          min: MIN(TCP_TAGRAM1_REQ_sum / $denom)
          max: MAX(TCP_TAGRAM1_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        Tag RAM 2 Req:
          avg: SUM(TCP_TAGRAM2_REQ_sum) / SUM($denom)
          min: MIN(TCP_TAGRAM2_REQ_sum / $denom)
          max: MAX(TCP_TAGRAM2_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        Tag RAM 3 Req:
          avg: SUM(TCP_TAGRAM3_REQ_sum) / SUM($denom)
          min: MIN(TCP_TAGRAM3_REQ_sum / $denom)
          max: MAX(TCP_TAGRAM3_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        L1-L2 Read:
          avg: SUM(TCP_TCC_READ_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_READ_REQ_sum / $denom)
          max: MAX(TCP_TCC_READ_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        L1-L2 Write:
          avg: SUM(TCP_TCC_WRITE_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_WRITE_REQ_sum / $denom)
          max: MAX(TCP_TCC_WRITE_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        L1-L2 Atomic:
          avg: SUM(TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum) / SUM($denom)
          min: MIN((TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum) / $denom)
          max: MAX((TCP_TCC_ATOMIC_WITH_RET_REQ_sum + TCP_TCC_ATOMIC_WITHOUT_RET_REQ_sum) / $denom)
          unit: (Requests + $normUnit)
        L1 Access Latency:
          avg: SUM(TCP_TCP_LATENCY_sum) / SUM($denom)
          min: MIN(TCP_TCP_LATENCY_sum / $denom)
          max: MAX(TCP_TCP_LATENCY_sum / $denom)
          unit: (Cycles + $normUnit)
        L1-L2 Read Latency:
          avg: SUM(TCP_TCC_READ_REQ_LATENCY_sum) / SUM($denom)
          min: MIN(TCP_TCC_READ_REQ_LATENCY_sum / $denom)
          max: MAX(TCP_TCC_READ_REQ_LATENCY_sum / $denom)
          unit: (Cycles + $normUnit)
        L1-L2 Write Latency:
          avg: SUM(TCP_TCC_WRITE_REQ_LATENCY_sum) / SUM($denom)
          min: MIN(TCP_TCC_WRITE_REQ_LATENCY_sum / $denom)
          max: MAX(TCP_TCC_WRITE_REQ_LATENCY_sum / $denom)
          unit: (Cycles + $normUnit)
  - metric_table:
      id: 1604
      title: L1D - L2 Transactions
      header:
        metric: Metric
        xfer: Xfer
        coherency: Coherency
        avg: Avg
        min: Min
        max: Max
        unit: Unit
      metric:
        NC - Read:
          xfer: Read
          coherency: NC
          avg: SUM(TCP_TCC_NC_READ_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_NC_READ_REQ_sum / $denom)
          max: MAX(TCP_TCC_NC_READ_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        UC - Read:
          xfer: Read
          coherency: UC
          avg: SUM(TCP_TCC_UC_READ_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_UC_READ_REQ_sum / $denom)
          max: MAX(TCP_TCC_UC_READ_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        CC - Read:
          xfer: Read
          coherency: CC
          avg: SUM(TCP_TCC_CC_READ_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_CC_READ_REQ_sum / $denom)
          max: MAX(TCP_TCC_CC_READ_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        RW - Read:
          xfer: Read
          coherency: RW
          avg: SUM(TCP_TCC_RW_READ_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_RW_READ_REQ_sum / $denom)
          max: MAX(TCP_TCC_RW_READ_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        RW - Write:
          xfer: Write
          coherency: RW
          avg: SUM(TCP_TCC_RW_WRITE_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_RW_WRITE_REQ_sum / $denom)
          max: MAX(TCP_TCC_RW_WRITE_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        NC - Write:
          xfer: Write
          coherency: NC
          avg: SUM(TCP_TCC_NC_WRITE_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_NC_WRITE_REQ_sum / $denom)
          max: MAX(TCP_TCC_NC_WRITE_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        UC - Write:
          xfer: Write
          coherency: UC
          avg: SUM(TCP_TCC_UC_WRITE_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_UC_WRITE_REQ_sum / $denom)
          max: MAX(TCP_TCC_UC_WRITE_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        CC - Write:
          xfer: Write
          coherency: CC
          avg: SUM(TCP_TCC_CC_WRITE_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_CC_WRITE_REQ_sum / $denom)
          max: MAX(TCP_TCC_CC_WRITE_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        NC - Atomic:
          xfer: Atomic
          coherency: NC
          avg: SUM(TCP_TCC_NC_ATOMIC_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_NC_ATOMIC_REQ_sum / $denom)
          max: MAX(TCP_TCC_NC_ATOMIC_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        UC - Atomic:
          xfer: Atomic
          coherency: UC
          avg: SUM(TCP_TCC_UC_ATOMIC_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_UC_ATOMIC_REQ_sum / $denom)
          max: MAX(TCP_TCC_UC_ATOMIC_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        CC - Atomic:
          xfer: Atomic
          coherency: CC
          avg: SUM(TCP_TCC_CC_ATOMIC_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_CC_ATOMIC_REQ_sum / $denom)
          max: MAX(TCP_TCC_CC_ATOMIC_REQ_sum / $denom)
          unit: (Requests + $normUnit)
        RW - Atomic:
          xfer: Atomic
          coherency: RW
          avg: SUM(TCP_TCC_RW_ATOMIC_REQ_sum) / SUM($denom)
          min: MIN(TCP_TCC_RW_ATOMIC_REQ_sum / $denom)
          max: MAX(TCP_TCC_RW_ATOMIC_REQ_sum / $denom)
          unit: (Requests + $normUnit)
  - metric_table:
      id: 1605
      title: L1 Unified Translation Cache (UTCL1)
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        units: Unit
      metric:
        Req:
          avg: SUM(TCP_UTCL1_REQUEST_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_REQUEST_sum / $denom)
          max: MAX(TCP_UTCL1_REQUEST_sum / $denom)
          units: (Requests + $normUnit)
        Inflight Req:
          avg: SUM(TCP_CLIENT_UTCL1_INFLIGHT_sum) / SUM($denom)
          min: MIN(TCP_CLIENT_UTCL1_INFLIGHT_sum / $denom)
          max: MAX(TCP_CLIENT_UTCL1_INFLIGHT_sum / $denom)
          units: (Requests + $normUnit)
        Hit Ratio:
          avg: 100 * SUM(TCP_UTCL1_TRANSLATION_HIT_sum) / SUM(TCP_UTCL1_REQUEST_sum)
          min: 100 * MIN(TCP_UTCL1_TRANSLATION_HIT_sum / TCP_UTCL1_REQUEST_sum)
          max: 100 * MAX(TCP_UTCL1_TRANSLATION_HIT_sum / TCP_UTCL1_REQUEST_sum)
          units: Percent
        Hits:
          avg: SUM(TCP_UTCL1_TRANSLATION_HIT_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_TRANSLATION_HIT_sum / $denom)
          max: MAX(TCP_UTCL1_TRANSLATION_HIT_sum / $denom)
          units: (Requests + $normUnit)
        Translation Misses:
          avg: SUM(TCP_UTCL1_TRANSLATION_MISS_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_TRANSLATION_MISS_sum / $denom)
          max: MAX(TCP_UTCL1_TRANSLATION_MISS_sum / $denom)
          units: (Requests + $normUnit)
        Misses under Translation Miss:
          avg: SUM(TCP_UTCL1_TRANSLATION_MISS_UNDER_MISS_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_TRANSLATION_MISS_UNDER_MISS_sum / $denom)
          max: MAX(TCP_UTCL1_TRANSLATION_MISS_UNDER_MISS_sum / $denom)
          units: (Requests + $normUnit)
        Permission Misses:
          avg: SUM(TCP_UTCL1_PERMISSION_MISS_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_PERMISSION_MISS_sum / $denom)
          max: MAX(TCP_UTCL1_PERMISSION_MISS_sum / $denom)
          units: (Requests + $normUnit)
  - metric_table:
      id: 1606
      title: L1D Addr Translation Stalls
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        units: Unit
      metric:
        Cache Full Stall:
          avg: SUM(TCP_UTCL1_STALL_INFLIGHT_MAX_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_STALL_INFLIGHT_MAX_sum / $denom)
          max: MAX(TCP_UTCL1_STALL_INFLIGHT_MAX_sum / $denom)
          units: (Cycles + $normUnit)
        Cache Miss Stall:
          avg: SUM(TCP_UTCL1_STALL_MULTI_MISS_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_STALL_MULTI_MISS_sum / $denom)
          max: MAX(TCP_UTCL1_STALL_MULTI_MISS_sum / $denom)
          units: (Cycles + $normUnit)
        Serialization Stall:
          avg: SUM(TCP_UTCL1_SERIALIZATION_STALL_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_SERIALIZATION_STALL_sum / $denom)
          max: MAX(TCP_UTCL1_SERIALIZATION_STALL_sum / $denom)
          units: (Cycles + $normUnit)
        Thrashing Stall:
          avg: SUM(TCP_UTCL1_THRASHING_STALL_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_THRASHING_STALL_sum / $denom)
          max: MAX(TCP_UTCL1_THRASHING_STALL_sum / $denom)
          units: (Cycles + $normUnit)
        Latency FIFO Stall:
          avg: SUM(TCP_UTCL1_LFIFO_FULL_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_LFIFO_FULL_sum / $denom)
          max: MAX(TCP_UTCL1_LFIFO_FULL_sum / $denom)
          units: (Cycles + $normUnit)
        Resident Page Full Stall:
          avg: SUM(TCP_UTCL1_STALL_LFIFO_NO_RES_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_STALL_LFIFO_NO_RES_sum / $denom)
          max: MAX(TCP_UTCL1_STALL_LFIFO_NO_RES_sum / $denom)
          units: (Cycles + $normUnit)
        UTCL2 Stall:
          avg: SUM(TCP_UTCL1_STALL_UTCL2_REQ_OUT_OF_CREDITS_sum) / SUM($denom)
          min: MIN(TCP_UTCL1_STALL_UTCL2_REQ_OUT_OF_CREDITS_sum / $denom)
          max: MAX(TCP_UTCL1_STALL_UTCL2_REQ_OUT_OF_CREDITS_sum / $denom)
          units: (Cycles + $normUnit)
  metrics_description:
    Hit rate: The ratio of the number of vL1D cache line requests that hit in vL1D
      cache over the total number of cache line requests to the vL1D Cache RAM.
    Bandwidth Utilization: The number of bytes looked up in the vL1D cache as a result
      of VMEM instructions, as a percent of the peak theoretical bandwidth achievable
      on the specific accelerator. The number of bytes is calculated as the number
      of cache lines requested multiplied by the cache line size. This value does
      not consider partial requests, so for instance, if only a single value is requested
      in a cache line, the data movement will still be counted as a full cache line.
    Utilization: Indicates how busy the vL1D Cache RAM was during the kernel execution.
      The number of cycles where the vL1D Cache RAM is actively processing any request
      divided by the number of cycles where the vL1D is active.
    Coalescing: Indicates how well memory instructions were coalesced by the address
      processing unit, ranging from uncoalesced (25%) to fully coalesced (100%). Calculated
      as the average number of thread-requests generated per instruction divided by
      the ideal number of thread-requests per instruction.
    Stalled on L2 Data: The ratio of the number of cycles where the vL1D is stalled
      waiting for requested data to return from the L2 cache divided by the number
      of cycles where the vL1D is active.
    Stalled on L2 Req: The ratio of the number of cycles where the vL1D is stalled
      waiting to issue a request for data to the L2 cache divided by the number of
      cycles where the vL1D is active.
    Stalled on Address: >-
      The ratio of the number of cycles where the vL1D is stalled waiting for the
      address processing unit to send address information, divided by the number
      of cycles where the vL1D is active.
    Stalled on Data: >-
      The ratio of the number of cycles where the vL1D is stalled waiting for the
      address processing unit to send write/atomic data, divided by the number of
      cycles where the vL1D is active.
    Stalled on Latency FIFO: >-
      The ratio of the number of cycles where the vL1D is stalled because the
      latency FIFO (tracking outstanding requests) is full, divided by the number
      of cycles where the vL1D is active.
    Stalled on Request FIFO: >-
      The ratio of the number of cycles where the vL1D is stalled because the
      request FIFO (queuing memory requests) is full, divided by the number of
      cycles where the vL1D is active.
    Stalled on Read Return: >-
      The ratio of the number of cycles where the vL1D is stalled waiting for read
      data to return from L2 before it can write into the cache, divided by the
      number of cycles where the vL1D is active.
    Tag RAM Stall (Read): The ratio of the number of cycles where the vL1D is stalled
      due to Read requests with conflicting tags being looked up concurrently, divided
      by the number of cycles where the vL1D is active.
    Tag RAM Stall (Write): The ratio of the number of cycles where the vL1D is stalled
      due to Write requests with conflicting tags being looked up concurrently, divided
      by the number of cycles where the vL1D is active.
    Tag RAM Stall (Atomic): The ratio of the number of cycles where the vL1D is stalled
      due to Atomic requests with conflicting tags being looked up concurrently, divided
      by the number of cycles where the vL1D is active.
    Total Req: The total number of incoming requests from the address processing unit
      after coalescing.
    Read Req: The total number of incoming read requests from the address processing
      unit after coalescing per normalization unit.
    Write Req: The total number of incoming write requests from the address processing
      unit after coalescing per normalization unit.
    Atomic Req: The total number of incoming atomic requests from the address processing
      unit after coalescing per normalization unit.
    Cache BW: The number of bytes looked up in the vL1D cache as a result of VMEM
      instructions divided by total duration. The number of bytes is calculated as
      the number of cache lines requested multiplied by the cache line size. This
      value does not consider partial requests, so for instance, if only a single
      value is requested in a cache line, the data movement will still be counted
      as a full cache line.
    Cache Hit Rate: The ratio of the number of vL1D cache line requests that hit in
      vL1D cache over the total number of cache line requests to the vL1D Cache RAM.
    Cache Accesses: The total number of cache line lookups in the vL1D.
    Cache Hits: The number of cache accesses minus the number of outgoing requests
      to the L2 cache, that is, the number of cache line requests serviced by the
      vL1D Cache RAM per normalization unit.
    Invalidations: The number of times the vL1D was issued a write-back invalidate
      command during the kernel's execution per normalization unit. This may be triggered
      by, for instance, the buffer_wbinvl1 instruction.
    L1-L2 BW: The number of bytes transferred across the vL1D-L2 interface as a result
      of VMEM instructions, divided by total duration. The number of bytes is calculated
      as the number of cache lines requested multiplied by the cache line size. This
      value does not consider partial requests, so for instance, if only a single
      value is requested in a cache line, the data movement will still be counted
      as a full cache line.
    Tag RAM 0 Req: >-
      The total number of vL1D cache requests (cache line lookups) that mapped to
      Tag RAM bank 0 during the vL1D tag lookup process, per normalization unit.
      The vL1D cache uses multiple Tag RAM banks for parallel tag lookups.
      Distribution across banks affects lookup efficiency.
    Tag RAM 1 Req: >-
      The total number of vL1D cache requests (cache line lookups) that mapped to
      Tag RAM bank 1 during the vL1D tag lookup process, per normalization unit.
      The vL1D cache uses multiple Tag RAM banks for parallel tag lookups.
      Distribution across banks affects lookup efficiency.
    Tag RAM 2 Req: >-
      The total number of vL1D cache requests (cache line lookups) that mapped to
      Tag RAM bank 2 during the vL1D tag lookup process, per normalization unit.
      The vL1D cache uses multiple Tag RAM banks for parallel tag lookups.
      Distribution across banks affects lookup efficiency.
    Tag RAM 3 Req: >-
      The total number of vL1D cache requests (cache line lookups) that mapped to
      Tag RAM bank 3 during the vL1D tag lookup process, per normalization unit.
      The vL1D cache uses multiple Tag RAM banks for parallel tag lookups.
      Distribution across banks affects lookup efficiency.
    L1-L2 Read: The number of read requests for a vL1D cache line that were not satisfied
      by the vL1D and must be retrieved from the to the L2 Cache per normalization
      unit.
    L1-L2 Write: The number of write requests to a vL1D cache line that were sent
      through the vL1D to the L2 cache, per normalization unit.
    L1-L2 Atomic: The number of atomic requests that are sent through the vL1D to
      the L2 cache, per normalization unit. This includes requests for atomics with,
      and without return.
    L1 Access Latency: Calculated as the average number of cycles that a vL1D cache
      line request spent in the vL1D cache pipeline.
    L1-L2 Read Latency: Calculated as the average number of cycles that the vL1D cache
      took to issue and receive read requests from the L2 Cache. This number also
      includes requests for atomics with return values.
    L1-L2 Write Latency: Calculated as the average number of cycles that the vL1D
      cache took to issue and receive acknowledgement of a write request to the L2
      Cache. This number also includes requests for atomics without return values.
    Inflight Req: >-
      The sum of inflight client-to-UTCL1 translation requests per cycle, per
      normalization unit. Measures the average number of translation requests
      actively being processed by the UTCL1 at any given time.
    Misses under Translation Miss: >-
      The number of translation requests that missed in the UTCL1 while another
      miss for the same translation was already being processed, per normalization
      unit. These are secondary misses that occur before the first miss completes.
    Cache Full Stall: >-
      The number of cycles the UTCL1 was stalled due to the inflight request
      counter reaching its maximum capacity, per normalization unit. Indicates
      the translation cache cannot accept more requests.
    Cache Miss Stall: >-
      The number of cycles the UTCL1 was stalled due to multiple cache misses
      being arbitrated simultaneously, per normalization unit.
    Serialization Stall: >-
      The number of cycles the UTCL1 was stalled due to serializing translation
      requests through the cache, per normalization unit.
    Thrashing Stall: >-
      The number of cycles the UTCL1 was stalled due to cache thrashing (rapid
      eviction and reload of translations), per normalization unit. This is an
      estimation when thrashing detection is active.
    Latency FIFO Stall: >-
      The number of cycles the UTCL1-to-UTCL2 latency-hiding FIFO was full,
      per normalization unit. This FIFO queues requests to the L2 translation
      cache (UTCL2).
    Resident Page Full Stall: >-
      The number of cycles the UTCL1 was stalled because the latency-hiding
      FIFO output indicated a non-resident page, per normalization unit.
    UTCL2 Stall: >-
      The number of cycles the UTCL1 was stalled due to running out of request
      credits for the UTCL2 (L2 translation cache), per normalization unit.
    NC - Read: Total read requests with NC mtype from this TCP to all TCCs Sum over
      TCP instances per normalization unit.
    UC - Read: Total read requests with UC mtype from this TCP to all TCCs Sum over
      TCP instances per normalization unit.
    CC - Read: Total read requests with CC mtype from this TCP to all TCCs Sum over
      TCP instances per normalization unit.
    RW - Read: Total read requests with RW mtype from this TCP to all TCCs Sum over
      TCP instances per normalization unit.
    RW - Write: Total write requests with RW mtype from this TCP to all TCCs Sum over
      TCP instances per normalization unit.
    NC - Write: Total write requests with NC mtype from this TCP to all TCCs Sum over
      TCP instances per normalization unit.
    UC - Write: Total write requests with UC mtype from this TCP to all TCCs Sum over
      TCP instances per normalization unit.
    CC - Write: Total write requests with CC mtype from this TCP to all TCCs Sum over
      TCP instances per normalization unit.
    NC - Atomic: Total atomic requests with NC mtype from this TCP to all TCCs Sum
      over TCP instances per normalization unit.
    UC - Atomic: Total atomic requests with UC mtype from this TCP to all TCCs Sum
      over TCP instances per normalization unit.
    CC - Atomic: Total atomic requests with CC mtype from this TCP to all TCCs Sum
      over TCP instances per normalization unit.
    RW - Atomic: Total atomic requests with RW mtype from this TCP to all TCCs Sum
      over TCP instances per normalization unit.
    Req: The number of translation requests made to the UTCL1 per normalization unit.
    Hit Ratio: The ratio of the number of translation requests that hit in the UTCL1
      divided by the total number of translation requests made to the UTCL1.
    Hits: The number of translation requests that hit in the UTCL1, and could be reused,
      per normalization unit.
    Translation Misses: The total number of translation requests that missed in the
      UTCL1 due to translation not being present in the cache, per normalization unit.
    Permission Misses: >-
      The total number of translation requests that missed in the UTCL1 due
      to a permission error, per normalization unit. This is unused and expected
      to be zero in most configurations for modern CDNA\u2122 accelerators.
