# AUTOGENERATED FILE. Only edit for testing purposes, not for development. Generated from utils/unified_config.yaml. Generated by utils/split_config.py
Panel Config:
  id: 500
  title: Command Processor (CPC/CPF)
  data source:
  - metric_table:
      id: 501
      title: Command processor fetcher (CPF)
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        unit: Unit
      metric:
        CPF Utilization:
          avg: 100 * SUM(CPF_CPF_STAT_BUSY) / SUM(CPF_CPF_STAT_BUSY + CPF_CPF_STAT_IDLE)
          min: 100 * MIN(CPF_CPF_STAT_BUSY / (CPF_CPF_STAT_BUSY + CPF_CPF_STAT_IDLE))
          max: 100 * MAX(CPF_CPF_STAT_BUSY / (CPF_CPF_STAT_BUSY + CPF_CPF_STAT_IDLE))
          unit: Percent
        CPF Stall:
          avg: 100 * SUM(CPF_CPF_STAT_STALL) / SUM(CPF_CPF_STAT_BUSY)
          min: 100 * MIN(CPF_CPF_STAT_STALL / CPF_CPF_STAT_BUSY)
          max: 100 * MAX(CPF_CPF_STAT_STALL / CPF_CPF_STAT_BUSY)
          unit: Percent
        CPF-L2 Utilization:
          avg: 100 * SUM(CPF_CPF_TCIU_BUSY) / SUM(CPF_CPF_TCIU_BUSY + CPF_CPF_TCIU_IDLE)
          min: 100 * MIN(CPF_CPF_TCIU_BUSY / (CPF_CPF_TCIU_BUSY + CPF_CPF_TCIU_IDLE))
          max: 100 * MAX(CPF_CPF_TCIU_BUSY / (CPF_CPF_TCIU_BUSY + CPF_CPF_TCIU_IDLE))
          unit: Percent
        CPF-L2 Stall:
          avg: 100 * SUM(CPF_CPF_TCIU_STALL) / SUM(CPF_CPF_TCIU_BUSY)
          min: 100 * MIN(CPF_CPF_TCIU_STALL / CPF_CPF_TCIU_BUSY)
          max: 100 * MAX(CPF_CPF_TCIU_STALL / CPF_CPF_TCIU_BUSY)
          unit: Percent
        CPF-UTCL1 Stall:
          avg: 100 * SUM(CPF_CMP_UTCL1_STALL_ON_TRANSLATION) / SUM(CPF_CPF_STAT_BUSY)
          min: 100 * MIN(CPF_CMP_UTCL1_STALL_ON_TRANSLATION / CPF_CPF_STAT_BUSY)
          max: 100 * MAX(CPF_CMP_UTCL1_STALL_ON_TRANSLATION / CPF_CPF_STAT_BUSY)
          unit: Percent
  - metric_table:
      id: 502
      title: Command processor packet processor (CPC)
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        unit: Unit
      metric:
        CPC SYNC FIFO Full Rate:
          avg: 100 * SUM(CPC_SYNC_FIFO_FULL) / SUM(CPC_SYNC_WRREQ_FIFO_BUSY)
          min: 100 * MIN(CPC_SYNC_FIFO_FULL / CPC_SYNC_WRREQ_FIFO_BUSY)
          max: 100 * MAX(CPC_SYNC_FIFO_FULL / CPC_SYNC_WRREQ_FIFO_BUSY)
          unit: Percent
        CPC CANE Stall Rate:
          avg: 100 * SUM(CPC_CANE_STALL) / SUM(CPC_CANE_BUSY)
          min: 100 * MIN(CPC_CANE_STALL / CPC_CANE_BUSY)
          max: 100 * MAX(CPC_CANE_STALL / CPC_CANE_BUSY)
          unit: Percent
        CPC ADC Utilization:
          avg: 100 * SUM(CPC_TG_SEND) / SUM(CPC_GD_BUSY)
          min: 100 * MIN(CPC_TG_SEND / CPC_GD_BUSY)
          max: 100 * MAX(CPC_TG_SEND / CPC_GD_BUSY)
          unit: Percent
        CPC Utilization:
          avg: 100 * SUM(CPC_CPC_STAT_BUSY) / SUM(CPC_CPC_STAT_BUSY + CPC_CPC_STAT_IDLE)
          min: 100 * MIN(CPC_CPC_STAT_BUSY / (CPC_CPC_STAT_BUSY + CPC_CPC_STAT_IDLE))
          max: 100 * MAX(CPC_CPC_STAT_BUSY / (CPC_CPC_STAT_BUSY + CPC_CPC_STAT_IDLE))
          unit: Percent
        CPC Stall Rate:
          avg: 100 * SUM(CPC_CPC_STAT_STALL) / SUM(CPC_CPC_STAT_BUSY)
          min: 100 * MIN(CPC_CPC_STAT_STALL / CPC_CPC_STAT_BUSY)
          max: 100 * MAX(CPC_CPC_STAT_STALL / CPC_CPC_STAT_BUSY)
          unit: Percent
        CPC Packet Decoding Utilization:
          avg: 100 * SUM(CPC_ME1_BUSY_FOR_PACKET_DECODE) / SUM(CPC_CPC_STAT_BUSY)
          min: 100 * MIN(CPC_ME1_BUSY_FOR_PACKET_DECODE / CPC_CPC_STAT_BUSY)
          max: 100 * MAX(CPC_ME1_BUSY_FOR_PACKET_DECODE / CPC_CPC_STAT_BUSY)
          unit: Percent
        CPC-Workgroup Manager Utilization:
          avg: 100 * SUM(CPC_ME1_DC0_SPI_BUSY) / SUM(CPC_CPC_STAT_BUSY)
          min: 100 * MIN(CPC_ME1_DC0_SPI_BUSY / CPC_CPC_STAT_BUSY)
          max: 100 * MAX(CPC_ME1_DC0_SPI_BUSY / CPC_CPC_STAT_BUSY)
          unit: Percent
        CPC-L2 Utilization:
          avg: 100 * SUM(CPC_CPC_TCIU_BUSY) / SUM(CPC_CPC_TCIU_BUSY + CPC_CPC_TCIU_IDLE)
          min: 100 * MIN(CPC_CPC_TCIU_BUSY / (CPC_CPC_TCIU_BUSY + CPC_CPC_TCIU_IDLE))
          max: 100 * MAX(CPC_CPC_TCIU_BUSY / (CPC_CPC_TCIU_BUSY + CPC_CPC_TCIU_IDLE))
          unit: Percent
        CPC-UTCL1 Stall:
          avg: 100 * SUM(CPC_UTCL1_STALL_ON_TRANSLATION) / SUM(CPC_CPC_STAT_BUSY)
          min: 100 * MIN(CPC_UTCL1_STALL_ON_TRANSLATION / CPC_CPC_STAT_BUSY)
          max: 100 * MAX(CPC_UTCL1_STALL_ON_TRANSLATION / CPC_CPC_STAT_BUSY)
          unit: Percent
        CPC-UTCL2 Utilization:
          avg: 100 * SUM(CPC_CPC_UTCL2IU_BUSY) / SUM(CPC_CPC_UTCL2IU_BUSY + CPC_CPC_UTCL2IU_IDLE)
          min: 100 * MIN(CPC_CPC_UTCL2IU_BUSY / (CPC_CPC_UTCL2IU_BUSY + CPC_CPC_UTCL2IU_IDLE))
          max: 100 * MAX(CPC_CPC_UTCL2IU_BUSY / (CPC_CPC_UTCL2IU_BUSY + CPC_CPC_UTCL2IU_IDLE))
          unit: Percent
  metrics_description:
    CPF Utilization: Percent of total cycles where the CPF was busy actively doing
      any work. The ratio of CPF busy cycles over total cycles counted by the CPF.
    CPF Stall: Percent of CPF busy cycles where the CPF was stalled for any reason.
    CPF-L2 Utilization: Percent of total cycles counted by the CPF-L2 interface where
      the CPF-L2 interface was active doing any work. The ratio of CPF-L2 busy cycles
      over total cycles counted by the CPF-L2.
    CPF-L2 Stall: Percent of CPF-L2 L2 busy cycles where the CPF-L2 interface was
      stalled for any reason.
    CPF-UTCL1 Stall: Percent of CPF busy cycles where the CPF was stalled by address
      translation.
    CPC Utilization: Percent of total cycles where the CPC was busy actively doing
      any work. The ratio of CPC busy cycles over total cycles counted by the CPC.
    CPC SYNC FIFO Full Rate: Percent of CPC sync counter request FIFO busy cycles
      where the FIFO was full. High values indicate backpressure in synchronization
      counter processing.
    CPC CANE Stall Rate: Percent of CPC CANE bus busy cycles where sync counter
      requests were stalled. High values indicate contention on the CANE bus for
      synchronization counter operations.
    CPC ADC Utilization: Percent of ADC busy cycles spent dispatching thread groups.
      The ADC is responsible for sending workgroups from the command processor to
      the workgroup manager.
    CPC Stall Rate: Percent of CPC busy cycles where the CPC was stalled for any reason.
    CPC Packet Decoding Utilization: Percent of CPC busy cycles spent decoding commands
      for processing.
    CPC-Workgroup Manager Utilization: Percent of CPC busy cycles spent dispatching
      workgroups to the workgroup manager.
    CPC-L2 Utilization: Percent of total cycles counted by the CPC-L2 interface where
      the CPC-L2 interface was active doing any work.
    CPC-UTCL1 Stall: Percent of CPC busy cycles where the CPC was stalled by address
      translation
    CPC-UTCL2 Utilization: >-
      Percent of total cycles counted by the CPC's L2 address translation
      interface where the CPC was busy doing address translation work.
