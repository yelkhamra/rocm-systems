# AUTOGENERATED FILE. Only edit for testing purposes, not for development. Generated from utils/unified_config.yaml. Generated by utils/split_config.py
Panel Config:
  id: 600
  title: Workgroup Manager (SPI)
  data source:
  - metric_table:
      id: 601
      title: Workgroup manager utilizations
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        unit: Unit
      metric:
        Accelerator Utilization:
          avg: 100 * SUM($GRBM_GUI_ACTIVE_PER_XCD) / SUM($GRBM_COUNT_PER_XCD)
          min: 100 * MIN($GRBM_GUI_ACTIVE_PER_XCD / $GRBM_COUNT_PER_XCD)
          max: 100 * MAX($GRBM_GUI_ACTIVE_PER_XCD / $GRBM_COUNT_PER_XCD)
          unit: Percent
        Scheduler-Pipe Utilization:
          avg: 100 * SUM(SPI_CSN_BUSY) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu)
          min: 100 * MIN(SPI_CSN_BUSY / ($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu))
          max: 100 * MAX(SPI_CSN_BUSY / ($GRBM_GUI_ACTIVE_PER_XCD * $pipes_per_gpu * $se_per_gpu))
          unit: Percent
        Workgroup Manager Utilization:
          avg: 100 * SUM($GRBM_SPI_BUSY_PER_XCD) / SUM($GRBM_GUI_ACTIVE_PER_XCD)
          min: 100 * MIN($GRBM_SPI_BUSY_PER_XCD / $GRBM_GUI_ACTIVE_PER_XCD)
          max: 100 * MAX($GRBM_SPI_BUSY_PER_XCD / $GRBM_GUI_ACTIVE_PER_XCD)
          unit: Percent
        Shader Engine Utilization:
          avg: 100 * SUM(SQ_BUSY_CYCLES) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $se_per_gpu)
          min: 100 * MIN(SQ_BUSY_CYCLES / ($GRBM_GUI_ACTIVE_PER_XCD * $se_per_gpu))
          max: 100 * MAX(SQ_BUSY_CYCLES / ($GRBM_GUI_ACTIVE_PER_XCD * $se_per_gpu))
          unit: Percent
        SIMD Utilization:
          avg: 100 * SUM(SQ_BUSY_CU_CYCLES) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 100 * MIN(SQ_BUSY_CU_CYCLES / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          max: 100 * MAX(SQ_BUSY_CU_CYCLES / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          unit: Percent
        Dispatched Workgroups:
          avg: AVG(SPI_CSN_NUM_THREADGROUPS)
          min: MIN(SPI_CSN_NUM_THREADGROUPS)
          max: MAX(SPI_CSN_NUM_THREADGROUPS)
          unit: Workgroups
        Dispatched Wavefronts:
          avg: AVG(SPI_CSN_WAVE)
          min: MIN(SPI_CSN_WAVE)
          max: MAX(SPI_CSN_WAVE)
          unit: Wavefronts
        VGPR Writes:
          avg: 4 * SUM(SPI_VWC_CSC_WR) / SUM(SPI_CSN_WAVE)
          min: 4 * MIN(SPI_VWC_CSC_WR / SPI_CSN_WAVE)
          max: 4 * MAX(SPI_VWC_CSC_WR / SPI_CSN_WAVE)
          unit: Cycles/wave
        SGPR Writes:
          avg: SUM(SPI_SWC_CSC_WR) / SUM(SPI_CSN_WAVE)
          min: MIN(SPI_SWC_CSC_WR / SPI_CSN_WAVE)
          max: MAX(SPI_SWC_CSC_WR / SPI_CSN_WAVE)
          unit: Cycles/wave
  - metric_table:
      id: 602
      title: Workgroup Manager - Resource Allocation
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        unit: Unit
      metric:
        Not-scheduled Rate (Workgroup Manager):
          avg: 100 * SUM(SPI_RA_REQ_NO_ALLOC_CSN) / SUM($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu)
          min: 100 * MIN(SPI_RA_REQ_NO_ALLOC_CSN / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
          max: 100 * MAX(SPI_RA_REQ_NO_ALLOC_CSN / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
          unit: Percent
        Not-scheduled Rate (Scheduler-Pipe):
          avg: 100 * SUM(SPI_RA_REQ_NO_ALLOC) / SUM($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu)
          min: 100 * MIN(SPI_RA_REQ_NO_ALLOC / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
          max: 100 * MAX(SPI_RA_REQ_NO_ALLOC / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
          unit: Percent
        Scheduler-Pipe Stall Rate:
          avg: 100 * SUM(SPI_RA_RES_STALL_CSN) / SUM($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu)
          min: 100 * MIN(SPI_RA_RES_STALL_CSN / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
          max: 100 * MAX(SPI_RA_RES_STALL_CSN / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
          unit: Percent
        Scratch Stall Rate:
          avg: 100 * SUM(SPI_RA_TMP_STALL_CSN) / SUM($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu)
          min: 100 * MIN(SPI_RA_TMP_STALL_CSN / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
          max: 100 * MAX(SPI_RA_TMP_STALL_CSN / ($GRBM_SPI_BUSY_PER_XCD * $se_per_gpu))
          unit: Percent
        Insufficient SIMD Waveslots:
          avg: 100 * SUM(SPI_RA_WAVE_SIMD_FULL_CSN) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 100 * MIN(SPI_RA_WAVE_SIMD_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          max: 100 * MAX(SPI_RA_WAVE_SIMD_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          unit: Percent
        Insufficient SIMD VGPRs:
          avg: 100 * SUM(SPI_RA_VGPR_SIMD_FULL_CSN) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 100 * MIN(SPI_RA_VGPR_SIMD_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          max: 100 * MAX(SPI_RA_VGPR_SIMD_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          unit: Percent
        Insufficient SIMD SGPRs:
          avg: 100 * SUM(SPI_RA_SGPR_SIMD_FULL_CSN) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 100 * MIN(SPI_RA_SGPR_SIMD_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          max: 100 * MAX(SPI_RA_SGPR_SIMD_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          unit: Percent
        Insufficient CU LDS:
          avg: 400 * SUM(SPI_RA_LDS_CU_FULL_CSN) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 400 * MIN(SPI_RA_LDS_CU_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          max: 400 * MAX(SPI_RA_LDS_CU_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          unit: Percent
        Insufficient CU Barriers:
          avg: 400 * SUM(SPI_RA_BAR_CU_FULL_CSN) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 400 * MIN(SPI_RA_BAR_CU_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          max: 400 * MAX(SPI_RA_BAR_CU_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          unit: Percent
        Reached CU Workgroup Limit:
          avg: 400 * SUM(SPI_RA_TGLIM_CU_FULL_CSN) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 400 * MIN(SPI_RA_TGLIM_CU_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          max: 400 * MAX(SPI_RA_TGLIM_CU_FULL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          unit: Percent
        Reached CU Wavefront Limit:
          avg: 400 * SUM(SPI_RA_WVLIM_STALL_CSN) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 400 * MIN(SPI_RA_WVLIM_STALL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          max: 400 * MAX(SPI_RA_WVLIM_STALL_CSN / ($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu))
          unit: Percent
  metrics_description:
    Accelerator Utilization: The percent of cycles in the kernel where the accelerator
      was actively doing any work.
    Scheduler-Pipe Utilization: The percent of total scheduler-pipe cycles in the
      kernel where the scheduler-pipes were actively doing any work.
    Workgroup Manager Utilization: The percent of cycles in the kernel where the workgroup
      manager was actively doing any work.
    Shader Engine Utilization: The percent of total shader engine cycles in the kernel
      where any CU in a shader-engine was actively doing any work, normalized over
      all shader-engines. Low values (e.g., << 100%) indicate that the accelerator
      was not fully saturated by the kernel, or a potential load-imbalance issue.
    SIMD Utilization: The percent of total SIMD cycles in the kernel where any SIMD
      on a CU was actively doing any work, summed over all CUs. Low values (less than
      100%) indicate that the accelerator was not fully saturated by the kernel, or
      a potential load-imbalance issue.
    Dispatched Workgroups: The total number of workgroups forming this kernel launch.
    Dispatched Wavefronts: The total number of wavefronts, summed over all workgroups,
      forming this kernel launch.
    VGPR Writes: The average number of cycles spent initializing VGPRs at wave creation.
    SGPR Writes: The average number of cycles spent initializing SGPRs at wave creation.
    Not-scheduled Rate (Workgroup Manager): The percent of total scheduler-pipe cycles
      in the kernel where a workgroup could not be scheduled to a CU due to a bottleneck
      within the workgroup manager rather than a lack of a CU or SIMD with sufficient
      resources.
    Not-scheduled Rate (Scheduler-Pipe): >-
      The percent of total scheduler-pipe cycles in the kernel where a workgroup
      could not be scheduled to a CU due to a bottleneck within the scheduler-pipes
      rather than a lack of a CU or SIMD with sufficient resources.
    Scheduler-Pipe Stall Rate: The percent of total scheduler-pipe cycles in the kernel
      where a workgroup could not be scheduled to a CU due to occupancy limitations
      (like a lack of a CU or SIMD with sufficient resources).
    Scratch Stall Rate: The percent of total shader-engine cycles in the kernel where
      a workgroup could not be scheduled to a CU due to lack of private (a.k.a., scratch)
      memory slots. While this can reach up to 100%, note that the actual occupancy
      limitations on a kernel using private memory are typically quite small (for
      example, less than 1% of the total number of waves that can be scheduled to
      an accelerator).
    Insufficient SIMD Waveslots: The percent of total SIMD cycles in the kernel where
      a workgroup could not be scheduled to a SIMD due to lack of available waveslots.
    Insufficient SIMD VGPRs: The percent of total SIMD cycles in the kernel where
      a workgroup could not be scheduled to a SIMD due to lack of available VGPRs.
    Insufficient SIMD SGPRs: The percent of total SIMD cycles in the kernel where
      a workgroup could not be scheduled to a SIMD due to lack of available SGPRs.
    Insufficient CU LDS: The percent of total CU cycles in the kernel where a workgroup
      could not be scheduled to a CU due to lack of available LDS.
    Insufficient CU Barriers: The percent of total CU cycles in the kernel where a
      workgroup could not be scheduled to a CU due to lack of available barriers.
    Reached CU Workgroup Limit: The percent of total CU cycles in the kernel where
      a workgroup could not be scheduled to a CU due to limits within the workgroup
      manager. This is expected to be always be zero on CDNA2 or newer accelerators
      (and small for previous accelerators).
    Reached CU Wavefront Limit: The percent of total CU cycles in the kernel where
      a wavefront could not be scheduled to a CU due to limits within the workgroup
      manager. This is expected to be always be zero on CDNA2 or newer accelerators
      (and small for previous accelerators).
