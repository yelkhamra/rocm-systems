# AUTOGENERATED FILE. Only edit for testing purposes, not for development. Generated from utils/unified_config.yaml. Generated by utils/split_config.py
Panel Config:
  id: 1100
  title: Compute Units - Compute Pipeline
  data source:
  - metric_table:
      id: 1101
      title: Compute Speed-of-Light
      header:
        metric: Metric
        value: Avg
        unit: Unit
        peak: Peak
        pct_of_peak: Percent of Peak
      metric:
        VALU FLOPs:
          value: 64 * SUM(SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + SQ_INSTS_VALU_TRANS_F16 + 2 * SQ_INSTS_VALU_FMA_F16 + SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + SQ_INSTS_VALU_TRANS_F32 + 2 * SQ_INSTS_VALU_FMA_F32 + SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + SQ_INSTS_VALU_TRANS_F64 + 2 * SQ_INSTS_VALU_FMA_F64) / SUM(End_Timestamp - Start_Timestamp)
          unit: GFLOPs
          peak: $max_sclk * $cu_per_gpu * 64 * 2 / 1000
          pct_of_peak: true
        MFMA FLOPs (BF16):
          value: 512 * SUM(SQ_INSTS_VALU_MFMA_MOPS_BF16) / SUM(End_Timestamp - Start_Timestamp)
          unit: GFLOPs
          peak: $max_sclk * $cu_per_gpu * 1024 / 1000
          pct_of_peak: true
        MFMA FLOPs (F16):
          value: 512 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F16) / SUM(End_Timestamp - Start_Timestamp)
          unit: GFLOPs
          peak: $max_sclk * $cu_per_gpu * 1024 / 1000
          pct_of_peak: true
        MFMA FLOPs (F32):
          value: 512 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F32) / SUM(End_Timestamp - Start_Timestamp)
          unit: GFLOPs
          peak: $max_sclk * $cu_per_gpu * 256 / 1000
          pct_of_peak: true
        MFMA FLOPs (F64):
          value: 512 * SUM(SQ_INSTS_VALU_MFMA_MOPS_F64) / SUM(End_Timestamp - Start_Timestamp)
          unit: GFLOPs
          peak: $max_sclk * $cu_per_gpu * 256 / 1000
          pct_of_peak: true
        MFMA IOPs (INT8):
          value: 512 * SUM(SQ_INSTS_VALU_MFMA_MOPS_I8) / SUM(End_Timestamp - Start_Timestamp)
          unit: GIOPs
          peak: $max_sclk * $cu_per_gpu * 1024 / 1000
          pct_of_peak: true
  - metric_table:
      id: 1102
      title: Pipeline Statistics
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        unit: Unit
      metric:
        IPC:
          avg: SUM(SQ_INSTS) / SUM(SQ_BUSY_CU_CYCLES)
          min: MIN(SQ_INSTS / SQ_BUSY_CU_CYCLES)
          max: MAX(SQ_INSTS / SQ_BUSY_CU_CYCLES)
          unit: Instructions per Cycle
        IPC (Issued):
          avg: SUM(SQ_INSTS_VALU + SQ_INSTS_VMEM + SQ_INSTS_SALU + SQ_INSTS_SMEM + SQ_INSTS_BRANCH + SQ_INSTS_SENDMSG + SQ_INSTS_VSKIPPED + SQ_INSTS_LDS) / SUM(SQ_ACTIVE_INST_ANY)
          min: MIN((SQ_INSTS_VALU + SQ_INSTS_VMEM + SQ_INSTS_SALU + SQ_INSTS_SMEM + SQ_INSTS_BRANCH + SQ_INSTS_SENDMSG + SQ_INSTS_VSKIPPED + SQ_INSTS_LDS) / SQ_ACTIVE_INST_ANY)
          max: MAX((SQ_INSTS_VALU + SQ_INSTS_VMEM + SQ_INSTS_SALU + SQ_INSTS_SMEM + SQ_INSTS_BRANCH + SQ_INSTS_SENDMSG + SQ_INSTS_VSKIPPED + SQ_INSTS_LDS) / SQ_ACTIVE_INST_ANY)
          unit: Instructions per Cycle
        SALU Utilization:
          avg: 100 * SUM(SQ_ACTIVE_INST_SCA) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 100 * MIN(SQ_ACTIVE_INST_SCA / $GRBM_GUI_ACTIVE_PER_XCD / $cu_per_gpu)
          max: 100 * MAX(SQ_ACTIVE_INST_SCA / $GRBM_GUI_ACTIVE_PER_XCD / $cu_per_gpu)
          unit: Percent
        VALU Utilization:
          avg: 100 * SUM(SQ_ACTIVE_INST_VALU) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 100 * MIN(SQ_ACTIVE_INST_VALU / $GRBM_GUI_ACTIVE_PER_XCD / $cu_per_gpu)
          max: 100 * MAX(SQ_ACTIVE_INST_VALU / $GRBM_GUI_ACTIVE_PER_XCD / $cu_per_gpu)
          unit: Percent
        VMEM Utilization:
          avg: 100 * SUM(SQ_ACTIVE_INST_FLAT + SQ_ACTIVE_INST_VMEM) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 100 * MIN((SQ_ACTIVE_INST_FLAT + SQ_ACTIVE_INST_VMEM) / $GRBM_GUI_ACTIVE_PER_XCD / $cu_per_gpu)
          max: 100 * MAX((SQ_ACTIVE_INST_FLAT + SQ_ACTIVE_INST_VMEM) / $GRBM_GUI_ACTIVE_PER_XCD / $cu_per_gpu)
          unit: Percent
        Branch Utilization:
          avg: 100 * SUM(SQ_ACTIVE_INST_MISC) / SUM($GRBM_GUI_ACTIVE_PER_XCD * $cu_per_gpu)
          min: 100 * MIN(SQ_ACTIVE_INST_MISC / $GRBM_GUI_ACTIVE_PER_XCD / $cu_per_gpu)
          max: 100 * MAX(SQ_ACTIVE_INST_MISC / $GRBM_GUI_ACTIVE_PER_XCD / $cu_per_gpu)
          unit: Percent
        VALU Active Threads:
          avg: SUM(SQ_THREAD_CYCLES_VALU) / SUM(SQ_ACTIVE_INST_VALU)
          min: MIN(SQ_THREAD_CYCLES_VALU / SQ_ACTIVE_INST_VALU)
          max: MAX(SQ_THREAD_CYCLES_VALU / SQ_ACTIVE_INST_VALU)
          unit: Work-items
        MFMA Utilization:
          avg: 100 * SUM(SQ_VALU_MFMA_BUSY_CYCLES) / (4 * SUM($cu_per_gpu * $GRBM_GUI_ACTIVE_PER_XCD))
          min: 100 * MIN(SQ_VALU_MFMA_BUSY_CYCLES / (4 * $cu_per_gpu * $GRBM_GUI_ACTIVE_PER_XCD))
          max: 100 * MAX(SQ_VALU_MFMA_BUSY_CYCLES / (4 * $cu_per_gpu * $GRBM_GUI_ACTIVE_PER_XCD))
          unit: Percent
        MFMA Instruction Cycles:
          avg: SUM(SQ_VALU_MFMA_BUSY_CYCLES) / SUM(SQ_INSTS_MFMA)
          min: MIN(SQ_VALU_MFMA_BUSY_CYCLES / SQ_INSTS_MFMA)
          max: MAX(SQ_VALU_MFMA_BUSY_CYCLES / SQ_INSTS_MFMA)
          unit: Cycles per Instruction
        VMEM Latency:
          avg: SUM(SQ_INST_LEVEL_VMEM_ACCUM) / SUM(SQ_INSTS_VMEM)
          min: MIN(SQ_INST_LEVEL_VMEM_ACCUM / SQ_INSTS_VMEM)
          max: MAX(SQ_INST_LEVEL_VMEM_ACCUM / SQ_INSTS_VMEM)
          unit: Cycles
        SMEM Latency:
          avg: SUM(SQ_INST_LEVEL_SMEM_ACCUM) / SUM(SQ_INSTS_SMEM)
          min: MIN(SQ_INST_LEVEL_SMEM_ACCUM / SQ_INSTS_SMEM)
          max: MAX(SQ_INST_LEVEL_SMEM_ACCUM / SQ_INSTS_SMEM)
          unit: Cycles
  - metric_table:
      id: 1103
      title: Arithmetic Operations
      header:
        metric: Metric
        avg: Avg
        min: Min
        max: Max
        unit: Unit
      metric:
        FLOPs (Total):
          avg: SUM(64 * (SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + SQ_INSTS_VALU_TRANS_F16 + SQ_INSTS_VALU_FMA_F16 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F16 + 512 * SQ_INSTS_VALU_MFMA_MOPS_BF16 + 64 * (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + SQ_INSTS_VALU_TRANS_F32 + SQ_INSTS_VALU_FMA_F32 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F32 + 64 * (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + SQ_INSTS_VALU_TRANS_F64 + SQ_INSTS_VALU_FMA_F64 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F64) / SUM($denom)
          min: MIN((64 * (SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + SQ_INSTS_VALU_TRANS_F16 + SQ_INSTS_VALU_FMA_F16 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F16 + 512 * SQ_INSTS_VALU_MFMA_MOPS_BF16 + 64 * (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + SQ_INSTS_VALU_TRANS_F32 + SQ_INSTS_VALU_FMA_F32 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F32 + 64 * (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + SQ_INSTS_VALU_TRANS_F64 + SQ_INSTS_VALU_FMA_F64 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F64) / $denom)
          max: MAX((64 * (SQ_INSTS_VALU_ADD_F16 + SQ_INSTS_VALU_MUL_F16 + SQ_INSTS_VALU_TRANS_F16 + SQ_INSTS_VALU_FMA_F16 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F16 + 512 * SQ_INSTS_VALU_MFMA_MOPS_BF16 + 64 * (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + SQ_INSTS_VALU_TRANS_F32 + SQ_INSTS_VALU_FMA_F32 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F32 + 64 * (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + SQ_INSTS_VALU_TRANS_F64 + SQ_INSTS_VALU_FMA_F64 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F64) / $denom)
          unit: (FLOPs + $normUnit)
        IOPs (Total):
          avg: SUM(64 * (SQ_INSTS_VALU_INT32 + SQ_INSTS_VALU_INT64) + SQ_INSTS_VALU_MFMA_MOPS_I8 * 512) / SUM($denom)
          min: MIN((64 * (SQ_INSTS_VALU_INT32 + SQ_INSTS_VALU_INT64) + SQ_INSTS_VALU_MFMA_MOPS_I8 * 512) / $denom)
          max: MAX((64 * (SQ_INSTS_VALU_INT32 + SQ_INSTS_VALU_INT64) + SQ_INSTS_VALU_MFMA_MOPS_I8 * 512) / $denom)
          unit: (IOPS + $normUnit)
        F16 OPs:
          avg: SUM(64 * SQ_INSTS_VALU_ADD_F16 + 64 * SQ_INSTS_VALU_MUL_F16 + 64 * SQ_INSTS_VALU_TRANS_F16 + 128 * SQ_INSTS_VALU_FMA_F16 + 512 * SQ_INSTS_VALU_MFMA_MOPS_F16) / SUM($denom)
          min: MIN((64 * SQ_INSTS_VALU_ADD_F16 + 64 * SQ_INSTS_VALU_MUL_F16 + 64 * SQ_INSTS_VALU_TRANS_F16 + 128 * SQ_INSTS_VALU_FMA_F16 + 512 * SQ_INSTS_VALU_MFMA_MOPS_F16) / $denom)
          max: MAX((64 * SQ_INSTS_VALU_ADD_F16 + 64 * SQ_INSTS_VALU_MUL_F16 + 64 * SQ_INSTS_VALU_TRANS_F16 + 128 * SQ_INSTS_VALU_FMA_F16 + 512 * SQ_INSTS_VALU_MFMA_MOPS_F16) / $denom)
          unit: (FLOPs + $normUnit)
        BF16 OPs:
          avg: 512 * SUM(SQ_INSTS_VALU_MFMA_MOPS_BF16) / SUM($denom)
          min: 512 * MIN(SQ_INSTS_VALU_MFMA_MOPS_BF16 / $denom)
          max: 512 * MAX(SQ_INSTS_VALU_MFMA_MOPS_BF16 / $denom)
          unit: (FLOPs + $normUnit)
        F32 OPs:
          avg: SUM(64 * (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + SQ_INSTS_VALU_TRANS_F32 + SQ_INSTS_VALU_FMA_F32 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F32) / SUM($denom)
          min: MIN((64 * (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + SQ_INSTS_VALU_TRANS_F32 + SQ_INSTS_VALU_FMA_F32 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F32) / $denom)
          max: MAX((64 * (SQ_INSTS_VALU_ADD_F32 + SQ_INSTS_VALU_MUL_F32 + SQ_INSTS_VALU_TRANS_F32 + SQ_INSTS_VALU_FMA_F32 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F32) / $denom)
          unit: (FLOPs + $normUnit)
        F64 OPs:
          avg: SUM(64 * (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + SQ_INSTS_VALU_TRANS_F64 + SQ_INSTS_VALU_FMA_F64 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F64) / SUM($denom)
          min: MIN((64 * (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + SQ_INSTS_VALU_TRANS_F64 + SQ_INSTS_VALU_FMA_F64 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F64) / $denom)
          max: MAX((64 * (SQ_INSTS_VALU_ADD_F64 + SQ_INSTS_VALU_MUL_F64 + SQ_INSTS_VALU_TRANS_F64 + SQ_INSTS_VALU_FMA_F64 * 2) + 512 * SQ_INSTS_VALU_MFMA_MOPS_F64) / $denom)
          unit: (FLOPs + $normUnit)
        INT8 OPs:
          avg: 512 * SUM(SQ_INSTS_VALU_MFMA_MOPS_I8) / SUM($denom)
          min: 512 * MIN(SQ_INSTS_VALU_MFMA_MOPS_I8 / $denom)
          max: 512 * MAX(SQ_INSTS_VALU_MFMA_MOPS_I8 / $denom)
          unit: (IOPS + $normUnit)
  metrics_description:
    VALU FLOPs: >-
      The total floating-point operations executed per second on the VALU.
      This is also presented as a percent of the peak theoretical FLOPs achievable
      on the specific accelerator. Note: this does not include any floating-point
      operations from MFMA instructions.
    MFMA FLOPs (BF16): >-
      The total number of 16-bit brain floating point MFMA operations executed
      per second. Note: this does not include any 16-bit brain floating point operations
      from VALU instructions. This is also presented as a percent of the peak theoretical
      BF16 MFMA operations achievable on the specific accelerator.
    MFMA FLOPs (F16): >-
      The total number of 16-bit floating point MFMA operations executed per
      second. Note: this does not include any 16-bit floating point operations from
      VALU instructions. This is also presented as a percent of the peak theoretical
      F16 MFMA operations achievable on the specific accelerator.
    MFMA FLOPs (F32): >-
      The total number of 32-bit floating point MFMA operations executed per
      second. Note: this does not include any 32-bit floating point operations from
      VALU instructions. This is also presented as a percent of the peak theoretical
      F32 MFMA operations achievable on the specific accelerator.
    MFMA FLOPs (F64): >-
      The total number of 64-bit floating point MFMA operations executed per
      second. Note: this does not include any 64-bit floating point operations from
      VALU instructions. This is also presented as a percent of the peak theoretical
      F64 MFMA operations achievable on the specific accelerator.
    MFMA IOPs (INT8): >-
      The total number of 8-bit integer MFMA operations executed per second.
      Note: this does not include any 8-bit integer operations from VALU instructions.
      This is also presented as a percent of the peak theoretical INT8 MFMA operations
      achievable on the specific accelerator.
    IPC: The ratio of the total number of instructions executed on the CU over the
      total active CU cycles.
    IPC (Issued): The ratio of the total number of (non-internal) instructions issued
      over the number of cycles where the scheduler was actively working on issuing
      instructions.
    SALU Utilization: Indicates what percent of the kernel's duration the SALU was
      busy executing instructions. Computed as the ratio of the total number of cycles
      spent by the scheduler issuing SALU / SMEM instructions over the total CU cycles.
    VALU Utilization: Indicates what percent of the kernel's duration the VALU was
      busy executing instructions. Does not include VMEM operations. Computed as the
      ratio of the total number of cycles spent by the scheduler issuing VALU instructions
      over the total CU cycles.
    VMEM Utilization: Indicates what percent of the kernel's duration the VMEM unit
      was busy executing instructions, including both global/generic and spill/scratch
      operations (see the VMEM instruction count metrics for more detail). Does not
      include VALU operations. Computed as the ratio of the total number of cycles
      spent by the scheduler issuing VMEM instructions over the total CU cycles.
    Branch Utilization: Indicates what percent of the kernel's duration the branch
      unit was busy executing instructions. Computed as the ratio of the total number
      of cycles spent by the scheduler issuing branch instructions over the total
      CU cycles.
    VALU Active Threads: Indicates the average level of divergence within a wavefront
      over the lifetime of the kernel. The number of work-items that were active in
      a wavefront during execution of each VALU instruction, time-averaged over all
      VALU instructions run on all wavefronts in the kernel
    MFMA Utilization: Indicates what percent of the kernel's duration the MFMA unit
      was busy executing instructions. Computed as the ratio of the total number of
      cycles spent by the MFMA was busy over the total CU cycles.
    MFMA Instruction Cycles: The average duration of MFMA instructions in this kernel
      in cycles. Computed as the ratio of the total number of cycles the MFMA unit
      was busy over the total number of MFMA instructions.
    VMEM Latency: The average number of round-trip cycles (that is, from issue to
      data return / acknowledgment) required for a VMEM instruction to complete.
    SMEM Latency: The average number of round-trip cycles (that is, from issue to
      data return / acknowledgment) required for a SMEM instruction to complete.
    FLOPs (Total): The total number of floating-point operations executed on either
      the VALU or MFMA units, per normalization unit.
    IOPs (Total): The total number of integer operations executed on either the VALU
      or MFMA units, per normalization unit.
    F16 OPs: The total number of 16-bit floating-point operations executed on either
      the VALU or MFMA units, per normalization unit.
    BF16 OPs: The total number of 16-bit brain floating-point operations executed
      on either the VALU or MFMA units, per normalization unit.
    F32 OPs: The total number of 32-bit floating-point operations executed on either
      the VALU or MFMA units, per normalization unit.
    F64 OPs: The total number of 64-bit floating-point operations executed on either
      the VALU or MFMA units, per normalization unit.
    INT8 OPs: The total number of 8-bit integer operations executed on either the
      VALU or MFMA units, per normalization unit.
