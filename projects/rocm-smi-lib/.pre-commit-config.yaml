# - How to use:
#     python3 -m pip install pre-commit
#     pre-commit install --install hooks
# Upon a new commit - the hooks should automagically run
#
# - How to skip:
#     git commit --no-verify
# or
#     SKIP=clang-format git commit
#     SKIP=ruff-format git commit
#     SKIP=gersemi git commit

exclude: |
  (?x)
    docs/
    | .*\.(md|rst|rtf)$
    | \.readthedocs\.yaml$
    | build/
    | build-.*/
    | _build/
    | third_party/
fail_fast: false
repos:
  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.2
    hooks:
      - id: codespell
        args: ["-w", "--config", "projects/rocm-smi-lib/.codespellrc"]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files

  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v22.1.0
    hooks:
      - id: clang-format
        files: '\.(c|cpp|cc|h|hpp|m|mm)$'

  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.4
    hooks:
      - id: ruff-format
        exclude: ^python_smi_tools/rsmiBindingsInit\.py$

  - repo: https://github.com/BlankSpruce/gersemi
    rev: 0.26.0
    hooks:
      - id: gersemi
