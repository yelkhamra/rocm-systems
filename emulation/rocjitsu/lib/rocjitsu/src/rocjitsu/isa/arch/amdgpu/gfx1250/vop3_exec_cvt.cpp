// Copyright (c) 2025-2026 Advanced Micro Devices, Inc.
// SPDX-License-Identifier: MIT
//
// AUTO-GENERATED by the amdisa codegen pipeline. DO NOT EDIT.
// See lib/python/amdisa/README.md for regeneration instructions.

#include "rocjitsu/isa/arch/amdgpu/gfx1250/vop3.h"
#include "rocjitsu/isa/arch/amdgpu/shared/dpp_sdwa_ops.h"
#include "rocjitsu/isa/arch/amdgpu/shared/execute_shared.h"
#include "rocjitsu/isa/arch/amdgpu/shared/simd_glue.h"
#include "rocjitsu/isa/arch/amdgpu/shared/transcendental.h"
#include "rocjitsu/vm/amdgpu/compute_unit.h"
#include "rocjitsu/vm/amdgpu/register_access.h"
#include "rocjitsu/vm/amdgpu/wavefront.h"
#include "util/data_types.h"
#include "util/except.h"
#include <algorithm>
#include <bit>
#include <cmath>
#include <limits>

namespace rocjitsu {
namespace gfx1250 {

const bool VCvtI32F64Vop3::execute_registered_ = register_exec_fn<VCvtI32F64Vop3>();

void VCvtI32F64Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_i32_f64_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF64I32Vop3::execute_registered_ = register_exec_fn<VCvtF64I32Vop3>();

void VCvtF64I32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f64_i32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32I32Vop3::execute_registered_ = register_exec_fn<VCvtF32I32Vop3>();

void VCvtF32I32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f32_i32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32U32Vop3::execute_registered_ = register_exec_fn<VCvtF32U32Vop3>();

void VCvtF32U32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f32_u32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtU32F32Vop3::execute_registered_ = register_exec_fn<VCvtU32F32Vop3>();

void VCvtU32F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_u32_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtI32F32Vop3::execute_registered_ = register_exec_fn<VCvtI32F32Vop3>();

void VCvtI32F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_i32_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF16F32Vop3::execute_registered_ = register_exec_fn<VCvtF16F32Vop3>();

void VCvtF16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half =
          static_cast<uint32_t>(static_cast<uint16_t>(util::f32_to_f16(std::bit_cast<float>(
              static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src0, lane))))));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32F16Vop3::execute_registered_ = register_exec_fn<VCvtF32F16Vop3>();

void VCvtF32F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (inst_.src0 != amdgpu::SRC_DPP && !amdgpu::dpp::is_src_dpp8(inst_.src0)) {
    auto &inst = *this;
    ROCJITSU_TRY_SIMD_CVT_F32_F16_VOP3_TRUE16();
  }
  uint64_t exec = wf.exec();
  const uint32_t opsel = ::rocjitsu::amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t raw = ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0);
    float src = util::f16_to_f32(static_cast<uint16_t>(raw));
    if (inst_.abs & (1u << 0))
      src = std::fabs(src);
    if (inst_.neg & (1u << 0))
      src = -src;
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane, std::bit_cast<uint32_t>(src));
  }

  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtNearestI32F32Vop3::execute_registered_ = register_exec_fn<VCvtNearestI32F32Vop3>();

void VCvtNearestI32F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_nearest_i32_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtFloorI32F32Vop3::execute_registered_ = register_exec_fn<VCvtFloorI32F32Vop3>();

void VCvtFloorI32F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_floor_i32_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtOffF32I4Vop3::execute_registered_ = register_exec_fn<VCvtOffF32I4Vop3>();

void VCvtOffF32I4Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_off_f32_i4_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32F64Vop3::execute_registered_ = register_exec_fn<VCvtF32F64Vop3>();

void VCvtF32F64Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f32_f64_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF64F32Vop3::execute_registered_ = register_exec_fn<VCvtF64F32Vop3>();

void VCvtF64F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f64_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32Ubyte0Vop3::execute_registered_ = register_exec_fn<VCvtF32Ubyte0Vop3>();

void VCvtF32Ubyte0Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f32_ubyte0_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32Ubyte1Vop3::execute_registered_ = register_exec_fn<VCvtF32Ubyte1Vop3>();

void VCvtF32Ubyte1Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f32_ubyte1_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32Ubyte2Vop3::execute_registered_ = register_exec_fn<VCvtF32Ubyte2Vop3>();

void VCvtF32Ubyte2Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f32_ubyte2_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32Ubyte3Vop3::execute_registered_ = register_exec_fn<VCvtF32Ubyte3Vop3>();

void VCvtF32Ubyte3Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f32_ubyte3_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtU32F64Vop3::execute_registered_ = register_exec_fn<VCvtU32F64Vop3>();

void VCvtU32F64Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_u32_f64_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF64U32Vop3::execute_registered_ = register_exec_fn<VCvtF64U32Vop3>();

void VCvtF64U32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_cvt_f64_u32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VFrexpExpI32F64Vop3::execute_registered_ = register_exec_fn<VFrexpExpI32F64Vop3>();

void VFrexpExpI32F64Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_frexp_exp_i32_f64_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VFrexpMantF64Vop3::execute_registered_ = register_exec_fn<VFrexpMantF64Vop3>();

void VFrexpMantF64Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_frexp_mant_f64_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VFrexpExpI32F32Vop3::execute_registered_ = register_exec_fn<VFrexpExpI32F32Vop3>();

void VFrexpExpI32F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_frexp_exp_i32_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VFrexpMantF32Vop3::execute_registered_ = register_exec_fn<VFrexpMantF32Vop3>();

void VFrexpMantF32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  amdgpu::execute_v_frexp_mant_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF16U16Vop3::execute_registered_ = register_exec_fn<VCvtF16U16Vop3>();

void VCvtF16U16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(
          static_cast<uint16_t>(util::f32_to_f16(static_cast<float>(static_cast<uint16_t>(
              ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0))))));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF16I16Vop3::execute_registered_ = register_exec_fn<VCvtF16I16Vop3>();

void VCvtF16I16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(
          static_cast<uint16_t>(util::f32_to_f16(static_cast<float>(static_cast<int16_t>(
              ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0) & 0xFFFF)))));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtU16F16Vop3::execute_registered_ = register_exec_fn<VCvtU16F16Vop3>();

void VCvtU16F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(static_cast<uint16_t>([&]() -> uint32_t {
        float s = util::f16_to_f32(static_cast<uint16_t>(
            ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0)));
        if (std::isnan(s) || s < 0.0f)
          return 0u;
        if (s >= 65536.0f)
          return static_cast<uint32_t>(UINT16_MAX);
        return static_cast<uint32_t>(static_cast<uint16_t>(s));
      }()));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtI16F16Vop3::execute_registered_ = register_exec_fn<VCvtI16F16Vop3>();

void VCvtI16F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(static_cast<uint16_t>([&]() -> uint32_t {
        float s = util::f16_to_f32(static_cast<uint16_t>(
            ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0)));
        if (std::isnan(s))
          return 0u;
        if (s >= 32768.0f)
          return static_cast<uint32_t>(static_cast<uint16_t>(INT16_MAX));
        if (s < -32768.0f)
          return static_cast<uint32_t>(static_cast<uint16_t>(INT16_MIN));
        return static_cast<uint32_t>(static_cast<uint16_t>(static_cast<int16_t>(s)));
      }()));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VFrexpMantF16Vop3::execute_registered_ = register_exec_fn<VFrexpMantF16Vop3>();

void VFrexpMantF16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (inst_.src0 != amdgpu::SRC_DPP && !amdgpu::dpp::is_src_dpp8(inst_.src0)) {
    auto &inst = *this;
    ROCJITSU_TRY_SIMD_VOP3_UNARY_TRUE16_FP16(
        [](auto a) { return util::frexp_mant_f32_simd(std::bit_cast<util::native<uint32_t>>(a)); });
  }
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(static_cast<uint16_t>(util::f32_to_f16([&]() {
        float v = [&]() {
          float v = [&]() {
            int e;
            return std::frexp(
                static_cast<float>([&]() {
                  float sv = util::f16_to_f32(static_cast<uint16_t>(
                      ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0)));
                  if (inst_.abs & (1u << 0))
                    sv = std::fabs(sv);
                  if (inst_.neg & (1u << 0))
                    sv = -sv;
                  return sv;
                }()),
                &e);
          }();
          if (inst_.omod == 1)
            v *= 2.0f;
          else if (inst_.omod == 2)
            v *= 4.0f;
          else if (inst_.omod == 3)
            v *= 0.5f;
          return v;
        }();
        if (inst_.clamp)
          v = std::clamp(v, 0.0f, 1.0f);
        return v;
      }())));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VFrexpExpI16F16Vop3::execute_registered_ = register_exec_fn<VFrexpExpI16F16Vop3>();

void VFrexpExpI16F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (inst_.src0 != amdgpu::SRC_DPP && !amdgpu::dpp::is_src_dpp8(inst_.src0)) {
    auto &inst = *this;
    ROCJITSU_TRY_SIMD_VOP3_UNARY_TRUE16_FP16([](auto a) {
      return util::stdx::static_simd_cast<util::native<float>>(
          util::frexp_exp_f32_simd(std::bit_cast<util::native<uint32_t>>(a)));
    });
  }
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(static_cast<uint16_t>(util::f32_to_f16([&]() {
        float v = [&]() {
          float v = [&]() {
            float s = [&]() {
              float sv = util::f16_to_f32(static_cast<uint16_t>(
                  ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0)));
              if (inst_.abs & (1u << 0))
                sv = std::fabs(sv);
              if (inst_.neg & (1u << 0))
                sv = -sv;
              return sv;
            }();
            int exp = 0;
            if (s != 0.0f && !std::isnan(s) && !std::isinf(s))
              std::frexp(s, &exp);
            return static_cast<uint32_t>(exp);
          }();
          if (inst_.omod == 1)
            v *= 2.0f;
          else if (inst_.omod == 2)
            v *= 4.0f;
          else if (inst_.omod == 3)
            v *= 0.5f;
          return v;
        }();
        if (inst_.clamp)
          v = std::clamp(v, 0.0f, 1.0f);
        return v;
      }())));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtNormI16F16Vop3::execute_registered_ = register_exec_fn<VCvtNormI16F16Vop3>();

void VCvtNormI16F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(static_cast<uint16_t>([&]() -> uint32_t {
        float s = util::f16_to_f32(static_cast<uint16_t>(
            ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0)));
        if (std::isnan(s))
          return 0u;
        float scaled = std::clamp(s * 32767.0f, -32768.0f, 32767.0f);
        return static_cast<uint32_t>(static_cast<uint16_t>(static_cast<int16_t>(scaled)));
      }()));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtNormU16F16Vop3::execute_registered_ = register_exec_fn<VCvtNormU16F16Vop3>();

void VCvtNormU16F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(static_cast<uint16_t>([&]() -> uint32_t {
        float s = util::f16_to_f32(static_cast<uint16_t>(
            ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0)));
        if (std::isnan(s))
          return 0u;
        float scaled = std::clamp(s * 65535.0f, 0.0f, 65535.0f);
        return static_cast<uint32_t>(static_cast<uint16_t>(scaled));
      }()));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtI32I16Vop3::execute_registered_ = register_exec_fn<VCvtI32I16Vop3>();

void VCvtI32I16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane,
        static_cast<uint32_t>(static_cast<int32_t>(static_cast<int16_t>(
            ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0) & 0xFFFF))));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtU32U16Vop3::execute_registered_ = register_exec_fn<VCvtU32U16Vop3>();

void VCvtU32U16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane, (::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0) & 0xFFFFu));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32Fp8Vop3::execute_registered_ = register_exec_fn<VCvtF32Fp8Vop3>();

void VCvtF32Fp8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane,
        std::bit_cast<uint32_t>((amdgpu::vop3_fp8_decode_e5m3(*this))
                                    ? util::fp8_e5m3_to_f32(static_cast<uint8_t>(
                                          ((amdgpu::RegisterAccess(wf).read_lane(src0, lane) >>
                                            ((((amdgpu::vop3_opsel(inst_) & 0x1u) << 1) |
                                              ((amdgpu::vop3_opsel(inst_) & 0x2u) >> 1)) *
                                             8u)) &
                                           0xFFu)))
                                    : util::fp8_e4m3_to_f32(static_cast<uint8_t>(
                                          ((amdgpu::RegisterAccess(wf).read_lane(src0, lane) >>
                                            ((((amdgpu::vop3_opsel(inst_) & 0x1u) << 1) |
                                              ((amdgpu::vop3_opsel(inst_) & 0x2u) >> 1)) *
                                             8u)) &
                                           0xFFu)))));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32Bf8Vop3::execute_registered_ = register_exec_fn<VCvtF32Bf8Vop3>();

void VCvtF32Bf8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane,
        std::bit_cast<uint32_t>(util::bf8_e5m2_to_f32(
            static_cast<uint8_t>(((amdgpu::RegisterAccess(wf).read_lane(src0, lane) >>
                                   ((((amdgpu::vop3_opsel(inst_) & 0x1u) << 1) |
                                     ((amdgpu::vop3_opsel(inst_) & 0x2u) >> 1)) *
                                    8u)) &
                                  0xFFu)))));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtPkF32Fp8Vop3::execute_registered_ = register_exec_fn<VCvtPkF32Fp8Vop3>();

void VCvtPkF32Fp8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t packed = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    bool src_hi = inst_.opsel & 1;
    uint32_t half = src_hi ? (packed >> 16) : (packed & 0xFFFFu);
    float lo = util::fp8_e4m3_to_f32(static_cast<uint8_t>(half & 0xFFu));
    float hi = util::fp8_e4m3_to_f32(static_cast<uint8_t>((half >> 8) & 0xFFu));
    uint32_t lo_bits = std::bit_cast<uint32_t>(lo);
    uint32_t hi_bits = std::bit_cast<uint32_t>(hi);
    amdgpu::RegisterAccess(wf).write_lane64(
        vdst, lane, static_cast<uint64_t>(lo_bits) | (static_cast<uint64_t>(hi_bits) << 32));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtPkF32Bf8Vop3::execute_registered_ = register_exec_fn<VCvtPkF32Bf8Vop3>();

void VCvtPkF32Bf8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t packed = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    bool src_hi = inst_.opsel & 1;
    uint32_t half = src_hi ? (packed >> 16) : (packed & 0xFFFFu);
    float lo = util::bf8_e5m2_to_f32(static_cast<uint8_t>(half & 0xFFu));
    float hi = util::bf8_e5m2_to_f32(static_cast<uint8_t>((half >> 8) & 0xFFu));
    uint32_t lo_bits = std::bit_cast<uint32_t>(lo);
    uint32_t hi_bits = std::bit_cast<uint32_t>(hi);
    amdgpu::RegisterAccess(wf).write_lane64(
        vdst, lane, static_cast<uint64_t>(lo_bits) | (static_cast<uint64_t>(hi_bits) << 32));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF32Bf16Vop3::execute_registered_ = register_exec_fn<VCvtF32Bf16Vop3>();

void VCvtF32Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane,
        std::bit_cast<uint32_t>(util::bf16_to_f32(static_cast<uint16_t>(
            ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0)))));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtPkF16Fp8Vop3::execute_registered_ = register_exec_fn<VCvtPkF16Fp8Vop3>();

void VCvtPkF16Fp8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t packed = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    bool src_hi = inst_.opsel & 1;
    uint32_t half = src_hi ? (packed >> 16) : (packed & 0xFFFFu);
    float lo = util::fp8_e4m3_to_f32(static_cast<uint8_t>(half & 0xFFu));
    float hi = util::fp8_e4m3_to_f32(static_cast<uint8_t>((half >> 8) & 0xFFu));
    uint32_t lo_bits = util::f32_to_f16(lo);
    uint32_t hi_bits = util::f32_to_f16(hi);
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane, lo_bits | (hi_bits << 16));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtPkF16Bf8Vop3::execute_registered_ = register_exec_fn<VCvtPkF16Bf8Vop3>();

void VCvtPkF16Bf8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t packed = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    bool src_hi = inst_.opsel & 1;
    uint32_t half = src_hi ? (packed >> 16) : (packed & 0xFFFFu);
    float lo = util::bf8_e5m2_to_f32(static_cast<uint8_t>(half & 0xFFu));
    float hi = util::bf8_e5m2_to_f32(static_cast<uint8_t>((half >> 8) & 0xFFu));
    uint32_t lo_bits = util::f32_to_f16(lo);
    uint32_t hi_bits = util::f32_to_f16(hi);
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane, lo_bits | (hi_bits << 16));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF16Fp8Vop3::execute_registered_ = register_exec_fn<VCvtF16Fp8Vop3>();

void VCvtF16Fp8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half =
          static_cast<uint32_t>(static_cast<uint16_t>(static_cast<uint32_t>(util::f32_to_f16(
              (amdgpu::vop3_fp8_decode_e5m3(*this))
                  ? util::fp8_e5m3_to_f32(static_cast<uint8_t>(
                        ((::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0) >>
                          (((opsel & 0x2u) >> 1) * 8u)) &
                         0xFFu)))
                  : util::fp8_e4m3_to_f32(static_cast<uint8_t>(
                        ((::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0) >>
                          (((opsel & 0x2u) >> 1) * 8u)) &
                         0xFFu)))))));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtF16Bf8Vop3::execute_registered_ = register_exec_fn<VCvtF16Bf8Vop3>();

void VCvtF16Bf8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  [[maybe_unused]] uint32_t opsel = amdgpu::vop3_opsel(inst_);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    {
      uint32_t src_half = static_cast<uint32_t>(static_cast<uint16_t>(
          static_cast<uint32_t>(util::f32_to_f16(util::bf8_e5m2_to_f32(static_cast<uint8_t>(
              ((::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, opsel, 0) >>
                (((opsel & 0x2u) >> 1) * 8u)) &
               0xFFu)))))));
      ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, opsel, src_half, true);
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtPkRtzF16F32Vop3::execute_registered_ = register_exec_fn<VCvtPkRtzF16F32Vop3>();

void VCvtPkRtzF16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  amdgpu::execute_v_cvt_pk_rtz_f16_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkU8F32Vop3::execute_registered_ = register_exec_fn<VCvtPkU8F32Vop3>();

void VCvtPkU8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  amdgpu::execute_v_cvt_pk_u8_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Fp4F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Fp4F32Vop3>();

void VCvtScalef32SrPk8Fp4F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[1] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xfu;
      uint32_t bit = index * 4u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 4u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp4_e2m1_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 1u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Fp8F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Fp8F32Vop3>();

void VCvtScalef32SrPk8Fp8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp8_e4m3_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Bf8F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Bf8F32Vop3>();

void VCvtScalef32SrPk8Bf8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf8_e5m2_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8F16Fp4Vop3::execute_registered_ = register_exec_fn<VCvtScalePk8F16Fp4Vop3>();

void VCvtScalePk8F16Fp4Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_payload = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = (src_payload >> (index * 4u)) & 0xfu;
      return util::fp4_e2m1_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[4] = {};
    for (uint32_t index = 0; index < 8u; ++index) {
      uint32_t bits = util::f32_to_f16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 4u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8Bf16Fp4Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk8Bf16Fp4Vop3>();

void VCvtScalePk8Bf16Fp4Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_payload = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = (src_payload >> (index * 4u)) & 0xfu;
      return util::fp4_e2m1_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[4] = {};
    for (uint32_t index = 0; index < 8u; ++index) {
      uint32_t bits = util::f32_to_bf16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 4u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8F32Fp4Vop3::execute_registered_ = register_exec_fn<VCvtScalePk8F32Fp4Vop3>();

void VCvtScalePk8F32Fp4Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_payload = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = (src_payload >> (index * 4u)) & 0xfu;
      return util::fp4_e2m1_to_f32(static_cast<uint8_t>(raw));
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_src(index) * scale;
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + index, lane,
                                                 std::bit_cast<uint32_t>(value));
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8F16Fp8Vop3::execute_registered_ = register_exec_fn<VCvtScalePk8F16Fp8Vop3>();

void VCvtScalePk8F16Fp8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint64_t src_payload = amdgpu::RegisterAccess(wf).read_lane64(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = static_cast<uint32_t>((src_payload >> (index * 8u)) & 0xffu);
      return util::fp8_e4m3_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[4] = {};
    for (uint32_t index = 0; index < 8u; ++index) {
      uint32_t bits = util::f32_to_f16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 4u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8Bf16Fp8Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk8Bf16Fp8Vop3>();

void VCvtScalePk8Bf16Fp8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint64_t src_payload = amdgpu::RegisterAccess(wf).read_lane64(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = static_cast<uint32_t>((src_payload >> (index * 8u)) & 0xffu);
      return util::fp8_e4m3_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[4] = {};
    for (uint32_t index = 0; index < 8u; ++index) {
      uint32_t bits = util::f32_to_bf16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 4u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8F32Fp8Vop3::execute_registered_ = register_exec_fn<VCvtScalePk8F32Fp8Vop3>();

void VCvtScalePk8F32Fp8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint64_t src_payload = amdgpu::RegisterAccess(wf).read_lane64(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = static_cast<uint32_t>((src_payload >> (index * 8u)) & 0xffu);
      return util::fp8_e4m3_to_f32(static_cast<uint8_t>(raw));
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_src(index) * scale;
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + index, lane,
                                                 std::bit_cast<uint32_t>(value));
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8F16Bf8Vop3::execute_registered_ = register_exec_fn<VCvtScalePk8F16Bf8Vop3>();

void VCvtScalePk8F16Bf8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint64_t src_payload = amdgpu::RegisterAccess(wf).read_lane64(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = static_cast<uint32_t>((src_payload >> (index * 8u)) & 0xffu);
      return util::bf8_e5m2_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[4] = {};
    for (uint32_t index = 0; index < 8u; ++index) {
      uint32_t bits = util::f32_to_f16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 4u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8Bf16Bf8Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk8Bf16Bf8Vop3>();

void VCvtScalePk8Bf16Bf8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint64_t src_payload = amdgpu::RegisterAccess(wf).read_lane64(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = static_cast<uint32_t>((src_payload >> (index * 8u)) & 0xffu);
      return util::bf8_e5m2_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[4] = {};
    for (uint32_t index = 0; index < 8u; ++index) {
      uint32_t bits = util::f32_to_bf16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 4u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk8F32Bf8Vop3::execute_registered_ = register_exec_fn<VCvtScalePk8F32Bf8Vop3>();

void VCvtScalePk8F32Bf8Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint64_t src_payload = amdgpu::RegisterAccess(wf).read_lane64(src0, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t raw = static_cast<uint32_t>((src_payload >> (index * 8u)) & 0xffu);
      return util::bf8_e5m2_to_f32(static_cast<uint8_t>(raw));
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_src(index) * scale;
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + index, lane,
                                                 std::bit_cast<uint32_t>(value));
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Fp4F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Fp4F32Vop3>();

void VCvtScalef32Pk8Fp4F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[1] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xfu;
      uint32_t bit = index * 4u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 4u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp4_e2m1_rne(value));
    }
    for (uint32_t word = 0; word < 1u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Fp4F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Fp4F16Vop3>();

void VCvtScalef32Pk8Fp4F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[1] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xfu;
      uint32_t bit = index * 4u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 4u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp4_e2m1_rne(value));
    }
    for (uint32_t word = 0; word < 1u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Fp8Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Fp8Bf16Vop3>();

void VCvtScalef32Pk8Fp8Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp8_e4m3_rne(value));
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Bf8Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Bf8Bf16Vop3>();

void VCvtScalef32Pk8Bf8Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf8_e5m2_rne(value));
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Fp4Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Fp4Bf16Vop3>();

void VCvtScalef32Pk8Fp4Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[1] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xfu;
      uint32_t bit = index * 4u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 4u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp4_e2m1_rne(value));
    }
    for (uint32_t word = 0; word < 1u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Fp4F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Fp4F16Vop3>();

void VCvtScalef32SrPk8Fp4F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[1] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xfu;
      uint32_t bit = index * 4u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 4u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp4_e2m1_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 1u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Fp4Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Fp4Bf16Vop3>();

void VCvtScalef32SrPk8Fp4Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[1] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xfu;
      uint32_t bit = index * 4u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 4u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp4_e2m1_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 1u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Fp8F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Fp8F16Vop3>();

void VCvtScalef32SrPk8Fp8F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp8_e4m3_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Fp8Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Fp8Bf16Vop3>();

void VCvtScalef32SrPk8Fp8Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp8_e4m3_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Bf8F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Bf8F16Vop3>();

void VCvtScalef32SrPk8Bf8F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf8_e5m2_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk8Bf8Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk8Bf8Bf16Vop3>();

void VCvtScalef32SrPk8Bf8Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf8_e5m2_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Fp8F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Fp8F32Vop3>();

void VCvtScalef32Pk8Fp8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp8_e4m3_rne(value));
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Fp8F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Fp8F16Vop3>();

void VCvtScalef32Pk8Fp8F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp8_e4m3_rne(value));
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Bf8F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Bf8F32Vop3>();

void VCvtScalef32Pk8Bf8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf8_e5m2_rne(value));
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk8Bf8F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk8Bf8F16Vop3>();

void VCvtScalef32Pk8Bf8F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint64_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane64(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 4u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[2] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0xffu;
      uint32_t bit = index * 8u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 8u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 8u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf8_e5m2_rne(value));
    }
    for (uint32_t word = 0; word < 2u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane64(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk16F16Fp6Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk16F16Fp6Vop3>();

void VCvtScalePk16F16Fp6Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 3u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      uint32_t raw = src_words[word] >> shift;
      if (shift > 26u)
        raw |= src_words[word + 1u] << (32u - shift);
      raw &= 0x3fu;
      return util::fp6_e2m3_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[8] = {};
    for (uint32_t index = 0; index < 16u; ++index) {
      uint32_t bits = util::f32_to_f16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 8u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk16Bf16Fp6Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk16Bf16Fp6Vop3>();

void VCvtScalePk16Bf16Fp6Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 3u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      uint32_t raw = src_words[word] >> shift;
      if (shift > 26u)
        raw |= src_words[word + 1u] << (32u - shift);
      raw &= 0x3fu;
      return util::fp6_e2m3_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[8] = {};
    for (uint32_t index = 0; index < 16u; ++index) {
      uint32_t bits = util::f32_to_bf16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 8u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk16F32Fp6Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk16F32Fp6Vop3>();

void VCvtScalePk16F32Fp6Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 3u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      uint32_t raw = src_words[word] >> shift;
      if (shift > 26u)
        raw |= src_words[word + 1u] << (32u - shift);
      raw &= 0x3fu;
      return util::fp6_e2m3_to_f32(static_cast<uint8_t>(raw));
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_src(index) * scale;
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + index, lane,
                                                 std::bit_cast<uint32_t>(value));
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk16F16Bf6Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk16F16Bf6Vop3>();

void VCvtScalePk16F16Bf6Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 3u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      uint32_t raw = src_words[word] >> shift;
      if (shift > 26u)
        raw |= src_words[word + 1u] << (32u - shift);
      raw &= 0x3fu;
      return util::bf6_e3m2_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[8] = {};
    for (uint32_t index = 0; index < 16u; ++index) {
      uint32_t bits = util::f32_to_f16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 8u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk16Bf16Bf6Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk16Bf16Bf6Vop3>();

void VCvtScalePk16Bf16Bf6Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 3u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      uint32_t raw = src_words[word] >> shift;
      if (shift > 26u)
        raw |= src_words[word + 1u] << (32u - shift);
      raw &= 0x3fu;
      return util::bf6_e3m2_to_f32(static_cast<uint8_t>(raw));
    };
    uint32_t dst_words[8] = {};
    for (uint32_t index = 0; index < 16u; ++index) {
      uint32_t bits = util::f32_to_bf16(read_scaled_src(index) * scale);
      dst_words[index / 2u] |= bits << ((index & 1u) * 16u);
    }
    for (uint32_t word = 0; word < 8u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalePk16F32Bf6Vop3::execute_registered_ =
    register_exec_fn<VCvtScalePk16F32Bf6Vop3>();

void VCvtScalePk16F32Bf6Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    uint32_t scale_payload = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint32_t scale_byte = (scale_payload >> ((inst_.opsel & 0x3u) * 8u)) & 0xffu;
    float scale = util::e8m0_to_f32(static_cast<uint8_t>(scale_byte));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[4] = {};
    for (uint32_t word = 0; word < 3u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_src = [&](uint32_t index) -> float {
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      uint32_t raw = src_words[word] >> shift;
      if (shift > 26u)
        raw |= src_words[word + 1u] << (32u - shift);
      raw &= 0x3fu;
      return util::bf6_e3m2_to_f32(static_cast<uint8_t>(raw));
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_src(index) * scale;
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + index, lane,
                                                 std::bit_cast<uint32_t>(value));
    }
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk16Fp6F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk16Fp6F32Vop3>();

void VCvtScalef32Pk16Fp6F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[16] = {};
    for (uint32_t word = 0; word < 16u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp6_e2m3_rne(value));
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk16Bf6F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk16Bf6F32Vop3>();

void VCvtScalef32Pk16Bf6F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[16] = {};
    for (uint32_t word = 0; word < 16u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf6_e3m2_rne(value));
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk16Fp6F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk16Fp6F16Vop3>();

void VCvtScalef32Pk16Fp6F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp6_e2m3_rne(value));
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk16Bf6F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk16Bf6F16Vop3>();

void VCvtScalef32Pk16Bf6F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf6_e3m2_rne(value));
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk16Fp6Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk16Fp6Bf16Vop3>();

void VCvtScalef32Pk16Fp6Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp6_e2m3_rne(value));
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32Pk16Bf6Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32Pk16Bf6Bf16Vop3>();

void VCvtScalef32Pk16Bf6Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf6_e3m2_rne(value));
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk16Fp6F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk16Fp6F32Vop3>();

void VCvtScalef32SrPk16Fp6F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[16] = {};
    for (uint32_t word = 0; word < 16u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp6_e2m3_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk16Bf6F32Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk16Bf6F32Vop3>();

void VCvtScalef32SrPk16Bf6F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[16] = {};
    for (uint32_t word = 0; word < 16u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      return std::bit_cast<float>(src_words[index]);
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf6_e3m2_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk16Fp6F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk16Fp6F16Vop3>();

void VCvtScalef32SrPk16Fp6F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp6_e2m3_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk16Bf6F16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk16Bf6F16Vop3>();

void VCvtScalef32SrPk16Bf6F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::f16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf6_e3m2_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk16Fp6Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk16Fp6Bf16Vop3>();

void VCvtScalef32SrPk16Fp6Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_fp6_e2m3_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtScalef32SrPk16Bf6Bf16Vop3::execute_registered_ =
    register_exec_fn<VCvtScalef32SrPk16Bf6Bf16Vop3>();

void VCvtScalef32SrPk16Bf6Bf16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t dst_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
    float scale = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src2, lane));
    uint32_t seed = static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t src_base =
        wf.vgpr_alloc().base +
        *Isa::resolved_vgpr_offset(wf, src0.opr_type_, src0.encoding_value_, src0.vgpr_msb_role());
    uint32_t src_words[8] = {};
    for (uint32_t word = 0; word < 8u; ++word)
      src_words[word] = amdgpu::RegisterAccess(wf.cu()).read_vgpr(src_base + word, lane);
    auto read_scaled_input = [&](uint32_t index) -> float {
      uint32_t raw = src_words[index / 2u];
      return util::bf16_to_f32(static_cast<uint16_t>(raw >> ((index & 1u) * 16u)));
    };
    uint32_t dst_words[3] = {};
    auto pack_scaled_dst = [&](uint32_t index, uint32_t code) {
      code &= 0x3fu;
      uint32_t bit = index * 6u;
      uint32_t word = bit / 32u;
      uint32_t shift = bit & 31u;
      dst_words[word] |= code << shift;
      if (shift + 6u > 32u)
        dst_words[word + 1u] |= code >> (32u - shift);
    };
    for (uint32_t index = 0; index < 16u; ++index) {
      float value = read_scaled_input(index) / scale;
      pack_scaled_dst(index, util::f32_to_bf6_e3m2_sr(value, seed));
      seed = util::prng_advance(seed);
    }
    for (uint32_t word = 0; word < 3u; ++word)
      amdgpu::RegisterAccess(wf.cu()).write_vgpr(dst_base + word, lane, dst_words[word]);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkI16F32Vop3::execute_registered_ = register_exec_fn<VCvtPkI16F32Vop3>();

void VCvtPkI16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float f0 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src0, lane));
    float f1 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    int16_t lo = static_cast<int16_t>(std::clamp(f0, -32768.0f, 32767.0f));
    int16_t hi = static_cast<int16_t>(std::clamp(f1, -32768.0f, 32767.0f));
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane,
                                          (static_cast<uint32_t>(static_cast<uint16_t>(hi)) << 16) |
                                              static_cast<uint32_t>(static_cast<uint16_t>(lo)));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkU16F32Vop3::execute_registered_ = register_exec_fn<VCvtPkU16F32Vop3>();

void VCvtPkU16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float f0 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src0, lane));
    float f1 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint16_t lo = static_cast<uint16_t>(std::clamp(f0, 0.0f, 65535.0f));
    uint16_t hi = static_cast<uint16_t>(std::clamp(f1, 0.0f, 65535.0f));
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane,
                                          (static_cast<uint32_t>(static_cast<uint16_t>(hi)) << 16) |
                                              static_cast<uint32_t>(static_cast<uint16_t>(lo)));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VPackB32F16Vop3::execute_registered_ = register_exec_fn<VPackB32F16Vop3>();

void VPackB32F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  if (inst_.src0 != amdgpu::SRC_DPP && !amdgpu::dpp::is_src_dpp8(inst_.src0)) {
    auto &inst = *this;
    ROCJITSU_TRY_SIMD_VOP3_BINARY_TRUE16_SRC(
        uint32_t, [](auto a, auto b) { return (a & 0xFFFFu) | ((b & 0xFFFFu) << 16); });
  }
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t s0 = ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, inst_.opsel, 0);
    uint32_t s1 = ::rocjitsu::amdgpu::read_vop3_true16_src(src1, wf, lane, inst_.opsel, 1);
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane, s0 | (s1 << 16));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkNormI16F16Vop3::execute_registered_ = register_exec_fn<VCvtPkNormI16F16Vop3>();

void VCvtPkNormI16F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  if (inst_.src0 != amdgpu::SRC_DPP && !amdgpu::dpp::is_src_dpp8(inst_.src0)) {
    auto &inst = *this;
    ROCJITSU_TRY_SIMD_VOP3_BINARY_TRUE16_SRC(uint32_t, [](auto a, auto b) {
      using U = util::native<uint32_t>;
      auto lo = util::cvt_pknorm_i16_f32_simd(util::f16_to_f32_simd(a));
      auto hi = util::cvt_pknorm_i16_f32_simd(util::f16_to_f32_simd(b));
      return ((util::stdx::static_simd_cast<U>(hi) & 0xFFFFu) << 16) |
             (util::stdx::static_simd_cast<U>(lo) & 0xFFFFu);
    });
  }
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = util::f16_to_f32(static_cast<uint16_t>(
        ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, inst_.opsel, 0)));
    float s1 = util::f16_to_f32(static_cast<uint16_t>(
        ::rocjitsu::amdgpu::read_vop3_true16_src(src1, wf, lane, inst_.opsel, 1)));
    auto cvt_i16 = [](float f) -> int16_t {
      if (std::isnan(f))
        return 0;
      return static_cast<int16_t>(std::clamp(f * 32767.0f, -32768.0f, 32767.0f));
    };
    int16_t lo = cvt_i16(s0);
    int16_t hi = cvt_i16(s1);
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane, (static_cast<uint32_t>(hi) << 16) | (static_cast<uint32_t>(lo) & 0xFFFF));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkNormU16F16Vop3::execute_registered_ = register_exec_fn<VCvtPkNormU16F16Vop3>();

void VCvtPkNormU16F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  if (inst_.src0 != amdgpu::SRC_DPP && !amdgpu::dpp::is_src_dpp8(inst_.src0)) {
    auto &inst = *this;
    ROCJITSU_TRY_SIMD_VOP3_BINARY_TRUE16_SRC(uint32_t, [](auto a, auto b) {
      auto lo = util::cvt_pknorm_u16_f32_simd(util::f16_to_f32_simd(a));
      auto hi = util::cvt_pknorm_u16_f32_simd(util::f16_to_f32_simd(b));
      return ((hi & 0xFFFFu) << 16) | (lo & 0xFFFFu);
    });
  }
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = util::f16_to_f32(static_cast<uint16_t>(
        ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, inst_.opsel, 0)));
    float s1 = util::f16_to_f32(static_cast<uint16_t>(
        ::rocjitsu::amdgpu::read_vop3_true16_src(src1, wf, lane, inst_.opsel, 1)));
    auto cvt_u16 = [](float f) -> uint16_t {
      if (std::isnan(f))
        return 0;
      return static_cast<uint16_t>(std::clamp(f * 65535.0f, 0.0f, 65535.0f));
    };
    uint16_t lo = cvt_u16(s0);
    uint16_t hi = cvt_u16(s1);
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane, (static_cast<uint32_t>(hi) << 16) | (static_cast<uint32_t>(lo) & 0xFFFF));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkNormI16F32Vop3::execute_registered_ = register_exec_fn<VCvtPkNormI16F32Vop3>();

void VCvtPkNormI16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  amdgpu::execute_v_cvt_pk_norm_i16_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkNormU16F32Vop3::execute_registered_ = register_exec_fn<VCvtPkNormU16F32Vop3>();

void VCvtPkNormU16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  amdgpu::execute_v_cvt_pk_norm_u16_f32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkU16U32Vop3::execute_registered_ = register_exec_fn<VCvtPkU16U32Vop3>();

void VCvtPkU16U32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  amdgpu::execute_v_cvt_pk_u16_u32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkI16I32Vop3::execute_registered_ = register_exec_fn<VCvtPkI16I32Vop3>();

void VCvtPkI16I32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  amdgpu::execute_v_cvt_pk_i16_i32_vop3(*this, wf);
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkFp8F32Vop3::execute_registered_ = register_exec_fn<VCvtPkFp8F32Vop3>();

void VCvtPkFp8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src0, lane));
    float s1 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t lo = (inst_.clamp) ? util::f32_to_fp8_e5m3_rne(s0) : util::f32_to_fp8_e4m3_rne(s0);
    uint32_t hi = (inst_.clamp) ? util::f32_to_fp8_e5m3_rne(s1) : util::f32_to_fp8_e4m3_rne(s1);
    uint32_t packed = static_cast<uint32_t>(lo) | (static_cast<uint32_t>(hi) << 8);
    ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, (inst_.opsel) & 0x8u, packed, true);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkBf8F32Vop3::execute_registered_ = register_exec_fn<VCvtPkBf8F32Vop3>();

void VCvtPkBf8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src0, lane));
    float s1 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t lo = util::f32_to_bf8_e5m2_rne(s0);
    uint32_t hi = util::f32_to_bf8_e5m2_rne(s1);
    uint32_t packed = static_cast<uint32_t>(lo) | (static_cast<uint32_t>(hi) << 8);
    ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, (inst_.opsel) & 0x8u, packed, true);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtSrFp8F32Vop3::execute_registered_ = register_exec_fn<VCvtSrFp8F32Vop3>();

void VCvtSrFp8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = std::bit_cast<float>(
        static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src0, lane)));
    uint32_t seed = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint8_t result =
        (inst_.clamp) ? util::f32_to_fp8_e5m3_sr(s0, seed) : util::f32_to_fp8_e4m3_sr(s0, seed);
    uint32_t dst_byte = (inst_.opsel >> 2) & 0x3;
    uint32_t old = amdgpu::RegisterAccess(wf).read_lane(vdst, lane);
    uint32_t mask = ~(0xFFu << (dst_byte * 8));
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane, (old & mask) | (static_cast<uint32_t>(result) << (dst_byte * 8)));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtSrBf8F32Vop3::execute_registered_ = register_exec_fn<VCvtSrBf8F32Vop3>();

void VCvtSrBf8F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = std::bit_cast<float>(
        static_cast<uint32_t>(amdgpu::RegisterAccess(wf).read_lane(src0, lane)));
    uint32_t seed = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint8_t result = util::f32_to_bf8_e5m2_sr(s0, seed);
    uint32_t dst_byte = (inst_.opsel >> 2) & 0x3;
    uint32_t old = amdgpu::RegisterAccess(wf).read_lane(vdst, lane);
    uint32_t mask = ~(0xFFu << (dst_byte * 8));
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane, (old & mask) | (static_cast<uint32_t>(result) << (dst_byte * 8)));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkBf16F32Vop3::execute_registered_ = register_exec_fn<VCvtPkBf16F32Vop3>();

void VCvtPkBf16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src0, lane));
    float s1 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t lo = util::f32_to_bf16_rne(s0);
    uint32_t hi = util::f32_to_bf16_rne(s1);
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane, lo | (hi << 16));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtSrPkBf16F32Vop3::execute_registered_ = register_exec_fn<VCvtSrPkBf16F32Vop3>();

void VCvtSrPkBf16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src0, lane));
    float s1 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t seed_lo = amdgpu::RegisterAccess(wf).read_lane(src2, lane);
    uint32_t seed_hi = util::prng_advance(seed_lo);
    uint32_t lo = util::f32_to_bf16_sr(s0, seed_lo);
    uint32_t hi = util::f32_to_bf16_sr(s1, seed_hi);
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane, lo | (hi << 16));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkF16F32Vop3::execute_registered_ = register_exec_fn<VCvtPkF16F32Vop3>();

void VCvtPkF16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src0, lane));
    float s1 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t lo = util::f32_to_f16(s0);
    uint32_t hi = util::f32_to_f16(s1);
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane, lo | (hi << 16));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtSrPkF16F32Vop3::execute_registered_ = register_exec_fn<VCvtSrPkF16F32Vop3>();

void VCvtSrPkF16F32Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    float s0 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src0, lane));
    float s1 = std::bit_cast<float>(amdgpu::RegisterAccess(wf).read_lane(src1, lane));
    uint32_t seed_lo = amdgpu::RegisterAccess(wf).read_lane(src2, lane);
    uint32_t seed_hi = util::prng_advance(seed_lo);
    uint32_t lo = util::f32_to_f16_sr(s0, seed_lo);
    uint32_t hi = util::f32_to_f16_sr(s1, seed_hi);
    amdgpu::RegisterAccess(wf).write_lane(vdst, lane, lo | (hi << 16));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtPkFp8F16Vop3::execute_registered_ = register_exec_fn<VCvtPkFp8F16Vop3>();

void VCvtPkFp8F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t raw = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    float s0 = util::f16_to_f32(static_cast<uint16_t>(raw));
    float s1 = util::f16_to_f32(static_cast<uint16_t>(raw >> 16));
    uint32_t lo = (inst_.clamp) ? util::f32_to_fp8_e5m3_rne(s0) : util::f32_to_fp8_e4m3_rne(s0);
    uint32_t hi = (inst_.clamp) ? util::f32_to_fp8_e5m3_rne(s1) : util::f32_to_fp8_e4m3_rne(s1);
    uint32_t packed = static_cast<uint32_t>(lo) | (static_cast<uint32_t>(hi) << 8);
    ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, (inst_.opsel) & 0x8u, packed, true);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtPkBf8F16Vop3::execute_registered_ = register_exec_fn<VCvtPkBf8F16Vop3>();

void VCvtPkBf8F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t raw = amdgpu::RegisterAccess(wf).read_lane(src0, lane);
    float s0 = util::f16_to_f32(static_cast<uint16_t>(raw));
    float s1 = util::f16_to_f32(static_cast<uint16_t>(raw >> 16));
    uint32_t lo = util::f32_to_bf8_e5m2_rne(s0);
    uint32_t hi = util::f32_to_bf8_e5m2_rne(s1);
    uint32_t packed = static_cast<uint32_t>(lo) | (static_cast<uint32_t>(hi) << 8);
    ::rocjitsu::amdgpu::write_vop3_true16_dst(vdst, wf, lane, (inst_.opsel) & 0x8u, packed, true);
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
}

const bool VCvtSrFp8F16Vop3::execute_registered_ = register_exec_fn<VCvtSrFp8F16Vop3>();

void VCvtSrFp8F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t raw = ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, inst_.opsel, 0);
    float s0 = util::f16_to_f32(static_cast<uint16_t>(raw));
    uint32_t seed = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint8_t result =
        (inst_.clamp) ? util::f32_to_fp8_e5m3_sr(s0, seed) : util::f32_to_fp8_e4m3_sr(s0, seed);
    uint32_t dst_byte = (inst_.opsel >> 2) & 0x3;
    uint32_t old = amdgpu::RegisterAccess(wf).read_lane(vdst, lane);
    uint32_t mask = ~(0xFFu << (dst_byte * 8));
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane, (old & mask) | (static_cast<uint32_t>(result) << (dst_byte * 8)));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

const bool VCvtSrBf8F16Vop3::execute_registered_ = register_exec_fn<VCvtSrBf8F16Vop3>();

void VCvtSrBf8F16Vop3::execute_impl(amdgpu::Wavefront &wf) {
  uint32_t sdwa_old_dst_[64] = {};
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t ex = wf.exec();
    for (uint32_t ln = 0; ln < wf.wf_size(); ++ln)
      if (ex & (1ULL << ln))
        sdwa_old_dst_[ln] = amdgpu::RegisterAccess(wf).read_lane(vdst, ln);
  }
  if (inst_.src0 == amdgpu::SRC_DPP)
    amdgpu::dpp::apply_dpp(src_operands_[0], dpp_ctrl_, dpp_row_mask_, dpp_bank_mask_,
                           dpp_bound_ctrl_, dpp_fi_, dpp_src0_, wf);
  if (amdgpu::dpp::is_src_dpp8(inst_.src0))
    amdgpu::dpp::apply_dpp8(src_operands_[0], dpp8_lane_sel_, dpp_fi_, dpp_src0_, wf);
  if (dpp_src0_)
    src0.set_delegate(dpp_src0_.get());
  if (dpp_src1_)
    src1.set_delegate(dpp_src1_.get());
  uint64_t exec = wf.exec();
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t raw = ::rocjitsu::amdgpu::read_vop3_true16_src(src0, wf, lane, inst_.opsel, 0);
    float s0 = util::f16_to_f32(static_cast<uint16_t>(raw));
    uint32_t seed = amdgpu::RegisterAccess(wf).read_lane(src1, lane);
    uint8_t result = util::f32_to_bf8_e5m2_sr(s0, seed);
    uint32_t dst_byte = (inst_.opsel >> 2) & 0x3;
    uint32_t old = amdgpu::RegisterAccess(wf).read_lane(vdst, lane);
    uint32_t mask = ~(0xFFu << (dst_byte * 8));
    amdgpu::RegisterAccess(wf).write_lane(
        vdst, lane, (old & mask) | (static_cast<uint32_t>(result) << (dst_byte * 8)));
  }
  if (inst_.src0 == amdgpu::SRC_DPP) {
    uint64_t dpp_write_mask = amdgpu::dpp::dpp_write_mask(wf.wf_size(), dpp_ctrl_, dpp_row_mask_,
                                                          dpp_bank_mask_, dpp_bound_ctrl_);
    if (dpp_write_mask != ~0ULL) {
      uint64_t ex = wf.exec();
      for (uint32_t ln = 0; ln < wf.wf_size(); ++ln) {
        if ((ex & (1ULL << ln)) && !(dpp_write_mask & (1ULL << ln)))
          amdgpu::RegisterAccess(wf).write_lane(vdst, ln, sdwa_old_dst_[ln]);
      }
    }
  }
  src0.clear_delegate();
  src1.clear_delegate();
}

} // namespace gfx1250
} // namespace rocjitsu
