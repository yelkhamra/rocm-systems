// Copyright (c) 2025-2026 Advanced Micro Devices, Inc.
// SPDX-License-Identifier: MIT
//
// AUTO-GENERATED by the amdisa codegen pipeline. DO NOT EDIT.
// See lib/python/amdisa/README.md for regeneration instructions.

#include "rocjitsu/isa/arch/amdgpu/gfx1250/addr_calc.h"
#include "rocjitsu/isa/arch/amdgpu/gfx1250/vglobal.h"
#include "rocjitsu/isa/arch/amdgpu/shared/gfx12_cache_flags.h"
#include "rocjitsu/vm/amdgpu/compute_unit.h"
#include "rocjitsu/vm/amdgpu/mem_state.h"
#include "rocjitsu/vm/amdgpu/register_access.h"
#include "rocjitsu/vm/amdgpu/wavefront.h"
#include "util/data_types.h"
#include "util/except.h"
#include <algorithm>
#include <bit>
#include <cmath>
#include <cstring>
#include <limits>
#include <memory>

namespace rocjitsu {
namespace gfx1250 {

const bool GlobalLoadU8Vglobal::execute_registered_ = register_exec_fn<GlobalLoadU8Vglobal>();

void GlobalLoadU8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadI8Vglobal::execute_registered_ = register_exec_fn<GlobalLoadI8Vglobal>();

void GlobalLoadI8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->sign_extend = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadU16Vglobal::execute_registered_ = register_exec_fn<GlobalLoadU16Vglobal>();

void GlobalLoadU16Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 2;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadI16Vglobal::execute_registered_ = register_exec_fn<GlobalLoadI16Vglobal>();

void GlobalLoadI16Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 2;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->sign_extend = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadB32Vglobal::execute_registered_ = register_exec_fn<GlobalLoadB32Vglobal>();

void GlobalLoadB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadB64Vglobal::execute_registered_ = register_exec_fn<GlobalLoadB64Vglobal>();

void GlobalLoadB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadB96Vglobal::execute_registered_ = register_exec_fn<GlobalLoadB96Vglobal>();

void GlobalLoadB96Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 3;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadB128Vglobal::execute_registered_ = register_exec_fn<GlobalLoadB128Vglobal>();

void GlobalLoadB128Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 4;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalStoreB8Vglobal::execute_registered_ = register_exec_fn<GlobalStoreB8Vglobal>();

void GlobalStoreB8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 1);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base, lane);
    d->store_data[lane * 1 + 0] = static_cast<uint8_t>(val0);
  }
  set_data(std::move(d));
}

const bool GlobalStoreB16Vglobal::execute_registered_ = register_exec_fn<GlobalStoreB16Vglobal>();

void GlobalStoreB16Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 2;
  d->num_elems = 1;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 2);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base, lane);
    std::memcpy(&d->store_data[lane * 2 + 0], &val0, 2);
  }
  set_data(std::move(d));
}

const bool GlobalStoreB32Vglobal::execute_registered_ = register_exec_fn<GlobalStoreB32Vglobal>();

void GlobalStoreB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalStoreB64Vglobal::execute_registered_ = register_exec_fn<GlobalStoreB64Vglobal>();

void GlobalStoreB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalStoreB96Vglobal::execute_registered_ = register_exec_fn<GlobalStoreB96Vglobal>();

void GlobalStoreB96Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 3;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 12);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 12 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 12 + 4], &val1, 4);
    uint32_t val2 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 2, lane);
    std::memcpy(&d->store_data[lane * 12 + 8], &val2, 4);
  }
  set_data(std::move(d));
}

const bool GlobalStoreB128Vglobal::execute_registered_ = register_exec_fn<GlobalStoreB128Vglobal>();

void GlobalStoreB128Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 4;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 16);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 16 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 16 + 4], &val1, 4);
    uint32_t val2 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 2, lane);
    std::memcpy(&d->store_data[lane * 16 + 8], &val2, 4);
    uint32_t val3 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 3, lane);
    std::memcpy(&d->store_data[lane * 16 + 12], &val3, 4);
  }
  set_data(std::move(d));
}

const bool GlobalLoadD16U8Vglobal::execute_registered_ = register_exec_fn<GlobalLoadD16U8Vglobal>();

void GlobalLoadD16U8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->d16_lo = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadD16I8Vglobal::execute_registered_ = register_exec_fn<GlobalLoadD16I8Vglobal>();

void GlobalLoadD16I8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->sign_extend = true;
  d->d16_lo = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadD16B16Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadD16B16Vglobal>();

void GlobalLoadD16B16Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 2;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->d16_lo = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadD16HiU8Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadD16HiU8Vglobal>();

void GlobalLoadD16HiU8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->d16_hi = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadD16HiI8Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadD16HiI8Vglobal>();

void GlobalLoadD16HiI8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->sign_extend = true;
  d->d16_hi = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadD16HiB16Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadD16HiB16Vglobal>();

void GlobalLoadD16HiB16Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 2;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->d16_hi = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalStoreD16HiB8Vglobal::execute_registered_ =
    register_exec_fn<GlobalStoreD16HiB8Vglobal>();

void GlobalStoreD16HiB8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 1);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base, lane);
    val0 >>= 16;
    d->store_data[lane * 1 + 0] = static_cast<uint8_t>(val0);
  }
  set_data(std::move(d));
}

const bool GlobalStoreD16HiB16Vglobal::execute_registered_ =
    register_exec_fn<GlobalStoreD16HiB16Vglobal>();

void GlobalStoreD16HiB16Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 2;
  d->num_elems = 1;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 2);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base, lane);
    val0 >>= 16;
    std::memcpy(&d->store_data[lane * 2 + 0], &val0, 2);
  }
  set_data(std::move(d));
}

const bool GlobalLoadAddtidB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadAddtidB32Vglobal>();

void GlobalLoadAddtidB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  {
    uint64_t exec = wf.exec();
    d->lane_mask = exec;
    d->exec_mask = exec;
    d->wf_size = wf.wf_size();
    d->wg_id = wf.wg_id();
    d->wf_id = wf.wf_id();
    d->cu_path = wf.cu().full_path();
    uint64_t base = amdgpu::RegisterAccess(wf).read_scalar64(saddr);
    int64_t offset = static_cast<int64_t>(static_cast<int32_t>(inst_.ioffset << 8) >> 8);
    for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
      if (!(exec & (1ULL << lane)))
        continue;
      d->per_lane_addr[lane] =
          base + static_cast<uint64_t>(offset + static_cast<int64_t>(lane * 4));
    }
  }
  set_data(std::move(d));
}

const bool GlobalStoreAddtidB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalStoreAddtidB32Vglobal>();

void GlobalStoreAddtidB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  {
    uint64_t exec = wf.exec();
    d->lane_mask = exec;
    d->exec_mask = exec;
    d->wf_size = wf.wf_size();
    d->wg_id = wf.wg_id();
    d->wf_id = wf.wf_id();
    d->cu_path = wf.cu().full_path();
    uint64_t base = amdgpu::RegisterAccess(wf).read_scalar64(saddr);
    int64_t offset = static_cast<int64_t>(static_cast<int32_t>(inst_.ioffset << 8) >> 8);
    for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
      if (!(exec & (1ULL << lane)))
        continue;
      d->per_lane_addr[lane] =
          base + static_cast<uint64_t>(offset + static_cast<int64_t>(lane * 4));
    }
  }
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base, lane);
    std::memcpy(&d->store_data[lane * 4], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalInvVglobal::execute_registered_ = register_exec_fn<GlobalInvVglobal>();

void GlobalInvVglobal::execute_impl(amdgpu::Wavefront &wf) {
  wf.cu().l1_vector().invalidate_all();
  if (auto *l2 = wf.cu().l2())
    l2->flush_all(wf.process_id());
}

const bool GlobalWbVglobal::execute_registered_ = register_exec_fn<GlobalWbVglobal>();

void GlobalWbVglobal::execute_impl(amdgpu::Wavefront &wf) { (void)wf; }

const bool GlobalAtomicSwapB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicSwapB32Vglobal>();

void GlobalAtomicSwapB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SWAP;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicCmpswapB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicCmpswapB32Vglobal>();

void GlobalAtomicCmpswapB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::CMPSWAP;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicAddU32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicAddU32Vglobal>();

void GlobalAtomicAddU32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::ADD;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicSubU32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicSubU32Vglobal>();

void GlobalAtomicSubU32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SUB;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicSubClampU32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicSubClampU32Vglobal>();

void GlobalAtomicSubClampU32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SUB;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMinI32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMinI32Vglobal>();

void GlobalAtomicMinI32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SMIN;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMinU32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMinU32Vglobal>();

void GlobalAtomicMinU32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::UMIN;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMaxI32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMaxI32Vglobal>();

void GlobalAtomicMaxI32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SMAX;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMaxU32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMaxU32Vglobal>();

void GlobalAtomicMaxU32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::UMAX;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicAndB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicAndB32Vglobal>();

void GlobalAtomicAndB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::AND;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicOrB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicOrB32Vglobal>();

void GlobalAtomicOrB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::OR;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicXorB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicXorB32Vglobal>();

void GlobalAtomicXorB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::XOR;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicIncU32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicIncU32Vglobal>();

void GlobalAtomicIncU32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::INC;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicDecU32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicDecU32Vglobal>();

void GlobalAtomicDecU32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::DEC;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicSwapB64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicSwapB64Vglobal>();

void GlobalAtomicSwapB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SWAP;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicCmpswapB64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicCmpswapB64Vglobal>();

void GlobalAtomicCmpswapB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::CMPSWAP;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 16);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 16 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 16 + 4], &val1, 4);
    uint32_t val2 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 2, lane);
    std::memcpy(&d->store_data[lane * 16 + 8], &val2, 4);
    uint32_t val3 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 3, lane);
    std::memcpy(&d->store_data[lane * 16 + 12], &val3, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicAddU64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicAddU64Vglobal>();

void GlobalAtomicAddU64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::ADD;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicSubU64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicSubU64Vglobal>();

void GlobalAtomicSubU64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SUB;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMinI64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMinI64Vglobal>();

void GlobalAtomicMinI64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SMIN;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMinU64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMinU64Vglobal>();

void GlobalAtomicMinU64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::UMIN;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMaxI64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMaxI64Vglobal>();

void GlobalAtomicMaxI64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SMAX;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMaxU64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMaxU64Vglobal>();

void GlobalAtomicMaxU64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::UMAX;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicAndB64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicAndB64Vglobal>();

void GlobalAtomicAndB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::AND;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicOrB64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicOrB64Vglobal>();

void GlobalAtomicOrB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::OR;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicXorB64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicXorB64Vglobal>();

void GlobalAtomicXorB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::XOR;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicIncU64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicIncU64Vglobal>();

void GlobalAtomicIncU64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::INC;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicDecU64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicDecU64Vglobal>();

void GlobalAtomicDecU64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::DEC;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalWbinvVglobal::execute_registered_ = register_exec_fn<GlobalWbinvVglobal>();

void GlobalWbinvVglobal::execute_impl(amdgpu::Wavefront &wf) { wf.cu().l1_vector().flush_all(); }

const bool GlobalAtomicCondSubU32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicCondSubU32Vglobal>();

void GlobalAtomicCondSubU32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::SUB;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMinNumF32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMinNumF32Vglobal>();

void GlobalAtomicMinNumF32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::FMIN;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMaxNumF32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMaxNumF32Vglobal>();

void GlobalAtomicMaxNumF32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::FMAX;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalLoadBlockVglobal::execute_registered_ = register_exec_fn<GlobalLoadBlockVglobal>();

void GlobalLoadBlockVglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 32;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalStoreBlockVglobal::execute_registered_ =
    register_exec_fn<GlobalStoreBlockVglobal>();

void GlobalStoreBlockVglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 32;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::STORECNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 128);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 128 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 128 + 4], &val1, 4);
    uint32_t val2 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 2, lane);
    std::memcpy(&d->store_data[lane * 128 + 8], &val2, 4);
    uint32_t val3 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 3, lane);
    std::memcpy(&d->store_data[lane * 128 + 12], &val3, 4);
    uint32_t val4 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 4, lane);
    std::memcpy(&d->store_data[lane * 128 + 16], &val4, 4);
    uint32_t val5 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 5, lane);
    std::memcpy(&d->store_data[lane * 128 + 20], &val5, 4);
    uint32_t val6 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 6, lane);
    std::memcpy(&d->store_data[lane * 128 + 24], &val6, 4);
    uint32_t val7 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 7, lane);
    std::memcpy(&d->store_data[lane * 128 + 28], &val7, 4);
    uint32_t val8 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 8, lane);
    std::memcpy(&d->store_data[lane * 128 + 32], &val8, 4);
    uint32_t val9 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 9, lane);
    std::memcpy(&d->store_data[lane * 128 + 36], &val9, 4);
    uint32_t val10 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 10, lane);
    std::memcpy(&d->store_data[lane * 128 + 40], &val10, 4);
    uint32_t val11 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 11, lane);
    std::memcpy(&d->store_data[lane * 128 + 44], &val11, 4);
    uint32_t val12 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 12, lane);
    std::memcpy(&d->store_data[lane * 128 + 48], &val12, 4);
    uint32_t val13 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 13, lane);
    std::memcpy(&d->store_data[lane * 128 + 52], &val13, 4);
    uint32_t val14 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 14, lane);
    std::memcpy(&d->store_data[lane * 128 + 56], &val14, 4);
    uint32_t val15 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 15, lane);
    std::memcpy(&d->store_data[lane * 128 + 60], &val15, 4);
    uint32_t val16 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 16, lane);
    std::memcpy(&d->store_data[lane * 128 + 64], &val16, 4);
    uint32_t val17 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 17, lane);
    std::memcpy(&d->store_data[lane * 128 + 68], &val17, 4);
    uint32_t val18 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 18, lane);
    std::memcpy(&d->store_data[lane * 128 + 72], &val18, 4);
    uint32_t val19 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 19, lane);
    std::memcpy(&d->store_data[lane * 128 + 76], &val19, 4);
    uint32_t val20 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 20, lane);
    std::memcpy(&d->store_data[lane * 128 + 80], &val20, 4);
    uint32_t val21 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 21, lane);
    std::memcpy(&d->store_data[lane * 128 + 84], &val21, 4);
    uint32_t val22 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 22, lane);
    std::memcpy(&d->store_data[lane * 128 + 88], &val22, 4);
    uint32_t val23 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 23, lane);
    std::memcpy(&d->store_data[lane * 128 + 92], &val23, 4);
    uint32_t val24 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 24, lane);
    std::memcpy(&d->store_data[lane * 128 + 96], &val24, 4);
    uint32_t val25 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 25, lane);
    std::memcpy(&d->store_data[lane * 128 + 100], &val25, 4);
    uint32_t val26 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 26, lane);
    std::memcpy(&d->store_data[lane * 128 + 104], &val26, 4);
    uint32_t val27 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 27, lane);
    std::memcpy(&d->store_data[lane * 128 + 108], &val27, 4);
    uint32_t val28 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 28, lane);
    std::memcpy(&d->store_data[lane * 128 + 112], &val28, 4);
    uint32_t val29 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 29, lane);
    std::memcpy(&d->store_data[lane * 128 + 116], &val29, 4);
    uint32_t val30 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 30, lane);
    std::memcpy(&d->store_data[lane * 128 + 120], &val30, 4);
    uint32_t val31 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 31, lane);
    std::memcpy(&d->store_data[lane * 128 + 124], &val31, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicAddF64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicAddF64Vglobal>();

void GlobalAtomicAddF64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::FADD;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicAddF32Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicAddF32Vglobal>();

void GlobalAtomicAddF32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::FADD;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalLoadTr16B128Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadTr16B128Vglobal>();

void GlobalLoadTr16B128Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 4;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->transpose = 4;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadTr8B64Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadTr8B64Vglobal>();

void GlobalLoadTr8B64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->transpose = 3;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalAtomicPkAddF16Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicPkAddF16Vglobal>();

void GlobalAtomicPkAddF16Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::FADD;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicPkAddBf16Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicPkAddBf16Vglobal>();

void GlobalAtomicPkAddBf16Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::FADD;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 4 + 0], &val0, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMinNumF64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMinNumF64Vglobal>();

void GlobalAtomicMinNumF64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::FMIN;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalAtomicMaxNumF64Vglobal::execute_registered_ =
    register_exec_fn<GlobalAtomicMaxNumF64Vglobal>();

void GlobalAtomicMaxNumF64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 8;
  d->num_elems = 1;
  d->is_load = amdgpu::gfx12_atomic_returns(inst_.th);
  d->atomic_op = amdgpu::AtomicOp::FMAX;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  uint32_t data_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t val0 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 0, lane);
    std::memcpy(&d->store_data[lane * 8 + 0], &val0, 4);
    uint32_t val1 = amdgpu::RegisterAccess(cu).read_vgpr(data_base + 1, lane);
    std::memcpy(&d->store_data[lane * 8 + 4], &val1, 4);
  }
  set_data(std::move(d));
}

const bool GlobalPrefetchB8Vglobal::execute_registered_ =
    register_exec_fn<GlobalPrefetchB8Vglobal>();

void GlobalPrefetchB8Vglobal::execute_impl(amdgpu::Wavefront &wf) { (void)wf; }

const bool GlobalLoadAsyncToLdsB8Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadAsyncToLdsB8Vglobal>();

void GlobalLoadAsyncToLdsB8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->lds_dst = true;
  d->lds_per_lane_addr = true;
  d->lds_base = wf.lds_base();
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lane_lds_addr = amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    d->per_lane_lds_addr[lane] = wf.lds_base() + lane_lds_addr;
  }
  set_data(std::move(d));
}

const bool GlobalLoadAsyncToLdsB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadAsyncToLdsB32Vglobal>();

void GlobalLoadAsyncToLdsB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->lds_dst = true;
  d->lds_per_lane_addr = true;
  d->lds_base = wf.lds_base();
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lane_lds_addr = amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    d->per_lane_lds_addr[lane] = wf.lds_base() + lane_lds_addr;
  }
  set_data(std::move(d));
}

const bool GlobalLoadAsyncToLdsB64Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadAsyncToLdsB64Vglobal>();

void GlobalLoadAsyncToLdsB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->lds_dst = true;
  d->lds_per_lane_addr = true;
  d->lds_base = wf.lds_base();
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lane_lds_addr = amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    d->per_lane_lds_addr[lane] = wf.lds_base() + lane_lds_addr;
  }
  set_data(std::move(d));
}

const bool GlobalLoadAsyncToLdsB128Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadAsyncToLdsB128Vglobal>();

void GlobalLoadAsyncToLdsB128Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 4;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->lds_dst = true;
  d->lds_per_lane_addr = true;
  d->lds_base = wf.lds_base();
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lane_lds_addr = amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    d->per_lane_lds_addr[lane] = wf.lds_base() + lane_lds_addr;
  }
  set_data(std::move(d));
}

const bool GlobalStoreAsyncFromLdsB8Vglobal::execute_registered_ =
    register_exec_fn<GlobalStoreAsyncFromLdsB8Vglobal>();

void GlobalStoreAsyncFromLdsB8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  const auto &lds = cu.lds();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 1);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lds_addr = wf.lds_base() + amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    lds.read(lds_addr, &d->store_data[lane * 1], 1);
  }
  set_data(std::move(d));
}

const bool GlobalStoreAsyncFromLdsB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalStoreAsyncFromLdsB32Vglobal>();

void GlobalStoreAsyncFromLdsB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  const auto &lds = cu.lds();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 4);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lds_addr = wf.lds_base() + amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    lds.read(lds_addr, &d->store_data[lane * 4], 4);
  }
  set_data(std::move(d));
}

const bool GlobalStoreAsyncFromLdsB64Vglobal::execute_registered_ =
    register_exec_fn<GlobalStoreAsyncFromLdsB64Vglobal>();

void GlobalStoreAsyncFromLdsB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  const auto &lds = cu.lds();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 8);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lds_addr = wf.lds_base() + amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    lds.read(lds_addr, &d->store_data[lane * 8], 8);
  }
  set_data(std::move(d));
}

const bool GlobalStoreAsyncFromLdsB128Vglobal::execute_registered_ =
    register_exec_fn<GlobalStoreAsyncFromLdsB128Vglobal>();

void GlobalStoreAsyncFromLdsB128Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 4;
  d->is_load = false;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  const auto &lds = cu.lds();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vsrc.opr_type_, vsrc.encoding_value_, vsrc.vgpr_msb_role());
  d->store_data.resize(wf.wf_size() * 16);
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lds_addr = wf.lds_base() + amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    lds.read(lds_addr, &d->store_data[lane * 16], 16);
  }
  set_data(std::move(d));
}

const bool ClusterLoadB32Vglobal::execute_registered_ = register_exec_fn<ClusterLoadB32Vglobal>();

void ClusterLoadB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  d->request_force_l1_bypass = true;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool ClusterLoadB64Vglobal::execute_registered_ = register_exec_fn<ClusterLoadB64Vglobal>();

void ClusterLoadB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  d->request_force_l1_bypass = true;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool ClusterLoadB128Vglobal::execute_registered_ = register_exec_fn<ClusterLoadB128Vglobal>();

void ClusterLoadB128Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 4;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  d->request_force_l1_bypass = true;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool ClusterLoadAsyncToLdsB8Vglobal::execute_registered_ =
    register_exec_fn<ClusterLoadAsyncToLdsB8Vglobal>();

void ClusterLoadAsyncToLdsB8Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 1;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->lds_dst = true;
  d->lds_per_lane_addr = true;
  d->lds_base = wf.lds_base();
  d->cluster_multicast = true;
  d->cluster_mcast_mask = wf.m0() & amdgpu::kClusterMulticastMask;
  d->request_force_l1_bypass = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lane_lds_addr = amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    d->per_lane_lds_addr[lane] = wf.lds_base() + lane_lds_addr;
  }
  set_data(std::move(d));
}

const bool ClusterLoadAsyncToLdsB32Vglobal::execute_registered_ =
    register_exec_fn<ClusterLoadAsyncToLdsB32Vglobal>();

void ClusterLoadAsyncToLdsB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->lds_dst = true;
  d->lds_per_lane_addr = true;
  d->lds_base = wf.lds_base();
  d->cluster_multicast = true;
  d->cluster_mcast_mask = wf.m0() & amdgpu::kClusterMulticastMask;
  d->request_force_l1_bypass = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lane_lds_addr = amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    d->per_lane_lds_addr[lane] = wf.lds_base() + lane_lds_addr;
  }
  set_data(std::move(d));
}

const bool ClusterLoadAsyncToLdsB64Vglobal::execute_registered_ =
    register_exec_fn<ClusterLoadAsyncToLdsB64Vglobal>();

void ClusterLoadAsyncToLdsB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->lds_dst = true;
  d->lds_per_lane_addr = true;
  d->lds_base = wf.lds_base();
  d->cluster_multicast = true;
  d->cluster_mcast_mask = wf.m0() & amdgpu::kClusterMulticastMask;
  d->request_force_l1_bypass = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lane_lds_addr = amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    d->per_lane_lds_addr[lane] = wf.lds_base() + lane_lds_addr;
  }
  set_data(std::move(d));
}

const bool ClusterLoadAsyncToLdsB128Vglobal::execute_registered_ =
    register_exec_fn<ClusterLoadAsyncToLdsB128Vglobal>();

void ClusterLoadAsyncToLdsB128Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->elem_size = 4;
  d->num_elems = 4;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::ASYNCCNT;
  d->lds_dst = true;
  d->lds_per_lane_addr = true;
  d->lds_base = wf.lds_base();
  d->cluster_multicast = true;
  d->cluster_mcast_mask = wf.m0() & amdgpu::kClusterMulticastMask;
  d->request_force_l1_bypass = true;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  auto &cu = wf.cu();
  uint64_t exec = wf.exec();
  // flat_calculate_addresses applies ioffset to the global side; the LDS operand is independent.
  uint32_t lds_addr_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  for (uint32_t lane = 0; lane < wf.wf_size(); ++lane) {
    if (!(exec & (1ULL << lane)))
      continue;
    uint32_t lane_lds_addr = amdgpu::RegisterAccess(cu).read_vgpr(lds_addr_base, lane);
    d->per_lane_lds_addr[lane] = wf.lds_base() + lane_lds_addr;
  }
  set_data(std::move(d));
}

const bool GlobalLoadMonitorB32Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadMonitorB32Vglobal>();

void GlobalLoadMonitorB32Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 1;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadMonitorB64Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadMonitorB64Vglobal>();

void GlobalLoadMonitorB64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadMonitorB128Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadMonitorB128Vglobal>();

void GlobalLoadMonitorB128Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 4;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadTr4B64Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadTr4B64Vglobal>();

void GlobalLoadTr4B64Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 2;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->transpose = 1;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

const bool GlobalLoadTr6B96Vglobal::execute_registered_ =
    register_exec_fn<GlobalLoadTr6B96Vglobal>();

void GlobalLoadTr6B96Vglobal::execute_impl(amdgpu::Wavefront &wf) {
  auto d = std::make_unique<amdgpu::VectorMemState>(amdgpu::GLOBAL_MEM);
  d->dst_reg_base =
      wf.vgpr_alloc().base +
      *Isa::resolved_vgpr_offset(wf, vdst.opr_type_, vdst.encoding_value_, vdst.vgpr_msb_role());
  d->elem_size = 4;
  d->num_elems = 3;
  d->is_load = true;
  d->wait_counter_type = amdgpu::WaitCounterType::LOADCNT;
  d->transpose = 2;
  d->mtype = amdgpu::mtype_from_flags_gfx12(inst_.scope, inst_.th);
  d->non_temporal = 0;
  flat_calculate_addresses(inst_, wf, *d);
  set_data(std::move(d));
}

} // namespace gfx1250
} // namespace rocjitsu
